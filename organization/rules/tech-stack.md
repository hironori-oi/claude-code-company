# 技術スタック方針

## 基本方針
スピード・コスパ・柔軟性を重視し、以下を標準技術スタックとする。
案件要件に応じて柔軟に調整するが、特別な理由がない限りこの構成をベースとする。

## 標準技術スタック

### フロントエンド
| 技術 | 用途 | 備考 |
|------|------|------|
| **Next.js** (App Router) | フレームワーク | Server Components を標準、`'use client'` は必要な箇所のみ |
| **TypeScript** | 言語 | 全プロジェクトで必須 |
| **Tailwind CSS** | スタイリング | ユーティリティファーストで統一 |
| **shadcn/ui** | UIコンポーネント | Radix UI ベース、コードとして管理、カスタマイズ自由 |
| **next/font (Geist)** | フォント | レイアウトシフト防止、Geist Sans + Geist Mono |

### バックエンド
| 技術 | 用途 | 備考 |
|------|------|------|
| **Next.js API Routes / Server Actions** | API・データ処理 | Server Actions をデータ変更の標準手段とする |
| **Supabase** | BaaS | DB・認証・ストレージの統合基盤 |

### データベース
| 技術 | 用途 | 備考 |
|------|------|------|
| **Supabase (PostgreSQL)** | メインDB（有料案件） | フルマネージド、リアルタイム対応 |
| **Neon** | メインDB（無料枠利用時） | サーバーレスPostgreSQL、コスト最適化 |

### 認証
| 技術 | 用途 | 備考 |
|------|------|------|
| **Supabase Auth** | ユーザー認証 | メール/パスワード、OAuth対応 |

### ストレージ
| 技術 | 用途 | 備考 |
|------|------|------|
| **Supabase Storage** | ファイルストレージ（Supabase案件） | Supabase利用案件ではこちらを優先 |
| **Vercel Blob** | ファイルストレージ（Neon案件） | Neon利用時やVercel完結案件で使用 |

### ホスティング・デプロイ
| 技術 | 用途 | 備考 |
|------|------|------|
| **Vercel** | ホスティング | GitHub連携で自動デプロイ |
| **GitHub** | ソースコード管理 | 全案件で必須 |

### AI機能（案件で必要な場合）
| 技術 | 用途 | 備考 |
|------|------|------|
| **Ollama** | ローカルLLM | 開発・検証、コスト削減、プライバシー重視案件 |
| **OpenAI API** | クラウドLLM | GPT系モデル、本番利用 |
| **AI SDK (Vercel)** | AI統合フレームワーク | streamText / useChat でストリーミングUI |

### メール（案件で必要な場合）
| 技術 | 用途 | 備考 |
|------|------|------|
| **Resend** | トランザクションメール | 必要に応じて導入 |

### 決済（将来対応）
| 技術 | 用途 | 備考 |
|------|------|------|
| **Stripe** | 決済機能 | 将来的に導入予定、現時点では未使用 |

## 技術選定ルール

### 新技術の導入基準
1. 標準スタックで対応できない場合のみ検討する
2. リサーチ部門が調査し、メリット/デメリットを比較する
3. CEOが最終判断する
4. 採用した場合は本ドキュメントを更新する

### 禁止事項
- jQuery の使用（レガシーコード除く）
- CSS-in-JS（styled-components, emotion 等）の新規採用
- 未メンテナンスのライブラリの採用（最終更新1年以上前）
- `any` 型の安易な使用（TypeScript の型安全性を維持）

### DB選定の判断基準
| 条件 | 選択 |
|------|------|
| 有料案件でリアルタイム機能が必要 | Supabase |
| 有料案件で認証+DB+ストレージを統合したい | Supabase |
| 無料枠でコストを抑えたい | Neon |
| DB のみ必要（認証は別手段） | Neon |

### ストレージ選定の判断基準
| 条件 | 選択 |
|------|------|
| Supabase を使っている案件 | Supabase Storage |
| Neon + Vercel の構成 | Vercel Blob |
| 大容量ファイル（5TB以下） | Vercel Blob |

## デザインシステム

### shadcn/ui 運用ルール
- `npx shadcn@latest init` でプロジェクト初期化
- 必要なコンポーネントを `npx shadcn@latest add {component}` で追加
- コンポーネントはプロジェクトのソースコードとして管理（カスタマイズ可能）
- `cn()` ユーティリティ（clsx + tailwind-merge）を使用
- カラーはCSS変数ベースのテーマを使用

### ダークモード/ライトモード
- 全案件でダーク/ライトモード切り替えをサポートする
- `next-themes` を使用してテーマ切り替えを実装
- `className="dark"` を `<html>` に適用する方式
- デフォルトは `system`（OS設定に追従）

---

## 標準アーキテクチャパターン（PRJ-002由来）

### 1. データフェッチング＋キャッシュ（必須）
全プロジェクトで以下のパターンを標準とする：
- **SWRキャッシュ**: `cachedFetch()` でTTL内はAPI呼び出しゼロ。TTL切れは再取得してReact stateに反映
- **同期初期値**: `cacheGetSync()` で `useState` 初期値にキャッシュを注入し、ローディングフラッシュを排除
- **プリフェッチ**: ログイン後/テナント切替後に `requestIdleCallback` で全データを事前取得
- **キャッシュ無効化**: 書き込み操作で `cacheInvalidateByPrefix()` を必ず呼ぶ

### 2. 外部サービス連携（必須）
ブラウザから外部サービスへの直接通信は禁止。必ずサーバーサイドプロキシを経由する：
- **API Route プロキシ**: `/api/data/xxx` → サーバーサイドで認証情報を解決 → 外部API
- **認証情報**: DB設定から `configId` で取得（ブラウザに認証情報を送信しない）
- **例外**: 接続テスト（設定保存前）のみ直接config送信を許可

### 3. マルチテナント設計（該当案件で必須）
- **RLS**: 全テーブルで `ENABLE ROW LEVEL SECURITY` + テナントスコープポリシー
- **UNIQUE制約**: 必ず `(カラム, tenant_id)` の複合UNIQUEにする（単体UNIQUEは禁止）
- **ストレージ抽象化**: 最初から `async` インターフェースで設計（localStorage⇔Supabase切替可能）
- **テナントストレージプロバイダー**: React Contextで `useTenantStorage()` を提供

### 4. セキュリティ標準（必須）
- **next.config.ts**: Strict-Transport-Security, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy を設定
- **API認証**: 全ルートで `requireAuth()` を呼ぶ。認証なしエンドポイントは禁止
- **エラーハンドリング**: `apiError()` で構造化JSON出力。内部エラーメッセージをクライアントに漏洩させない
- **Supabase Storage**: バケットRLSでテナント分離（パスプレフィックスベース）
- **.env.local**: `.gitignore` で除外（.env.local.example にダミー値のみ記載）

### 5. コードレビュー基準（必須）
- **CRITICAL**: 本番ブロッカー（認証バイパス、データ漏洩、無制限リソース消費）
- **HIGH**: 本番前に修正必須（エラーハンドリング不備、競合状態、N+1クエリ）
- **MEDIUM**: 推奨修正（コード重複、型安全性、UX改善）
- **LOW**: 軽微（コメント更新、スタイル統一）
- CRITICAL/HIGHが残っている状態ではマージ不可

### 6. DBマイグレーション運用（必須）
- Phase番号付きの段階的マイグレーション（`data-migration-phaseN.sql`）
- DEPLOYMENT.md にマイグレーション適用手順を記載
- 本番適用前にステージングで動作確認
- RLS無効化SQL（デバッグ用）は本番実行禁止の警告を明記

### デザイン原則
- AI感が出すぎないクリーンなデザインを心がける
- ミニマルだが温かみのあるデザイン
- zinc/neutral 系のニュートラルカラーをベースに、アクセントカラーは1色
- タイポグラフィとスペーシングで階層を表現する
- モバイルファーストのレスポンシブデザイン
- アニメーションは控えめに（`framer-motion` は必要な場合のみ）
