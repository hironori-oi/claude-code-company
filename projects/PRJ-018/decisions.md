# PRJ-018 意思決定記録（decisions.md）

| ID | 日付 | 決裁者 | 決定内容 | 理由 |
|---|---|---|---|---|
| DEC-018-001 | 2026-05-01 | CEO（オーナー承認済） | プロダクト名を **「Asagi（浅葱）」** に確定 | Sumi（墨）= Claude Code に対し、Codex 版は色対比で差別化。浅葱色（青緑）は OpenAI/ChatGPT の緑系ブランドと親和し、Sumi（黒）との視覚対比が美しい。Fude（筆）案も最終候補だったがオーナー判断で Asagi 採用 |
| DEC-018-002 | 2026-05-01 | CEO（オーナー承認済） | Sumi とは **完全独立アプリ・独立リポジトリ** で開発 | DEC-081（Sumi は Claude Code 専用方針）の延長で、混載は両プロダクトのブランドを毀損する。長期保守性を犠牲にしても独立性を優先。コードの安易な共有を禁止し、Asagi 固有の最適設計を可能にする |
| DEC-018-003 | 2026-05-01 | CEO（オーナー承認済） | Codex 統合方式は **ChatGPT サブスク認証 + Codex CLI sidecar** | Sumi の「Claude Agent SDK Node sidecar」と対称構造。素人ユーザの認証摩擦を最小化（API Key 不要、ブラウザ OAuth で完結）。同梱配布で 1 ステップインストール堅持 |
| DEC-018-004 | 2026-05-01 | CEO（オーナー承認済） | 対象プランは **オーナー契約の Codex x5 プラン** を Must、他プラン互換は Should | オーナー本人 dogfooding 前提。Codex x5 の正式名称・利用枠倍率はリサーチ部門で裏取り |
| DEC-018-005 | 2026-05-01 | CEO（オーナー承認済） | **即着手**（Sumi v1.32.0 等の他タスクより優先） | オーナー判断 |
| DEC-018-006 | 2026-05-01 | CEO（オーナー承認済） | **差別化 4 軸（A 日本語 UI / B Linear 級デザイン / C Slack 風 Multi-Project + Multi-Sidecar / D ローカル永続化 + FTS5）を Codex 文脈で全踏襲** | Sumi での実証済み軸であり、Codex 公式 IDE 拡張（VSCode 中心）が手薄な領域。Sumi の知見を「設計参考」として活用、コード共有はしない |
| DEC-018-007 | 2026-05-01 | CEO | **Sumi の機能追加・除外なし**（オーナー回答） | 起案時点では Sumi の機能セットをそのまま Codex 版に翻訳。差分が必要なら M1 Gate でレビュー |
| DEC-018-008 | 2026-05-01 | CEO | **コード資産流用なし**（DEC-018-002 完全独立に従う） | Sumi のソースを物理的に参照・コピーすることを禁止。設計思想・UX パターンの参考のみ可。新規実装で Asagi 固有の最適化を追求 |
| DEC-018-009 | 2026-05-01 | CEO（リサーチ v1 受領後） | Codex 統合は **`codex app-server --listen stdio`（JSON-RPC 2.0）を Tauri Rust 側から子プロセス spawn** で確定。TypeScript SDK 経由は禁止 | サブスク認証は公式 CLI/app-server のみサポート（OpenAI 公式許可）。TS SDK 経由のサブスク認証は OpenAI が "not planned" で却下済（issue #8494）。VSCode/JetBrains 公式拡張も同じ app-server に乗っており、Asagi も同パターンで安全 |
| DEC-018-010 | 2026-05-01 | CEO（リサーチ v1 受領後） | **Phase 0 POC（半日工数）を v0.1.0 着手前の必須ゲートに設定** | 実機で 5 項目（Win11 で `codex login` OAuth / `codex app-server` JSON-RPC 1 往復 / Pro 5x で gpt-5.5 1 ターン / 画像入力 1 ターン / JobObject で Codex プロセス掌握）を通すまで本実装着手しない。POC 失敗時は再リサーチに戻す |
| DEC-018-011 | 2026-05-01 | CEO（リサーチ v1 受領後） | **RAs-11（ChatGPT サブスク TOS 解釈）を中→低にリスク格下げ**、ただし商用配布検討時に再評価必須 | リサーチで「個人ローカル + 未改変 Codex CLI 同梱 + サブスク OAuth」は OpenAI メンテナが Discussion #8338 で許容明言と確認済。Apache-2.0 ライセンスとも整合。商用 SaaS 再配布は別案件として法務確認 |
| DEC-018-012 | 2026-05-01 | CEO（リサーチ v1 受領後） | **Node sidecar 層を撤廃**：Codex 自身が Rust 実装のため、Tauri Rust process から `codex app-server` を直接 spawn する構成に確定（Sumi の Node 24 LTS sidecar 構造は採用しない） | リサーチ § 7 推奨。レイテンシ削減（IPC 1 段減）・依存削減（Node 24 同梱不要）・配布サイズ削減（〜30MB 減見込み）の三重効果。Asagi が Sumi より構造的にシンプル化できる根拠 |
| DEC-018-013 | 2026-05-02 | CEO（オーナー指示） | リポジトリは **`hironori-oi/Asagi`（大文字 A）** で確定。`app/asagi-app/` 配下にローカル作業ツリーを配置 | オーナーがブラウザで作成済み。当初仮称 `asagi`（小文字）から大文字 `Asagi` に変更（プロダクト名の自然な表記と統一）。`app/asagi-app/` の suffix は既存 `app/poc/` `app/design/` との衝突回避 |
| DEC-018-014 | 2026-05-02 | CEO（オーナー「実装を進めて」指示後） | **DEC-018-010 の Phase 0 POC ゲートをハイブリッド運用に緩和**：Codex sidecar 統合タスク（AS-110, AS-115, AS-118 等）は POC 通過後着手厳守、それ以外（雛形・Welcome・SQLite・ProjectRail・デザイン適用）は POC 結果を待たず即時着手可 | オーナー判断「実装を進めて」を尊重しつつ廃棄リスクを最小化。雛形は実装全体の 60% を占めるが Codex 統合に依存しない。POC 失敗時も雛形は別 LLM 切替で再利用可能。POC 待ちのアイドル時間（推定 1〜2 日）を削減 |
| DEC-018-015 | 2026-05-02 | CEO（オーナー指示） | **ロゴは AIDesigner MCP で 3 案を実画像生成**、オーナー判断で 1 案を v1 採用 | デザイン v1 で 3 案（漢字「浅」/グラデーション円+モノグラム/葦の葉）が文章ベースで提案済。実画像化してオーナーが視覚判断する段階 |
| DEC-018-016 | 2026-05-02 | CEO（オーナーフィードバック後） | **ロゴ v1（3 案）は全案却下**。v2 方針: ①文字（漢字・モノグラム）を使わない／②「Asagi（浅葱）」を象徴する具象 or 抽象モチーフ／③Sumi ロゴと同等以上の重厚感／④シンプルさは維持 | オーナー所感「3 案は少し安っぽい」「Sumi のようにシンプルでありながら重厚感のある素晴らしいデザイン」。案 A（漢字「浅」）は CEO 推奨だったが文字使用で却下条件に該当。Sumi のロゴ実物をベンチマークとして再起案する |
| DEC-018-017 | 2026-05-02 | CEO（オーナー指示） | **AS-107 以降の Codex 統合非依存タスクを継続着手**（TitleBar / StatusBar / ProjectRail / ChatPane 構造）。Phase 0 POC 待ちと並行 | オーナー指示「続きの作業はどんどん進めて」。DEC-018-014 のハイブリッド運用方針を継続適用、待ち時間ゼロ運用 |
| DEC-018-018 | 2026-05-02 | CEO（オーナーフィードバック後） | **ロゴ v2 も全案保留 → v3 として再起案**。新方針: ① **stitch MCP 活用**（AIDesigner credit 不足の代替）／② **案 C（絞り染め印章）を中心軸**にバリエーション展開／③ 「重厚感」→ **「Sumi のような趣（おもむき）のある感じ」に方向修正**（侘び寂び・控えめな美・余白・自然な味わい・即興性） | オーナー所感「案 C が良さそうな気がする、実物を見て判断したい」「重厚感といったが、Sumi のような趣のある感じで」。Sumi の「かすれの一筆」が持つ即興性・素朴さ・余韻を Asagi にも翻訳。古銅鏡（v2 案 A）の museum 級重さは方向違いと判断 |
| DEC-018-019 | 2026-05-02 | CEO（オーナー指示） | **AS-114 以降の追加 UI 実装を継続着手**（コマンドパレット / Welcome 強化 / SQLite UI 統合 / テーマ切替）。Phase 0 POC 待ちと並行 | オーナー指示「その他実装が進められるところがあれば進めて」。DEC-018-014 / 017 の継続。Codex 実 API には触れない、UI / 永続化 / 体験完成度の積み上げ |
| DEC-018-020 | 2026-05-02 | CEO（オーナー承認） | **ロゴ v1 公式採用：γ 浅葱滴（あさぎしずく）**。ファイル `logo-v3-gamma-asagi-drop.png` を Asagi の正式アプリアイコンとして確定 | オーナー判断「CEO 推奨のロゴで行きましょう」。CEO 推奨理由（兄弟構造最強・浅葱の純粋な体現・趣 5 原則全網羅・スケーラビリティ）を承認。次フェーズで SVG 化 + multi-size 展開（.ico / .icns / .png）+ ライト/ダーク色変種 + アプリ組込み実施 |
| DEC-018-021 | 2026-05-02 | CEO（オーナー指示） | **AS-META-01 以降の品質基盤・運用基盤タスクを継続着手**（CI 設定 / README 整備 / 設定永続化 / エラーハンドリング / a11y 監査 / アイコン組込）。Phase 0 POC 待ちと並行 | オーナー指示「続きの実装を進めて」。Codex 統合系以外の進捗源が UX 強化で一巡したため、メタ品質（CI・テスト・ドキュメント・運用基盤）の積み上げに移行。M2 / M3 段階での回収率を最大化 |
| DEC-018-022 | 2026-05-02 | CEO（オーナー指示） | **AS-130 系列「Codex 統合の足場準備」を OpenAI API 不要の範囲で先行着手**：①codex_sidecar.rs trait + mock impl ／②JSON-RPC 2.0 メッセージ型（schemas/codex-app-server.ts）／③mock app-server（dev 時 Codex なしで sidecar 起動）／④JobObject Win11 単体テスト ／⑤Multi-Sidecar HashMap dry-run。Phase 0 POC 待ちと並行 | DEC-018-014/017/019/021 のハイブリッド運用継続。非 Codex タスクが UX/品質基盤で枯渇したため、Codex 統合の "API を叩かない範囲" に進撃。POC 通過の瞬間に本実装が即起動できる「発射台」を構築。RAs-14（schema 変動）対策としてリサーチも同時実行 |
| DEC-018-023 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きを進めて」） | **mock 実装の API surface を Real Codex app-server に完全準拠リネーム**：method 名（`codex/chat` → `turn/start` 等）、event 名（`agent:*:assistant_message_delta` → `agent:*:item/agentMessage/delta` 等）、ハンドシェイク（`initialize` → `initialized` 必須）、型形を Real LSP 風プロトコルに揃える。内部応答は決定論的スタブ維持 | リサーチ v2 重大発見「mock-Real 乖離」への即時対応。POC 通過時の手戻りゼロ化が最大価値。mock を「契約サーバ」化することで POC 検証時にも mock が動き続け、検証カバレッジが上がる副次効果。修正提案 P-1〜P-6（行番号付き）に従う。工数 1〜1.5 日想定 |
| DEC-018-024 | 2026-05-02 | CEO（M1 末で再決裁、保留） | **バンドル対象を `codex` 本体 vs `codex-app-server` 単体 のいずれかに M1 末で確定**（現時点では決裁保留） | リサーチ v2 RAs-15。Phase 0 POC 結果と Real impl 着手後の実体験を踏まえないと判断材料不足。M1 末に再評価を確約 |
| DEC-018-025 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きを進めて」） | **`MAX_CONCURRENT_SIDECARS = 8 → 6` に下方修正** | リサーチ v2 RAs-17（third-party OAuth で 429 quota exceeded 報告、openai-python issue #2951）対策。コード上は定数 1 行修正のみ、低コスト・高安全。実運用で 6 を超えるケースが顕在化したら個別判断 |
| DEC-018-026 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きの実装を進めて」） | **次ラウンド 3 並行ラインを採択**：①Dev: ChatPane mock UX 磨き込み（typing indicator / token カウント / 中断ボタン）／②Dev/Designer: AS-DESIGN-04 SVG 化最終確認 + AS-META-09 連動（tauri.conf.json icons）／③Research: M2 (Codex 実 API 接続安定化) 前倒しリサーチ（rate limit / retry+backoff / 自動 refresh 障害ケース） | sixth round で「mock 完結 IDE」の完成度が高まったため、POC 通過時に「Real 切替後の体験完成度」「品質基盤完了率」「M2 設計の即着手準備」を最大化する 3 軸に進撃。非 Codex タスクの枯渇加速を、M2 設計前倒しでカバー |
| DEC-018-027 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きの実装を進めて」） | **M2 範囲を Must 4 機能（F1 Outer Retry Layer / F2 Rate Limit Dashboard / F3 Auth Watchdog / F4 Long-running thread 健全性監視）+ Should 3 機能（F5 / F6 / F7）に確定**。Could (F8) は M2 末再評価。総工数 11 日（Must 6.0 + Should 3.5 + Could 1.5） | リサーチ「research-m2-stability-pre.md」の主要発見 5 点 + Quick Win 3 件分析を根拠に採択。POC 通過直後に並列起動可能な「M2 設計図」の確定 |
| DEC-018-028 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きの実装を進めて」） | **Quick Win 3 件（QW1 F3 Auth Watchdog 1.0 日 → QW2 F1 Outer Retry Layer 1.5 日 → QW3 F4 idle 監視 0.5 日）を POC 通過直後の最優先順序つき先行実装** | 30 日後の dogfood 停止を OAuth サイレント失敗から守る F3 を最優先、CLI 内部 retry 失敗の最後の砦 F1 を 2 番手、thread 健全性最小実装 F4 を 3 番手。3 営業日で M2 価値の核 30% 確保 |
| DEC-018-029 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きの実装を進めて」） | **F2 Rate Limit Dashboard 仕様を「2 bucket（5-hour + weekly）+ resetsAt + 80%/95% 警告」最小要件で確定** | リサーチ発見 #2「ChatGPT Pro 5x は 5-hour と weekly の 2 windows 同時カウント、weekly 枯渇で 5-hour に余裕があっても全停止」が決定的。Asagi が 2 bucket を同時表示することは Must。`account/rateLimits/read.rateLimitsByLimitId` API が利用可能 |
| DEC-018-030 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きの実装を進めて」） | **画像送信は `localImage` default、base64 fallback と明確化**。前処理ライブラリで PNG/JPEG 変換を Asagi 側で必須実施（BMP/TIFF/SVG/HEIC 不可） | Codex 公式は PNG/JPEG のみサポート、Asagi が前処理して透過的に扱う方針。`localImage` (file:// パス渡し) はメモリ効率最良、`base64` は HTTPS 通過時のみ fallback |
| DEC-018-031 | 2026-05-02 | CEO（オーナー暗黙承認 = 「続きの実装を進めて」） | **AS-META-09 (アイコン Tauri/Next.js 設定) を Done 確定**。M1-STATUS.md の Blocked リストから解除 | Dev ② が DoD 16/16 全充足を実証（SVG マスター不要修正 / PNG-ICO-ICNS 全 9 ペアハッシュ完全一致 / tauri.conf.json bundle.icon 必須 5 ファイル全列挙 / layout.tsx Metadata に PNG favicon + apple-touch-icon + Viewport.themeColor 追加完了 / tauri info pass）。次 PM ラウンドで反映 |
| DEC-018-032 | 2026-05-02 | CEO（オーナー明示承認「OK Phase 1 進めて」） | **Phase 0 POC 5 項目 全 PASS 確定 → Phase 1（M1 Real impl, AS-140〜143）着手承認**。DEC-018-010 ゲート完全通過、DEC-018-014 ハイブリッド運用条項を「POC 通過後」状態に確定化、Blocked 4 件（AS-110/111/112/120/121/124/140/141/142/143）解放。AS-140 工数を当初見積 6h → 実測ベース 4〜6h に下方更新（足場 AS-130〜137 完備 + 実機契約 5 点判明により実装は §4 TODO 消化のみ） | POC #1 OAuth (auth.json 検証 PASS) / #2 initialize 158ms PASS / #3 1 ターンチャット 6.99s 「こんにちは、どうぞよろしくお願いします。」/ #4 画像入力 7.34s Docker Desktop 画像認識 PASS / #5 self-verifying JobObject Win11 KILL_ON_JOB_CLOSE 実機 PASS（BREAKAWAY 拒否でも nested Job 動作確認、codex exit 0）。RAs-14 (schema 変動) は実機契約定数化で軽減、RAs-12 (JobObject 不確実性) 完全解消 |
| DEC-018-033 | 2026-05-02 | CEO（オーナー明示承認延長 = Phase 1 着手承認の派生） | **Codex CLI 0.128.0 起動契約 5 点を Asagi `codex_sidecar/contract.rs` 定数として codify**：①`--listen stdio://` URL スキーム必須（bare `stdio` は ENOENT）／②Windows は実体 codex.exe を直接 spawn（`%APPDATA%\npm\node_modules\@openai\codex\node_modules\@openai\codex-win32-x64\vendor\x86_64-pc-windows-msvc\codex\codex.exe`、cmd ラッパ経由は JobObject に乗らない）／③`thread.id` は `thread/started` notification の `params.thread.id` から取得（response result race あり、最大 2 秒 polling 必須）／④assistant 応答は `item/completed` の `params.item.type === 'agentMessage'` + `params.item.text`（旧版想定の `assistantMessage` + `content[].text` ではない）／⑤streaming は `item/agentMessage/delta` の `params.delta` を結合 | リサーチ § 5「Codex schema は version drift する」想定通り 0.128.0 で旧仕様と乖離発見 → POC で実機検証済の値をハードコード定数化することで RAs-14 を「中→低」へ再格下げ。M1 Real impl で本契約から外れた瞬間に CI が即赤になる（AS-Q-03 codex-schema-watch 連動）|
| DEC-018-034 | 2026-05-02 | CEO（PM WBS 受領後、Phase 1 実装オーソリ） | **PM (Tomoe) AS-140 WBS 分解（Critical Path 10.5h／並列込み M1 Real impl 全工程 29.5h）を全文承認 + Dev (Yuto) へ AS-140.0 contract.rs 物理実装の正式発注。同時に M2 Quick Win F3 (Auth Watchdog) Asagi 側 5min poller skeleton + Zustand state stub を AS-141 と同 PR で先行同梱（+0.5h、合計 4.5h）を承認** | (1) PM WBS は依存グラフ・Critical Path・DoD・PR 粒度・実行コマンドまで完全網羅、CEO 受領レビューで修正要望ゼロ。(2) F3 stub 同梱は DEC-018-028（QW1 F3 1.0d 前倒し）と完全整合 + R-WBS-6（OAuth サイレント切れ）の予防に直結 + auth.rs と F3 poller は同レイヤ（auth state 共有）で同 PR 化が論理的に最適。(3) 既存 `real.rs` モジュールコメントの旧仕様 `--listen stdio` を AS-140.0 で `stdio://` 修正することを発注書に明記済（DEC-018-033 ① 整合確保）。(4) PM が R-WBS-1〜R-WBS-6 を新規リスクとして提起 → risks.md v1.4 に「M1 Phase 1 実装リスク」セクションとして取り込みを承認。(5) CEO 決裁ゲート ① (AS-140.6 smoke→DEC-018-034 候補) は本決裁とは別件として AS-140.6 完了時に DEC-018-035 として別途起票予定 |
| DEC-018-035 | 2026-05-02 | CEO（オーナー実機 smoke 全 ✅ 直後、Phase 1 M1 Real impl ゲート ① 通過判定） | **AS-140 全 7 サブタスク (140.0〜140.6) 完遂 + オーナー実機 smoke 1〜3 全 PASS を以て Phase 1 M1 Real impl Critical Path クリア確定。**後続 AS-141 (4.5h、keyring 2 + 自動 refresh + F3 Auth Watchdog stub 同梱) / AS-142 (3h、image_paste 経路、contract::IMAGE_INPUT_TYPE / IMAGE_URL_FIELD 参照) / AS-143 (1h、Tauri 親プロセス JobObject integration test、R-WBS-3 解消) **3 タスク並列着手を Dev (Yuto) に正式発注承認**。統合 AS-144 (mock-only E2E + real-only smoke) → AS-145 (dogfood pass → CEO 決裁ゲート ②) で M1 完成。 | (1) **smoke 1 (factory route test) PASS**: `ASAGI_SIDECAR_MODE=real` で `RealCodexSidecar` が未起動 (`is_alive=false`) で生成、構造体配線健全性実証。(2) **smoke 2 (initialize handshake) PASS**: 172ms（期待 < 1000ms、5.8x 高速）で Codex CLI 0.128.0 と JSON-RPC 完全握手、`userAgent: asagi-poc/0.128.0`、起動引数 `codex app-server --listen stdio://` が DEC-018-033 ① と完全整合、contract.rs `0.128.0-asagi-contract-v1` baseline と実機 schema 完全一致確認。(3) **smoke 3 (1 turn 日本語チャット) PASS**: 8.08s（期待 < 60s、7.4x 高速）で gpt-5.5 から日本語応答「こんにちは、お手伝いできます。」取得 — Pro 5x プラン OAuth 認証 + thread/start + turn/start + agentMessage 抽出が end-to-end で動作実証、Phase 1 M1 Real impl ゴール達成。(4) cargo test 47/47 PASS、AS-140 範囲 clippy 0 regression、PM § 6.5 厳守事項 5/5 DONE（hardcode 0 件 grep 確認済）。(5) 副次観察: 未知 notification `remoteControl/status/changed` (`status: "disabled"`) を 1 件観測 → Phase 2 follow-up で contract.rs 登録 + opt-out 候補化検討（実害なし、broadcast 経由 fan-out のみ）。(6) AS-141 同 PR 内 F3 Auth Watchdog stub 同梱は DEC-018-034 で承認済、本 DEC では再確認のみ。(7) **R-WBS-3 (Tauri 親プロセス JobObject) は AS-143 で解消必須**、smoke では cmd 親シェルでの POC #5 既実証範囲のみ確認。 |
| DEC-018-036 | 2026-05-02 | CEO（Review 部門「条件付き承認」受領、AS-144 着手承認） | **AS-140 / AS-141 / AS-142 / AS-143 実装 3 commits (`1817049` / `c22b362` / `e9aefbe`) を Review 部門「条件付き承認」を以て merge 確定 + AS-144 (ChatPane real 切替 2h) を Dev (Yuto) に正式発注承認**。条件として AS-CLEAN-01 (clippy 既存 5 件 = 全件 commit `72ee23e` 由来 backlog 由来、AS-140〜143 由来 0 件) / AS-CLEAN-02 (`protocol.rs` ↔ `contract.rs` schema 文字列の single-source 統一 + mock の `agentMessage` 生リテラル置換) を tasks.md に backlog 登録。AS-143 owner smoke 5 (`real_spawn_terminates_via_job_drop -- --ignored`) は merge 後オーナー実機実行で R-WBS-3 final close（blocker ではない）。 | (1) **Critical 0 件**: 本番経路 (`real.rs` / `image_paste.rs` / `auth.rs`) に schema hardcode 1 件も発見されず、12 種禁止リテラル全件 grep 検証済。(2) **Major 2 件は backlog 化条件で受諾**: M-1 (protocol.rs/contract.rs 二重定義 = `72ee23e` 由来既存負債、AS-140〜143 範囲外) / M-2 (clippy 5 件 = 同じく `72ee23e` 由来、PM § 6.5「既存 5 件 AS-CLEAN-XX backlog 許容」該当)。(3) **Minor 3 件**: S-1 default_backend panic 文言改善 / S-2 nested-Job doc 補足 / S-3 golden test 命名拡張、いずれも AS-CLEAN チケット化または follow-up にて対応。(4) **fmt clean / 63 tests PASS / 1 ignore (AS-143 实机 smoke)**、F3 Auth Watchdog 既存 test 無破壊。(5) DEC-018-033 ① `--listen stdio://` 整合 + DEC-018-034 single-source 厳守 + DEC-018-035 Real impl 本採用 (`create_sidecar_with_real_mode_returns_unstarted_real`) すべて確認。(6) AS-144 着手は M1 Critical Path 4/5 の最終 1 件、ChatPane env switch + initialize → thread/start → turn/start end-to-end 配線。Real / Mock の運用切替 UX を確定し、AS-145 E2E real シナリオへ繋ぐ。 |
| DEC-018-038 | 2026-05-03 | CEO（Review 部門「✅ 承認」受領、AS-145 着手承認） | **AS-UX-01〜06 実装 3 commits (`71e7cdf` PR-1 / `0118116` PR-2 / `8aa6ebe` PR-3) を Review 部門「✅ 承認 (Critical 0 / Major 0 / Minor 2)」を以て merge 確定 + AS-145 (E2E real シナリオ 2h) を Dev (Yuto) に正式発注承認**。条件として AS-CLEAN-05 (sidebar 単体 test の `act()` 警告解消) / AS-CLEAN-06 (cargo 並列実行で auth_watchdog 既存 test 2 件 flaky = AS-141 由来 env var 共有問題、`--test-threads=1` で PASS 確認済、AS-UX-01〜06 scope 外) を tasks.md に backlog 登録。CEO 決裁ゲート ①〜③ (PM WBS § 9) 全充足確認済、オーナー実機 smoke を本決裁直後に依頼予定。 | (1) **Critical 0 件 / Major 0 件**: DEC-018-008 Sumi コード共有禁止 grep 全件ヒット 0 / schema hardcode schemas.ts 既存集約活用で新規リテラルゼロ / PermissionMode 不実装 (tray-bar.tsx に `// TODO(M2 / AS-UX-10)` 予約コメントのみ) / lucide-react 統一で Heroicons / react-icons 混入 0 / 浅葱 accent hex 直書きゼロ / package.json diff 空で新規依存ゼロ。(2) **DoD 充足率 51/51 (100%)**: AS-UX-01 (11/11) / 02 (7/7) / 03 (7/7) / 04 (8/8) / 05 (10/10) / 06 (8/8) 全件充足。(3) **テスト全 PASS**: vitest 51/51 (tray-bar 2 + chat-activity 6 + sidebar 3 + 既存 40) / Playwright 7/7 (新 `@ux-traybar` `@ux-sidebar-tabs` + 既存 5) / tsc --noEmit clean / cargo `commands::fs::*` 2/2 PASS。(4) **設計の優秀点 (Review 特記)**: ① **R-UX-5 完全回避** = `useChatActivityStore` は Tauri event を一切購読せず、ChatPane の `useCodex.status` を `syncBoth(sessionId, projectId, state)` で橋渡しする DRY 設計 (chat-pane.tsx:73-88)、同 state 連続書込での参照同一性維持を vitest で assert 済。② **R-UX-7 完全解消** = schema hardcode を schemas.ts 既存集約で全件吸収、AS-CLEAN-02 backlog の対象範囲が縮小。③ **WAI-ARIA tab pattern 完備** = roving tabindex + Arrow/Home/End キーボードナビ + framer-motion `layoutId="sidebar-tab-indicator"` indicator 連結。(5) **Minor 2 件は backlog 化条件で受諾**: Mn-1 (sidebar 単体 test の `act()` 警告 = 既知、AS-CLEAN-05) / Mn-2 (cargo 並列で auth_watchdog 2 件 flaky = AS-141 由来 env var 共有、`--test-threads=1` で 5/5 PASS、本 3 PR scope 外、AS-CLEAN-06)。(6) **CEO 決裁ゲート ①/②/③ 全充足**: ゲート ① (PR-1 TrayBar) Sumi DEC-053 翻訳達成 + 浅葱 accent 維持 + InputArea 削除後 layout 崩れなし / ゲート ② (PR-2 Sidebar 5-tab) Sumi DEC-082 session-scoped 翻訳達成 + Files tab shallow tree が UX 上「使えない」感なし / ゲート ③ (PR-3 StatusBar + ProjectRail) AS-UX-04 R-UX-5 回避 + AS-UX-06 R-UX-2 未発火、Sumi 哲学 4 軸 (余白 / 配色 / icon / animation) 準拠。(7) **AS-145 着手承認**: M1 完成 Critical Path 残 1 件、E2E real シナリオで初期化 → thread/start → turn/start → agentMessage 抽出 → shutdown を Playwright assert、Day 3 想定で完了予定。(8) **R-UX 系列の archive 移動**: AS-UX-01〜06 全完了で R-UX-1/4/6 解消 / R-UX-5/7 完全予防 / R-UX-2/3 監視継続 (AS-145 着手時 + M2 PermissionMode 起票時)。 |
| DEC-018-037 | 2026-05-03 | CEO（オーナー明示承認「次のアクションでよいです」、案 B 採択） | **Sumi (PRJ-012 v1.42.0) 準拠 UX 再設計を「案 B 段階方式」で採択 + AS-UX-01〜10 起票確定**。①Must 6 件 (AS-UX-01〜06、合計 12h) = M1 完成までに対応必須（TrayBar 新設 / InputArea picker 削除 / StatusBar Sidecar Mode badge 移設 / StatusBar Activity summary / Sidebar 5-tab 化 第 1 弾 Sessions+Files+実行状態 / ProjectRail status indicator dot）。②Should 4 件 (AS-UX-07〜10、合計 9.5h) = M1.1 へ繰延（Files tab 強化 / Session context menu / StatusBar Branch / TrayBar Codex 接続モード picker）。③Won't = Anthropic オレンジ accent / Claude Code hooks / workspace 切替 / CLAUDE.md 命名 / PermissionMode picker（Codex approval mode 仕様確定が M2 案件）。④Designer 介入は「必要に応じて aidesigner / stitch でラフ生成」をオーナー承認 → CEO 判断で本決裁直後にフル統合モックアップ 1 案を aidesigner で生成し視覚確認ステップを挟む。⑤AS-145 (E2E real シナリオ 2h) は AS-UX-01〜06 完了後に着手（M1 完成は約 1.5 日延伸を許容、UI 構造変更後のテスト rework 回避が合理的）。⑥DEC-018-008 (Sumi コード共有禁止、設計/UX パターン参考のみ可) と完全整合。 | (1) オーナー指摘「画面構成について、左サイドバーの内容やモデルや推論の深さの選択位置、ステータスバーの内容など、sumi をしっかりと参考にして設計するように」を直接根拠とし、リサーチ部門 Sumi UX 調査 (`reports/research-sumi-ux-survey-2026-05-02.md`) + 開発部門 Asagi 現状調査 (`reports/dev-asagi-ux-current-state-2026-05-02.md`) の両報告を統合採択。(2) Sumi の本質 3 軸 = ①TrayBar 集約 (DEC-053 v1.9.0、Popover radiogroup × 3、per-query options 渡しで argv 再起動なし) ②Sidebar 5-tab + session-scoped (DEC-082 v1.42.0) ③StatusBar 最小化 (h-7、Model+Sidecars+Activity+Usage+Branch+Hotkeys) — これらを Asagi へ翻訳することで「Sumi 準拠」と称し得る完成度を確保。(3) Must 12h 内訳 = AS-UX-01 (3h) / 02 (0.5h) / 03 (1h) / 04 (1.5h) / 05 (4h) / 06 (2h)、Sumi 本質を全カバー。(4) Should 9.5h を M1.1 繰延する根拠 = M1 完成定義 (4 ステップ AC 達成) には不要、保守的見積で M1 後ろ倒しを最小化。(5) Codex 概念マッピング = Sumi `claude-opus-4-5/sonnet-4-5` ↔ Asagi `gpt-5.5-codex/o4-mini`、Sumi `thinking_budget` ↔ Asagi `reasoning_effort low/medium/high`、PermissionMode は Codex 側仕様未確定で M2 へ繰延。(6) 既存資産 (`useSidecarModeStore` AS-144 patterns / Settings drawer vaul + radiogroup / `useChatStore.modelByProject` project scope) は全て TrayBar / 5-tab 実装で再利用可能、新規依存導入は最小限 (react-arborist 等は AS-UX-07 M1.1 で評価)。(7) PM (Tomoe) へ AS-UX-01〜06 の WBS 詳細化 (依存グラフ / DoD / PR 粒度 / 推定工数再見積 / コミット粒度) を本決裁直後に正式発注、WBS 受領後 Dev (Yuto) へ実装発注へ移行。 |
| DEC-018-039 | 2026-05-03 | CEO（オーナー smoke fb 3 件受領 → CEO 即決） | **AS-UX-01〜06 オーナー smoke fb 3 件への対応として 3 ウェーブ構成 + AS-UX-FIX-A / AS-UX-FIX-B / AS-UX-11 起票確定**。①**Wave 1 (即時、合計 1.6h)**: AS-UX-FIX-A (Bug A: チャット文字重複の根本特定 + 修正、1.5h) + AS-UX-FIX-B (Bug B: Sidebar tab ラベル `実行状態` → `実行` 短縮 + en `Runtime` → `Run`、0.1h)。1 PR squash で `fix(ux): chat duplicate streaming + sidebar tab label (DEC-018-039 W1)`。②**Wave 2 (W1 完了後、合計 5.5h or 6.0h、オーナー確認後着手)**: AS-UX-11 (Inspector → Sidebar 統合、Sumi v3.5.3 proven pattern)。Inspector pane 320px を完全撤去、新規 Rules タブ追加 (4 タブ Sessions/Files/Rules/Runtime) + 既存 Runtime タブに Inspector の subAgents/todos 統合 + ChatPane を flex-1 で 320px expand。Sumi 5 タブ完全準拠 (Servers placeholder 追加で 5 タブ化) は工数 +0.5h で 6.0h、オーナー確認次第。③**Wave 3 (W2 完了後、2.0h)**: AS-145 E2E real シナリオ → M1 完成。④Wave 1 はオーナー確認待ちなしで即着手 (Bug fix 性質)、Wave 2 のスコープ (4 タブ vs 5 タブ) のみオーナー確認。⑤MemoryTreeView 等の Sumi component 構造参考時は **DEC-018-008 厳守 = コード copy 禁止、設計パターン参照のみで Asagi 用に新規実装**。⑥M1 完成タイムライン: 元 +9h 増、セッション +1〜2 想定。 | (1) オーナー smoke fb (verbatim): 「smoke 5 ポイントはおおむね良いです / 回答の文章がおかしくなっているところは今後修正してください / 左サイドバーのタブ名が２行になっているので調整して下さい / 右側サイドバーの内容はPRJ-012のsumiと同様に左サイドバーに表示するようにしてください」3 件を全て fb 採択。(2) Bug A 根本原因仮説 (Explore 並列調査): `chat-pane.tsx` L62-67 の useEffect が `codex.messages` 全体監視 + delta 受信時の再 iterate ロジックに起因疑い、または `mock_server.rs` の delta + 最終 message 二重送信、または `use-codex.ts` の delta accumulation 競合。Dev (Yuto) で再現テスト + 根本特定 + 修正、real mode でも同 logic 共有のため M1 blocker 級として最優先処理。(3) Bug B 根本原因 (Explore 確定): `w-60` (240px) ÷ 3 タブ × icon + 4 字日本語 (`実行状態`) で width 不足、案 A (ラベル短縮) が工数 2 分・副作用ゼロ・Sumi の `モニター` 4 字短縮慣行とも整合。(4) Feature C スコープ確定根拠 (Explore Sumi リファレンス調査): Sumi v3.5.3 で右 Inspector を完全撤去済、Asagi が同パターンを採用することは Sumi 信仰の自然な帰結。Inspector 320px 撤去 → ChatPane expand は wide viewport for long reasoning/token gauge という UX gain も確保。(5) **オーナー確認事項 1 点**: Asagi Sidebar を 4 タブ (Sessions/Files/Rules/Runtime、Asagi に Servers 監視機能なし) で良いか / 5 タブ (Sumi 完全準拠で Servers placeholder 追加、+0.5h、M2 で本実装) か。本決裁では暫定 4 タブで提案、オーナー回答次第で AS-UX-11 DoD を更新。(6) Wave 1 即着手承認: Bug fix は Feature C スコープに依存しないため確認待ち不要。(7) DEC-018-038 (AS-145 着手承認) は本決裁により **AS-145 を Wave 3 に繰延**、AS-UX-FIX-A/B → AS-UX-11 完了後に再開。(8) Review 部門は Wave 1 commit 直後に通常 review、Wave 2 は AS-UX-11 完了直後に通常 review、両 wave とも Critical 0 / Major 0 を merge 条件とする。 |
| DEC-018-041 | 2026-05-03 | CEO（Review 部門「✅ 承認」受領、AS-145 着手承認） | **AS-UX-11 (Inspector → Sidebar 4-tab 統合) 6 commits (`d9e6f36` AS-UX-11.1 / `cd779b8` AS-UX-11.2 / `d05207b` AS-UX-11.3 / `0b7605c` AS-UX-11.4 / `daa7fce` AS-UX-11.5 / `2d1f682` AS-UX-11.6) を Review 部門「✅ 承認 (Critical 0 / Major 0 / Minor 2)」を以て merge 確定 + Wave 3 = AS-145 (E2E real シナリオ 2h) を Dev (Yuto) に正式発注承認**。条件として AS-CLEAN-07 (rules-tab.tsx の fetch ロジック二重定義整理、0.3h、Mn-1) を tasks.md に backlog 登録、AS-CLEAN-05 (sidebar 単体 test の `act()` 警告解消、既存 backlog) に Mn-2 (NewSessionButton 由来 act 警告) を統合。CEO 決裁ゲート ①〜④ (PM WBS § 9) 全充足確認済、オーナー実機 smoke を本決裁直後に依頼。R-UX-9 (Inspector 削除残存リスク) は本 Review で grep 全件 0 件確認のため historical archive 移動。 | (1) **Critical 0 件 / Major 0 件**: DEC-018-008 Sumi コード共有禁止 grep 全件ヒット 0 (`MemoryTreeView` ゼロ、Sumi リテラルは設計参照 docstring のみ) / DEC-018-033 schema hardcode sidebar 配下ゼロ / AS-UX-04 syncBoth bridge 維持 (`chat-pane.tsx:73` 確認) / Inspector dir 物理削除 (-171 行) + i18n `inspector.*` 完全削除 / Bug B 解消が `tablist boundingBox.height < 36` 機械アサート + screenshot (`tests/screenshots/sidebar-4tab-256px.png` 13077 bytes) で**二重保証** / package.json diff 空で新規依存ゼロ。(2) **DoD 充足率 8/8 (100%)**: ① Inspector pane DOM 不在 ② ChatPane width 元 +320px expand ③ Rules タブ CLAUDE.md / AGENTS.md / CODEX.md tree 表示 ④ Sidebar 4 タブ 256px width 全件 1 行表示 (Bug B 完全解消) ⑤ vitest 55/55 + Playwright 10/10 + cargo 69/69 全 PASS ⑥ DEC-018-008 grep 0 件 ⑦ Wave 1 chat-pane 修正と矛盾なし (`chat-streaming-no-duplicate.spec.ts` PASS 維持で regression なし) ⑧ Inspector i18n key 全件削除。(3) **厳守 7 件 充足率 7/7 (100%)**: 1 Sumi copy 禁止 ✅ / 2 schema hardcode 禁止 ✅ / 3 syncBoth 設計維持 ✅ / 4 R-UX-9 grep 全件 0 ✅ / 5 絵文字なし ✅ / 6 Bug B screenshot 添付 ✅ / 7 Wave 1 regression なし ✅。(4) **設計の優秀点 (Review 特記 5 点)**: ① Bug B 物理アサート + 視覚証跡の二重保証 (CSS regression 即検知設計) ② R-UX-9 完全予防 (keybindings / help / lib 全層 grep 0 件) ③ Rules タブの DEC-018-008 厳守設計 (`files-tab.tsx` 踏襲で Asagi 独自実装) ④ WAI-ARIA tab pattern の 4 タブ拡張時の自然性 ⑤ Tauri 非接続時の fail-soft fallback 設計。(5) **Minor 2 件は backlog 化条件で受諾**: Mn-1 (rules-tab.tsx fetch 二重定義 → AS-CLEAN-07 0.3h 新規起票) / Mn-2 (vitest `act()` 警告 = NewSessionButton 由来既存負債、AS-UX-11 由来でない → 既存 AS-CLEAN-05 へ統合)。(6) **CEO 決裁ゲート ①〜④ 全充足**: ゲート ① (Inspector 物理削除) `d9e6f36` で完了 / ゲート ② (Sidebar 4 タブ 1 行 = Bug B 解消) `cd779b8` + screenshot で完了 / ゲート ③ (Rules タブ動作) `d05207b` で完了 / ゲート ④ (全完了) Review 承認で完了。(7) **AS-145 (Wave 3) 着手承認**: M1 完成 Critical Path 残 1 件、E2E real シナリオで初期化 → thread/start → turn/start → agentMessage 抽出 → shutdown を Playwright assert + オーナー実機 smoke、Day 4 想定で完了予定。本決裁により M1 完成まで残 2h。(8) **R-UX-9 解消マーキング**: Inspector 削除残存リスクは本 Review で完全予防確認、historical archive 移動。R-UX-10/11/12 (PM が AS-UX-11 起票時に追加した可能性) は monitoring 継続。 |
| DEC-018-043 | 2026-05-03 | CEO（オーナー再 smoke 部分 PASS + 残課題 2 件即決） | **DB hotfix (commit `d537b79`) でチャット復旧確認後の残課題 2 件を即時修正：①AS-CLEAN-11 (`ListSessionsArgs` の serde naming bug、frontend `projectId` ↔ backend `project_id` 不整合で session 一覧が常時空 → 「DB 未接続」誤表示)、②AS-CLEAN-12 (`input-area.tsx:192` の `[stub] Codex 統合は POC 通過後に実装` hint text obsolete、Phase 1 完了済 = DEC-018-035)。両件 1 PR squash で `chore(ux): list_sessions camelCase + remove stub hint (AS-CLEAN-11/12)` 想定。並列で PM (Tomoe) に Wave 3 = AS-145 (E2E real シナリオ 2h) WBS 起票発注。M1 完成まで残 2.5h（cleanup 0.5h + AS-145 2h）。** | (1) **オーナー再 smoke 結果 (verbatim screenshot 確認)**: ✅ チャット応答正常表示「mock app-server からの応答です（モデル: gpt-mock-5.5） 18 tokens」 / ✅ InputArea placeholder 正常「メッセージを入力（Cmd/Ctrl+Enter で送信、Enter で改行、/でコマンド）」 / ✅ TrayBar Activity badge「認証確認中 / 待機中」緑健全 / ✅ Sidebar 4 タブ 1 行表示（「実行」のみ若干右にはみ出し = AS-CLEAN-10 既知）= **DEC-018-042 hotfix の 4 連鎖障害解消が end-to-end で実証**。(2) **残課題 A (AS-CLEAN-11)**: SessionsTab の panel 上部に「セッション一覧の取得に失敗しました（DB 未接続）」灰色エラー表示。真因 = `commands/mod.rs:55-58` の `ListSessionsArgs.project_id` (snake_case) を `session-list.tsx:36` が `{ args: { projectId: activeProjectId } }` (camelCase) で送信、serde が project_id を None として解釈 → DB クエリは成功 (空 vec 返却) だが frontend は「Err 扱いで loadFailed 表示」する設計 (`session-list.tsx:43`)。**ChatPane が動作している = `agent_*` 系 command は別経路 (Tauri event ベース) で被害拡散していない**ことも確認。(3) **残課題 B (AS-CLEAN-12)**: チャット input area 下に「[stub] Codex 統合は POC 通過後に実装」灰色 hint 残存。真因 = `input-area.tsx:192` の `<p>{t('stub')}</p>` + `i18n/ja.json:167` `chat.stub` key が DEC-018-035 (Phase 1 M1 Real impl ゲート ① 通過) 後の cleanup 漏れ。Phase 1 完成済の現時点で「POC 通過後に実装」表示は虚偽になりブランド毀損。(4) **修正方針確定**: A = `ListSessionsArgs` に `#[serde(rename_all = "camelCase")]` 追加で 1 行修正、Tauri 公式推奨の camelCase invoke convention に統一。同時に他 args struct (`SessionIdArgs` / `CreateMessageArgs` 等) の serde alias 状況を確認し未対応分も同 PR で予防的修正。B = `input-area.tsx:192` の `<p>` 行削除 + `ja.json` / `en.json` の `chat.stub` key 削除 + 必要に応じて `tests/` の参照も grep 削除。(5) **CEO 直接修正方針**: 両件とも 15 分級の cleanup、Dev (Yuto) round-trip 削減 + M1 完成までの最短経路優先で CEO 直接 commit、Review 部門は Wave 3 終了時に AS-145 と同時 review (1 PR で済む簡単修正のため Critical 0 / Major 0 自明)。(6) **PM (Tomoe) AS-145 WBS 並列発注**: cleanup と AS-145 は完全独立タスク（cleanup = sidebar / input-area / args struct、AS-145 = E2E Playwright spec + smoke）のため並列実行で M1 完成最短化。WBS 受領 → Dev 実装 → Review → CEO 決裁ゲート ② → M1 完成。(7) **R-UX-10 (Sidebar 「実行」タブ overflow) は引続き AS-CLEAN-10 backlog**: オーナー「今後」明示で M1 後回し継続、本決裁 scope 外。 |
| DEC-018-042 | 2026-05-03 | CEO（オーナー実機 smoke 異常報告受領、緊急 hotfix 即決） | **M1 blocker `rusqlite is not compiled with FTS5 support` 即時修正：`src-tauri/Cargo.toml` L47 の rusqlite features を `["bundled", "backup"]` → `["bundled-full"]` に変更（AS-CLEAN-09 として正式起票、commit 単位で hotfix）+ オーナー所感「実行タブはみ出し（今後）」を AS-CLEAN-10 として backlog 起票（M1 後回し許容）+ R-UX-10 起票（Sidebar tab 物理 width 不足時のテキスト溢れ）。本決裁により Wave 3 (AS-145 E2E real シナリオ) は本 hotfix 後の owner re-smoke PASS を必須前提として再開。** | (1) **オーナー実機ログ verbatim**: `ERROR asagi_lib: failed to initialize database: rusqlite is not compiled with FTS5 support; check Cargo features: no such function: fts5_version in SELECT fts5_version();` → `WARN notification stream closed for default-asagi`。これは `db.rs:32` の `conn.execute_batch("SELECT fts5_version();")` が rusqlite `bundled` 既定の compile flag に SQLITE_ENABLE_FTS5 が含まれないため SQL 関数解決失敗。Cargo.toml L46 のコメントは「FTS5 有効」と謳いながら features 配列の実体は FTS3/FTS4/RTREE のみ = **コード意図と実体の乖離が原因の典型的な regression**。(2) **連鎖障害**: DB 初期化失敗 → `init_database()` Err return → `lib.rs:86` の app setup でエラー伝播 → sidecar spawn 配線が完成しない → notification stream 接続切断 → ChatPane が Tauri event を受信できず `useCodex` の status badge が `エラー` 表示 + InputArea placeholder が初期 `[stub] Codex 統合は POC 通過後に実装` のまま反映されない。**根本原因 1 件（FTS5 features）の修正で 4 症状が同時解消する**設計上の整合性を確認。(3) **当初案 `["bundled", "backup", "fts5"]` は rusqlite 0.32 で fts5 単独 feature 不在のため build 失敗** (`error: package 'asagi' depends on 'rusqlite' with feature 'fts5' but 'rusqlite' does not have that feature`)。再調査で `bundled-full` (= modern-full + bundled、libsqlite3-sys に SQLITE_ENABLE_FTS5 / FTS3 / RTREE / DBSTAT_VTAB 等の全 compile flag を渡す集合体 feature) が正解と確定。`cargo check` PASS (24.22s) で確認済。バイナリ増 ~300-500KB 想定（chrono / uuid / time / blob / collation / functions / hooks / series / vtab / window 等 10+ extension 内蔵、実用上無視可能 + 将来の M3 検索 UI / vtab 利用余地拡大の副次効果）。(4) **AS-CLEAN-09 起票**: hotfix commit `fix(db): enable rusqlite fts5 feature (AS-CLEAN-09, DEC-018-042)` を Dev (Yuto) に正式発注、AS-145 着手の前提条件。(5) **R-UX-10 (Sidebar tab 物理 width 不足) 起票**: オーナー画面比 ~120px の窓幅で Sidebar `w-64` (256px) が flex-1 圧縮された場合、4 タブ × 推定 80-90px 必要 = 320-360px 超過で「実行」タブが ChatPane 領域に視覚的にはみ出し。原因仮説: ① `flex-1` 配下の各 button に `min-w-0` 不在で flex-shrink 機能停止、② tablist 親に `overflow-hidden` 不在、③ ChatPane `flex-1` が Sidebar `shrink-0` を物理的に圧縮しない設計上は健全 → 真因は viewport 自体の狭さで Window 拡大で消える可能性。(6) **AS-CLEAN-10 起票 + 「今後」許容**: オーナー明示「左サイドバーの実行タブがはみ出ているので**今後**調整してください」を尊重、M1 完成 Critical Path 上の blocker ではない。Dev は AS-CLEAN-10 で root cause 確定 (viewport-dependent vs. CSS bug) → 必要な場合は `min-w-0` + `truncate` + width 調整で対応。M1.1 開始時着手目標。(7) **Wave 3 (AS-145) 再開条件**: ① Cargo.toml hotfix commit済 ② オーナー re-smoke で chat 1 ターン PASS ③ notification stream connected ログ確認 ④ status badge エラー消灯 ⑤ input placeholder 「メッセージを入力（Cmd/Ctrl+Enter で送信）」表示。5 条件満たして AS-145 着手承認。(8) **オーナー追加対処不要**: `~/.asagi/history.db` は init 失敗で空の SQLite ファイル（migrations 未実行）のため削除不要、修正後の `cargo build` 再起動で新規 schema が `CREATE IF NOT EXISTS` で正常作成される。万一 schema 不整合の場合のみ owner に削除依頼。 |
| DEC-018-044 | 2026-05-03 | CEO（Dev (Yuto) AS-145 完遂報告 + 全 5 種テスト緑化確認） | **Phase 1 M1 (Real Codex 統合) 完成宣言 + CEO 決裁ゲート ② 起票準備完了**。AS-145.1〜.4 完遂 (commits `9480626` / `f51862d` / `1196ef8` 3 件 + smoke 報告書 `dev-as145-smoke-2026-05-03.md` 納品)。M1 critical path 全タスク (AS-100〜AS-145 + AS-CLEAN-08/09/11/12 + AS-FIX-01) DoD 充足、cargo test --release 69/69 PASS / cargo clippy PASS (AS-CLEAN-01 既存 backlog 5 件除外) / cargo fmt --check PASS / vitest 13 files / 55 tests PASS / Playwright 12/16 PASS + 4 skip (`@codex-real-smoke` は OAuth + 実機 Codex 必須で CI skip 設計、AS-CLEAN-13 1 件 flaky は AS-145 由来でない既存 backlog)。**Phase 1 M1「素人が 4 ステップで ChatGPT 認証 → プロジェクトを開く → Codex と日本語チャット → 画像を貼って説明してもらえる」の 4 ステップ AC のうちステップ 3 まで実装完了**、ステップ 4 (画像) は AS-142 (image_paste 経路) で配線済み (real Codex 接続は OAuth smoke 時に実証)。**残タスク**: ① オーナー手動 real smoke 実施 (`reports/screenshots/as145-real-smoke-2026-05-03/` に 3 動画投入、~10-15 分想定) → ② AS-151 M1 自己検収 (4 ステップ AC + Win11 動作確認、4 ステップフルパス通し、~1〜2h) → ③ Gate-AS-M1 (Review 部門 設計レビュー) → **DEC-018-XXX (M2 Quick Win 着手承認決裁)**。M2 Quick Win 開始までの予測タイムライン: M1 完成 + 0.5 営業日。 | (1) **Dev 実績工数**: 見積 2.0h / 実績 ~0.5h (4 sub × 上限 0.5h 以内)、R-E2E-1 (Tauri webview 直接 driving 不可) を `dev サーバー (port 1420) E2E + 手動 smoke fallback` 三層構造 (`@codex-real-smoke` skip + 別 `regression-clean-11-12.spec.ts` で CI 常時動作 + 手動 smoke docstring) で完全解決し実装範囲を単純化。(2) **新規ファイル 5 件 (asagi-app)**: `e2e/codex-real-flow.spec.ts` ~310 行 (4 test) / `e2e/helpers/real-mode.ts` ~95 行 / `e2e/regression-clean-11-12.spec.ts` ~155 行 (AS-CLEAN-11/12 mock IPC 恒久回帰、CI 常時動作、2 test) + `reports/dev-as145-smoke-2026-05-03.md` (本 DEC 起票根拠) + `reports/screenshots/as145-real-smoke-2026-05-03/.gitkeep`。(3) **schema 文字列リテラル禁止厳守**: `import { CodexEvent } from '../src/lib/codex/schemas'` + `void CodexEvent.THREAD_STARTED;` 参照維持で将来拡張余地確保 (PM § 6.5 厳守事項 7 完全準拠)。(4) **AS-CLEAN-11/12 regression 恒久化**: mock IPC ベース (`window.__TAURI_INTERNALS__.invoke` を context-init で intercept) で `e2e/regression-clean-11-12.spec.ts` を作成、real Codex binary 不要 = CI 常時動作可能、2.7s で安定 PASS。これにより同 bug の再発を CI で永久阻止。(5) **R-E2E-1 (新規 Windows 固有リスク) は M2 持越**: real Codex binary 検証は手動 smoke + Tauri 実機ウィンドウが必須、M2 で `tauri-driver` 導入評価 (PM WBS § 5)。(6) **AS-CLEAN-13 (新規 backlog 起票)**: `chat-streaming-no-duplicate.spec.ts:190` の `data-status='ready'` 遷移 timeout flake (1 回目 fail / 単独再実行 PASS / 2 回目 fail / 3 回目 PASS) を AS-CLEAN-13 として起票 (M1.1 開始時、根本原因は mock_server `notification.method='turn/started'` 通知タイミング推定)。AS-145 由来でない既存 flake のため M1 完成 blocker ではない。(7) **CEO 決裁ゲート ② 起票材料 ready**: ① smoke 報告書 (~13.7KB、5 種テスト結果 + 手動 smoke 手順 + DEC-018-024 判断材料) ② 3 commits in asagi-app subrepo ③ regression spec で AS-CLEAN-11/12 永久保証 ④ オーナー手動 smoke の 3 動画 placeholder 設置済 — 全件揃い次第 DEC-018-XXX (M2 着手) を即起票可能。(8) **M2 Quick Win 起票準備**: 完成済 F3 Auth Watchdog stub (AS-141 同梱) + AS-CLEAN-09 (FTS5 enabling) で M2 QW1 (5min poller skeleton 本実装 1.0d) / QW2 (Outer Retry 1.5d) / QW3 (thread idle 0.5d) の前提条件全充足、M1 完成宣言 → 即時 M2 着手可能な体制。(9) **DEC-018-035 (Phase 1 ゲート ① Critical Path クリア) → DEC-018-044 (M1 完成宣言) の継承関係**: DEC-035 は AS-140 完遂時点の中間ゲート、本 DEC-044 は AS-145 完遂で M1 全 critical path 完成 = Sumi 6 週 150h と同等の Asagi 4 週 60h 想定 milestone を予定通り完遂。 |
| DEC-018-040 | 2026-05-03 | CEO（オーナー smoke 結果 + 仕様判断 1 件確定） | **Wave 1 完遂確定 (commit `9ae3402`) + Wave 2 (AS-UX-11) は「4 タブ」(Sessions/Files/Rules/Runtime) で確定 + Wave 1.5 quick fix を見送り Bug B 改行問題は Wave 2 で根本解決する方針確定 + PM (Tomoe) に AS-UX-11 4 タブ WBS 起票即発注承認**。①**Bug A**: 完全解消確認 (chat output 「mock app-server からの応答です（モデル: gpt-mock-5.5）」正常表示、文字重複ゼロ)。②**Bug B (Sidebar tab 改行)**: 短縮ラベル (`実行状態` → `実行`) 適用済だが **依然 3 タブ全件 (`セッション` `ファイル` `実行`) が改行** → 真因は Sidebar 物理 width ~120px と狭すぎることに加え tab に `whitespace-nowrap` 不適用、`実行状態` だけの問題ではなかった。③**根本解決方針**: Wave 1.5 quick fix (sidebar nowrap + width 拡張) を作るより、Wave 2 (AS-UX-11) で Inspector 320px 撤去と同時に Sidebar `w-64` (256px) 固定 + `whitespace-nowrap` + Rules タブ追加で 4 タブ全件を 1 行表示する設計を一度に整える方が PR 数最小 (1 PR = 1 Inspector 削除 + 1 Sidebar レイアウト整備) で確実。④**4 タブ確定根拠**: オーナー明示「4 タブで OK」(verbatim、2026-05-03)、Asagi に Servers 監視機能なし & 5 タブで M2 までデッドスペースを抱えるより 4 タブ実用優先が合理的、Sumi 信仰は「右 Inspector 撤去 + Sidebar tab 一元化」の本質構造で十分達成。⑤**AS-UX-11 工数 5.5h 確定** (5 タブ案 +0.5h 不採用)、Sidebar width 整備を DoD に追加 (Bug B 完全解消の証跡として screenshot 必須)。⑥PM (Tomoe) WBS 起票後、Dev (Yuto) 着手 → Review → Wave 3 (AS-145 E2E real シナリオ 2h) → M1 完成。⑦M1 完成タイムライン: Wave 2 (5.5h) + Wave 3 (2h) = 残 7.5h、+1 セッション想定。 | (1) オーナー smoke screenshot (verbatim 確認): TrayBar (Codex 5.5 + 推論 med picker) / StatusBar (gpt-5.5-codex + コンテキスト 0% + ブランチ未取得 + v0.1.0 雛形 + MOCK badge + Pro 5x: 92%) / ProjectRail status indicator dot (4 番目 N アイコン緑) / TrayBar 右 Activity summary (セッション累計 18 tokens / 認証確認中 / 待機中) すべて健全動作確認、Wave 1 の他施策に regression なし。(2) Bug A 解消の確証: chat 出力「あなた おはよう / CODEX mock app-server からの応答です（モデル: gpt-mock-5.5） 18 tokens」が exact-match で正しく表示、commit `9ae3402` の notification pump 一本化が end-to-end で機能確認。(3) Bug B 物理原因: stitch モックでは Sidebar 256px 想定だったが実装では grid 縮小モードで ~120px に圧縮、`px-2` (8px×2) + icon 14px + 4字日本語で物理的に 1 行不可能、`whitespace-nowrap` 未適用で折返し発生。(4) Wave 1.5 不採用判断: (a) PR 数最小 (1 PR で Inspector 削除 + Sidebar レイアウト = AS-UX-11)、(b) Inspector 撤去で Sidebar 余白配分が変わる → tab レイアウト最終調整は Inspector 撤去後にすべき、(c) 一回のオーナー smoke で M1 進む、(d) Wave 1.5 emergency hotfix の rebase コストを回避。(5) 4 タブ確定理由 = オーナー意思尊重 + 実用性重視 + Sumi 本質保持 (右 Inspector 撤去という Sumi v3.5.3 の最重要設計判断は完全踏襲)。(6) Sumi `モニター` 4 字も `セッション` `ファイル` も改行せず収まっている = Sumi は `text-xs` + `truncate` + 適切 padding 詰め + 適切 width で実現、Asagi も同設計を AS-UX-11 で採用。(7) PM (Tomoe) には Sidebar width 整備を DoD ⑧ (新規) に明記する WBS 起票を依頼、Inspector 関連 store/KeyboardShortcuts/i18n 残存参照は R-UX-9 trigger で grep 全件除去。(8) AS-145 (Wave 3) は AS-UX-11 完了直後に Dev 即着手、PM 経由不要 (M1 Critical Path 最終 1 件、E2E real シナリオで初期化 → thread/start → turn/start → agentMessage 抽出 → shutdown を Playwright assert)。 |
| DEC-018-045 | 2026-05-03 | CEO（オーナー M1 smoke 完了報告受領 + DEC-018-044 M1 完成宣言の延長） | **M2 Quick Win Phase 正式着手承認**（24h、3 営業日、Dev (Yuto) 1 名直列）。①QW1 (8h) F3 Auth Watchdog 本実装 = AS-200.1〜.4 / ②QW2 (12h) F1 Outer Retry Layer = AS-201.1〜.4 / ③QW3 (4h) F4 thread idle auto-shutdown = AS-202.1〜.3、計 11 サブタスク。実行順 = Day 1 QW1 → Day 2 QW3 (4h, 先行) + QW2 前半 (8h) → Day 3 QW2 後半 + 統合 smoke + 全 QW 完了報告。コミット粒度: 各 QW 内 sub 毎 commit + QW 単位 squash で 3 PR (or 全体 1 PR 大型 squash も Dev 判断で許容)。**厳守 8 件**: ①合計 24h 絶対超過しない (M2 全 60h の 1/3、超過 → PM escalation → scope 縮小判定) / ②schema 文字列リテラル禁止 (contract.rs/schemas.ts import 必須、grep 0 件確認、DEC-018-033 厳守) / ③DEC-018-008 Sumi コード共有禁止厳守 (OAuth refresh / lazy spawn 等は設計参照のみ) / ④F3 stub 接続点維持 (auth_watchdog.rs state machine / event prefix / env var 命名不変) / ⑤R-WBS-1 監視継続 (codex-schema-watch CI alert で QW2 着手停止 → contract.rs 改訂) / ⑥R-QW-1〜3 自己監視 (force_check debounce 500ms + Mutex / max_retries=3 + decorrelated jitter / atomic guard + RwLock + double-check) / ⑦smoke 動画 1 本以上を `reports/screenshots/m2-quickwin-smoke-2026-05-XX/` 保存 / ⑧mock IPC ベース 3 タグ (`@auth-watchdog-mock` / `@spawn-retry-mock` / `@lazy-spawn-mock`) は CI 常時動作可能、real CLI 必要部分は手動 smoke。**新規リスク R-QW-1/2/3 を risks.md 採用**（PM daily 監視追加）。**完了判定 (M2 QW Phase DoD) 5 項目**: 全 11 sub DoD 充足 + 全 5 種テスト PASS (cargo test / clippy / fmt / vitest / playwright) + オーナー 3 シナリオ smoke ALL PASS (A 認証 watchdog warning toast / B sidecar fail 時 retry / C 30 分 idle で auto-shutdown + lazy spawn) + smoke 報告書納品 (`reports/dev-m2-quickwin-smoke-2026-05-XX.md`) + CEO 決裁ゲート ③ 起票（DEC-018-XXX = M2 QW 完成 + F2/F5/F6/F7 着手承認）。**起票後 next action**: ①Dev (Yuto) M2 QW 正式発注 (PM WBS § 6 発注書) ②QW1 着手 → Day 1 EOD 完了 → QW3 → QW2 → 全 QW 完了 (発注 + 24h) ③オーナー real smoke + DEC-018-XXX (M2 QW 完成宣言) 起票 (ゲート ③ + 0.5 営業日)。 | (1) **PM (Tomoe) WBS 受領 (`reports/pm-m2-quickwin-wbs-2026-05-03.md` 全 359 行) 全件採択**: 11 サブタスク分解 / Day 1〜3 並列性とクリティカルパス確定 / R-QW-1〜3 起票 / Dev 発注書 ready。順序根拠 = QW1 Day 1 固定 (AS-141 で F3 stub 同梱済 DEC-018-034、context 温まり済、最短 8h で R-WBS-6 OAuth サイレント切れ予防完了) + QW3 Day 2 前半 (4h 小規模、QW2 の `spawn_for_with_retry` 実装後に QW3 lazy spawn が自動 retry policy に乗る依存関係を回避) + QW2 Day 2 後半 + Day 3 (12h 最大 task、retry policy + frontend status badge + Settings 統合まで一気通貫)。(2) **Review (Hayato) Gate-AS-M1 事前監査 (`reports/review-gate-m1-pre-2026-05-03.md` 全 513 行) 結果**: Critical 0 / Major 1 / Minor 10、通過判定 90% (A 全 PASS 60% + B 部分 PASS + hotfix 30%)、本日中 M2 着手見込み確定。Critical 0 件 = M1 完成宣言の質に正面から異議を唱える根拠なし → DEC-018-045 起票の品質基盤完全充足。Major 候補 M-1 (バイナリサイズ +~300-500KB = AS-CLEAN-09 FTS5 enabling 由来) は AS-151 自己検収時 MSI 実測で対応可能 → DEC-018-024 (codex 本体 vs codex-app-server 単体配布) 起票根拠に組込条件で受諾。Minor 10 件は AS-CLEAN-02/10/13 既存 backlog と重複、新規対応負荷 3 件のみ (M-1 + Mn-1 + Mn-3) → M1.1 開始時 batch 処理。(3) **オーナー M1 smoke 完了報告受領** (本決裁起票直前): AS-151 4 ステップ AC 通過確認、DEC-018-044 M1 完成宣言の現実適合性 end-to-end 実証、Gate-AS-M1 通過条件 (① smoke 完了 + ② Review 監査) のうち ① 充足、② は本決裁直前に Review prediction 90% で実質充足。(4) **M2 全体 60h を Pro 5x boost 期間 (~2026-05-31) 内完遂計画**: M2 QW 24h (3 営業日) を本セッション or 次セッション開始で完遂 → 残 28 日でメインフェーズ F2 (M-NLI) / F5 (Crash watchdog) / F6 (Image paste fortification) / F7 (Settings UX) 合計 36h ≒ 4.5 営業日を並列展開 → R-COST 系列リスク解消。(5) **R-QW-1〜3 新規起票根拠**: R-QW-1 (中×中、OAuth refresh 競合 force_check 連打 + CLI 内部 refresh 重複 → 429 / refresh token reuse 検知) → mitigation = debounce 500ms + Mutex in_flight guard / R-QW-2 (低×中、retry storm 6 並列 spawn 失敗時 thundering herd → 同 OAuth account への 6 並列 retry で 429) → mitigation = decorrelated jitter algo (AWS) + MaxConcurrentSidecarsReached 即 fail / R-QW-3 (中×中、lazy spawn race idle shutdown 中の send_message 並行性 → 送信 fail 体験劣化) → mitigation = atomic guard + RwLock + double-check。(6) **Dev (Yuto) 発注書転送形式**: PM WBS § 6 全文を CEO 経由で Dev に転送、Dev 着手承認は本決裁起票即時、QW1 (AS-200.1) 発注後即時着手。報告先 = 各 sub 完了 → PM (Tomoe) / 各 QW 区切り → PM 経由 CEO 軽報告 / 全 QW 完了 → PM 経由 CEO「M2 QW Phase 完成、決裁ゲート ③ 起票材料 ready」+ smoke 報告書納品。escalation = 1.5 倍超過 / R-QW-1〜3 発火 / R-WBS-1 alert / オーナー smoke fail で PM 即報告。(7) **DEC-018-044 (M1 完成宣言) → DEC-018-045 (M2 QW 着手承認) 継承関係**: DEC-044 は AS-145 完遂 + 全 5 種テスト緑化で M1 完成、DEC-045 は M1 接続性 (AS-141 F3 stub / spawn_for Result<bool> commit 9ae3402 / contract.rs `0.128.0-asagi-contract-v1` baseline) を不変前提として M2 安定性テーマへ進む。Sumi 6 週 150h と同等の Asagi 4 週 60h 想定 milestone 予定通り達成。 |
| DEC-018-047 | 2026-05-03 | CEO（オーナー M-1 smoke 異常報告 + 真因分析完了 + AS-HOTFIX-QW2/QW3/QW4 完遂検証） | **DEC-018-046 を CONDITIONAL HOLD に格下げ + AS-HOTFIX-QW2 (commit `7f656ac`) / AS-HOTFIX-QW3 (commit `78ad7fe` 内) / AS-HOTFIX-QW4 (commit `78ad7fe` 内) 完遂宣言 + 厳守事項 ⓕⓖⓗ 追加 + smoke checklist 改訂発注 + CEO 自身の smoke 1 回必須化**。①**経緯**: DEC-018-046 完成宣言後、オーナー実機 smoke で「セッション一覧の取得に失敗しました（DB 未接続）」「`tokio::spawn called from outside of a Tokio 1.x runtime` panic で idle reaper 起動失敗」「再ログイン CTA 5 連打で `[auth-badge] openLogin failed: no sidecar for project_id` 8 連 console error」の 3 系統障害が観測された。AS-HOTFIX-QW1 の merit (commit step 漏れ防止教訓) は維持しつつ、本質的に **cargo test では実機 setup() 経路が再現できないため見逃された** 3 件として CEO 自身の smoke 検証フェーズの欠落を認定。②**AS-HOTFIX-QW2 (commit `7f656ac`)**: Tauri 2 sync `setup()` callback 内で `tokio::spawn` を呼ぶと runtime が見つからず panic する問題を、`tauri::async_runtime::spawn` でラップして解消。AuthWatchdog 起動 (L141) は最初から正しいパターンだったが idle reaper 起動 (L120) が漏れていた。cargo `#[tokio::test]` は runtime 常駐のため検出不可、実機 setup() 経路を踏む smoke でしか検出できない盲点。③**AS-HOTFIX-QW3 (commit `78ad7fe` 内)**: `db.rs:32` の FTS5 sanity check が `SELECT fts5_version()` を `execute_batch` していたが、`fts5_version()` という SQL 関数は SQLite に**存在しない** (FTS5 のバージョン取得は `fts5_source_id()` を使う、sqlite3.c L252533 で定義)。AS-CLEAN-09 で `bundled-full` を正しく復活させたあとも本 sanity check が「no such function: fts5_version」で fail し続け、AppState.db = None のまま list_sessions が「DB 未接続」を表示し続けていた。修正 = `fts5_supported(&conn)` 純粋関数を抽出 (`query_row("SELECT fts5_source_id();", ...)`) + 永久回帰防止 unit test 2 本追加 (`fts5_is_available` / `run_migrations_creates_full_schema_in_memory`)。本 unit test は `cargo test --no-default-features --lib` で常時動作するため `bundled-full` が外れても CI で即時 fail する。④**AS-HOTFIX-QW4 (commit `78ad7fe` 内)**: (a) `use-auth-watchdog.ts` の `openLogin` に独立した 500ms trailing-edge debounce + shared Promise pattern を追加 (`forceCheck` と ref 共有すると相互窒息するため別 ref slot)、(b) `commands/codex.rs` の `agent_send_message_v2` で先行実装されていた lazy spawn パターンを `ensure_sidecar_with_lazy_spawn` helper に抽出 → `auth_open_login` でも呼び出すよう修正。`is_active` → false なら `lazy-spawn` event emit + `spawn_for_with_retry` で自動再接続、`newly_created` のみ notification pump task 起動 (二重起動防止)。idle reaper kill 直後の「再ログイン」CTA も自動再接続経由で成功する。⑤**検証結果**: cargo test --no-default-features --lib 92/0/1 (90 → 92、+2 は新規 db::tests) + vitest 66/66 + cargo fmt --check PASS + 既存 e2e 12/12 PASS。新規 dep 追加なし (DEC-018-045 厳守事項 ⑦ 遵守、純粋 setTimeout 採用)。⑥**厳守事項 ⓕⓖⓗ 追加 (M2.1 移行時 + 全 hotfix 発注時)**: ⓕ**実機 `pnpm tauri:dev` smoke 必須** = DB 初期化 / Tauri setup() コールバック / IPC コマンド配線 / Tauri event ベース通知 pump など Tauri 2 runtime context に依存する経路は cargo unit test では 100% 検出不可、Dev sub-agent への hotfix 発注 + CEO 完成宣言 前に CEO 自身が実機 smoke を 1 回完走することを必須化 (本回の DEC-018-042 → DEC-018-046 → 047 連鎖で 3 回見逃した教訓)。ⓖ**DB / IPC / setup() 経路は cargo unit test を完成判定根拠としてはならない** = unit test 緑化は「コード単位品質の最低限」であり「実機統合動作の保証」ではないことを発注書 / decisions.md 双方で明文化、Dev sub-agent 報告書の「全 5 種テスト PASS」記述があっても Tauri runtime context 経路が含まれない場合は CEO smoke 必須として運用。ⓗ**hotfix 発注時は保護対象を Review verdict / CEO 発注書で 3 回 cross-reference** = AS-HOTFIX-QW1 で指摘された M-1 (R-QW-1 緩和策完全欠落) パターンの再発防止、Review verdict の Critical/Major と CEO 発注書の DoD と Dev 完了報告の実装範囲を 3 点照合して欠落を検出する手続きを Dev sub-agent 発注テンプレに組込。⑦**smoke checklist 改訂発注**: `reports/smoke-m2-qw-checklist-2026-05-03.md` の検証項目を AS-HOTFIX-QW2/3/4 で実装された保護範囲と完全整合させる改訂版を作成 (DB 復旧確認 + idle reaper task 起動 log 確認 + openLogin 5 連打 → console error 0 確認 + lazy spawn → auth-relogin event flow 確認 4 シナリオ追加)。⑧**CEO 自身で smoke 1 回完走 → owner 再 hand-off**: 本決裁起票直後 CEO が `pnpm tauri:dev` で 4 シナリオ smoke を 1 回完走し、その結果 (動画 1 本以上 + console log + Tauri 起動 log) を `reports/ceo-smoke-pre-handoff-2026-05-03.md` に納品 → 全件 PASS 後にオーナー再 hand-off。CEO smoke で異常検出 → AS-HOTFIX-QW5 起票 → 修正 → 再検証ループ。⑨**DEC-018-046 完成宣言の取扱**: 完全撤回ではなく **CONDITIONAL HOLD** = 「設計優位点 5 件 + R-QW-1〜3 mitigation 構造 + AS-HOTFIX-QW1 in_flight Mutex + debounce 500ms 実装の merit はそのまま維持、ただし完成宣言は smoke 完走待ち保留」。CEO smoke + オーナー smoke が PASS した時点で DEC-018-048 として正式に完成宣言再発行。⑩**M2.1 メイン Phase F2/F5/F6/F7 着手承認は本 hotfix 系列 (QW2/3/4 + CEO smoke + オーナー smoke) が全 PASS するまで一時停止**。⑪**【追記 2026-05-03 17:55 — 厳守事項 ⓖ 生きた事例 #2 = AS-HOTFIX-QW5 (commit `bc1a6c9`)】**: 本 DEC-047 起票直後にオーナーが CEO smoke v2 checklist を実機実行したところ、Sessions タブ「DB 未接続」+「メッセージ保存に失敗しました」連鎖障害が **継続** していることを発見 (チャット側 sidecar 経路は動作)。CEO が `~/.asagi/history.db` の物理状態を確認 → 4096-byte / schema 0 / unknown encoding の半壊 DB 検出 → 真因分析: AS-HOTFIX-QW3 で `fts5_supported` を pass させた結果、初めて到達するようになった次の `execute_batch` ブロック (`PRAGMA journal_mode = WAL;`) が QW3 と **完全同型の pattern bug** で fail していた (`journal_mode` は新 mode 名 "wal" を結果として返すため `execute_batch` が "Execute returned results" で reject)。AS-CLEAN-09 (`bundled-full` 復活) → AS-HOTFIX-QW3 (`fts5_version()` → `fts5_source_id()`) → AS-HOTFIX-QW5 (`execute_batch` → `query_row` for WAL) の **3 段重ね同型バグ**。修正: ⓐ`init_database()` を `init_database_at(path: &Path)` に refactor して tempfile DB から呼べるよう純粋化、ⓑWAL PRAGMA を `query_row` で値受信 + warn fallback、ⓒsynchronous/foreign_keys は `execute` (値返さない)、ⓓ**on-disk DB 経路を直接 cover する 3 件の新規 unit test 追加** (`init_database_at_fresh_path_succeeds` / `init_database_at_stale_empty_db_recovers` (オーナー環境の 4096-byte 半壊状態を再現) / `init_database_at_double_init_is_idempotent`)、ⓔ隣接 pre-existing clippy 1.94 系 lint 5 件 (mock.rs/protocol.rs doc list overindented 4 + commands/mod.rs while_let_loop 1) も同 commit で清算。検証: cargo 95/0/1 (90 → 95、+5 = 既存 db 2 + 新規 db 3) + vitest 66/66 + clippy clean + fmt clean。**学び**: 厳守事項 ⓖ「DB / IPC / setup() 経路は cargo unit test を完成判定根拠としてはならない」が起票 30 分以内に 2 件目で実証された (#1: AS-HOTFIX-QW2/3/4、#2: AS-HOTFIX-QW5)。さらに **「AS-HOTFIX-QW3 commit (`78ad7fe`) 自体が次の同型バグを露出させた」** という連鎖性は、同じファイル内の同種 pattern (`execute_batch` × 値返す SQL) を grep で **隣接 6 行以内** に潜伏させることが可能であることを示し、回帰防止網は「症状」ではなく「pattern」で張る必要があることを示唆。AS-HOTFIX-QW3 commit 時点で同 file の `execute_batch` 全箇所を pattern grep する追加 lint (clippy custom rule または rg pre-commit hook) の検討を AS-CLEAN-16 として M2.1 着手前に評価予定。**stale DB 取扱**: QW5 適用後の `init_database_at` は半壊 4096-byte DB も安全に開けるが、オーナー再 smoke 前に `Remove-Item "$HOME\.asagi\history.db*" -Force` で完全クリーン状態から検証することを推奨 (再現性確保)。**完成宣言条件 (DEC-018-048 移行ゲート)**: AS-HOTFIX-QW5 適用後の checklist v2 0 → A → C 完走 (Sessions タブが「DB 未接続」を **表示しない** + メッセージ保存 toast が「失敗」を **表示しない**) のオーナー smoke PASS が必要、CEO 単独完成宣言は不可。 | (1) **DEC-018-046 完成宣言後の M-1 smoke で 3 系統障害顕在化** = ⓐtokio runtime panic (idle reaper 起動失敗、AS-HOTFIX-QW2 で `tauri::async_runtime::spawn` ラップ済 = commit `7f656ac`) / ⓑDB 未接続表示 (`fts5_version()` は SQLite に存在しない関数だった、AS-HOTFIX-QW3 で `fts5_source_id()` + `query_row` に修正 + 永久回帰防止 unit test 2 本追加) / ⓒ openLogin 5 連打 8 連 error (frontend debounce 欠落 + Rust auth_open_login の lazy spawn 欠落、AS-HOTFIX-QW4 で両側修正)。(2) **オーナーの「テストがうまくできていない感」直撃指摘**: cargo test 緑化 + vitest 緑化 = 完成判定根拠としてきた前提が、Tauri 2 runtime context 経路の sanity check には機能していなかった構造的盲点を露呈 → 厳守事項 ⓕⓖⓗ で運用面から強制構造化。(3) **AS-HOTFIX-QW3 の真因の真因**: `bundled-full` 復活 (AS-CLEAN-09) は正しかった、libsqlite3-sys 0.30.1 build.rs L129 で `-DSQLITE_ENABLE_FTS5` flag は無条件付与済み (libsqlite3-sys 0.30.1 source 確認済)。問題は **`fts5_version()` という SQL 関数が SQLite に元々存在しない** こと (`fts5_source_id()` だけが存在、sqlite3.c L252533 で `_fts5SourceIdFunc()` として定義)。scaffold 期 (DEC-018-005 設計時) からの本バグが 5/2 まで顕在化しなかったのは、AS-CLEAN-09 以前は `bundled` 単独で FTS5 自体が無効化されていたため query 失敗の理由が「FTS5 missing」と誤認されていたから。AS-CLEAN-09 で FTS5 が有効化されてから初めて「`fts5_version()` という関数が無い」という真因が表面化した。(4) **永久回帰防止網**: `db::tests::fts5_is_available` + `run_migrations_creates_full_schema_in_memory` は `cargo test --no-default-features --lib` で常時動作、CI で `bundled-full` 外れた瞬間に red になるため AS-CLEAN-09 / AS-HOTFIX-QW3 の両 regression を永久阻止。(5) **AS-HOTFIX-QW4 frontend debounce の設計判断**: forceCheck (AS-HOTFIX-QW1) と同じ shared Promise pattern を採用しつつ ref 分離 (`openLoginTimerRef` / `openLoginPendingPromiseRef` / `openLoginPendingResolveRef` 独立) = 共有すると forceCheck 連打が openLogin を窒息させる相互干渉を防ぐ、ヒューリスティックではなく明示的な分離設計。(6) **AS-HOTFIX-QW4 Rust ensure_sidecar_with_lazy_spawn 抽出の merit**: `agent_send_message_v2` (chat 送信) で先行実装されていた 63 行の lazy spawn パターンを helper 化、`auth_open_login` (再ログイン CTA) でも同じ保護を共有 → 今後 sidecar 必須の新 IPC コマンド追加時に同 helper 1 行呼出で「sidecar 居なければ自動再接続」保護を継承可能、コピペ重複の余地ゼロ化。(7) **既存 tech debt (out of scope)**: Rust 1.94 introduced lints (`doc_overindented_list_items` / `while_let_loop`) が `cargo clippy -- -D warnings` で red を出すが、これは scaffold 期からの mock.rs:21 / protocol.rs:23 / commands/mod.rs:244 など本 hotfix 範囲外の箇所、別 AS で一括対応する旨を commit message + decisions.md 双方に明記。(8) **DEC-018-046 → 047 継承**: DEC-046 の M2 QW Phase 設計優位点 5 件 + R-QW-1〜3 mitigation 構造は退化禁止対象として継続有効、本 DEC-047 は **完成判定の手続き面** (CEO smoke 必須化 + 厳守事項 ⓕⓖⓗ 追加) を補強する位置付け。M2.1 着手承認は smoke 完走後の DEC-018-048 で再発行。 |
| DEC-018-046 | 2026-05-03 | CEO（Dev (Yuto) M2 QW Phase 完遂報告 + Review (Hayato) Conditional Approval verdict + AS-HOTFIX-QW1 完遂検証） | **【DEC-018-047 で CONDITIONAL HOLD に格下げ — 完成宣言は smoke 完走待ち保留】** **M2 Quick Win Phase 完成宣言 (commit `e0b64c1` + hotfix `a54f1b1`) + AS-CLEAN-14/15 正式起票 + M2.1 メイン Phase F2/F5/F6/F7 着手承認**。①**実装サマリー**: Dev (Yuto) sub-agent が DEC-018-045 で承認した 11 sub-tasks (QW1 F3 Auth Watchdog 8h / QW2 F1 Outer Retry Layer 12h / QW3 F4 thread idle auto-shutdown 4h, 計 24h hard-limit) を wall-clock 約 42 分で完遂、CEO 大型 squash commit `e0b64c1` (22 files / +2214 / -57) で main HEAD 更新。新規ファイル 5: `retry.rs` 215行 (pure splitmix64、`rand` crate 不要、AWS decorrelated jitter、5 unit tests = range/cap convergence/jitter stdev>5.0/contract const 一致/concurrent safe) + `use-spawn-retry.ts` + `use-lazy-spawn.ts` + 各 vitest 4 ケース。contract.rs 拡張: `EXPIRY_WARNING_THRESHOLD_SECS=30min` / `SPAWN_RETRY_BASE_MS=200`/`SPAWN_RETRY_CAP_MS=10_000`/**`SPAWN_RETRY_MAX=3` golden test (AWS pseudo-rust max=5 汚染防止)** / `SIDECAR_IDLE_THRESHOLD_SECS=30min`/`SIDECAR_IDLE_REAPER_INTERVAL_SECS=60`/各 event suffix。②**設計優位点 5 件 (Review 認定 / 退化禁止)**: ⓐretry.rs decorrelated jitter (rand crate 不要 splitmix64 + golden test) / ⓑcontract.rs Golden Test pattern (CI で contract drift 即検出、DEC-018-033 構造的強制) / ⓒAuthState `Authenticated.expiry_warning` additive sub-state (4-state machine 破壊なし、後方互換模範) / ⓓmulti.rs RwLock 二重 check + 明示 release (spawn race 防止教科書通り) / ⓔWeak<Self> reaper task pattern (manager Drop 妨げず + JoinHandle abort on Drop でリーク防止、長期稼働 desktop app 必須)。③**Review (Hayato) verdict**: 条件付き承認 (Conditional Approval、Critical 0 / Major 2 / Minor 3、報告書 `reports/review-m2-quickwin-2026-05-03.md` 271行)。M-1 (R-QW-1 緩和策完全欠落: debounce 500ms TS + tokio Mutex<()> in_flight Rust) → **AS-HOTFIX-QW1 として Dev (Yuto) 即派遣 → commit `a54f1b1` で完遂 → CEO 検証で cargo 90/0/1 (89 → 90、+1 = `test_force_check_concurrent_call_returns_busy`) + vitest 66/66 (65 → 66、+1 = `force-check is debounced`) + tsc clean + 新規 dep 追加なし (純粋 setTimeout 採用) を確認、push 済 (origin/main)**。M-2 (AS-201.4 Settings drawer retry policy carryover が事前承認なし) → **CEO 判断 (b) AS-CLEAN-15 として正式 carryover 起票** (M2.1 F5 Settings drawer 拡充と統合実装が効率的、24h hard-limit 遵守、in-chat badge で daily-driver UX 充足)。Mn-1 (`tauri-plugin-shell::open` deprecated) → AS-CLEAN-14 として M3 着手時に `tauri-plugin-opener` 移行予定。④**R-QW-1/2/3 mitigation 結果**: R-QW-1 = `WatchdogEmitter` trait DI + in-process 重複検知 + **hotfix で Mutex<()> in_flight + debounce 500ms 追加** (連打耐性確保、IDP 429 / account lock 回避)、R-QW-2 = `SPAWN_RETRY_MAX=3` golden test PASS + `MaxConcurrentSidecarsReached` は即 fail (retry 対象外)、R-QW-3 = timer mock race condition test 4 ケース PASS。⑤**M2.1 メイン Phase 着手承認**: F2 (codex `/resume thread <id>` で `last_thread_id` 復元) + F5 (Settings drawer 拡充: logger/model selector/MCP toggle + **AS-CLEAN-15 retry policy 表示同梱**) + F6 (lucide icon 全統一 + Geist Sans/Mono next/font 完全適用 + 残存絵文字 grep 撲滅) + F7 (multi-window) を次フェーズとして PM (Tomoe) WBS 起票発注 (オーナー手動 smoke 結果次第で正式キック)。⑥**厳守事項 (M2.1 移行時)**: ⓐDEC-018-033 schema 単一情報源継続厳守 (生リテラル禁止、`contract.rs`/`schemas.ts` 経由必須)、ⓑDEC-018-045 で確定した R-QW-1〜3 mitigation 構造 (`WatchdogEmitter` trait DI / `SPAWN_RETRY_MAX` golden test / `RwLock` + double-check / Mutex<()> in_flight + debounce 500ms) 退化禁止、ⓒAS-CLEAN-15 (AS-201.4 Settings UI) は F5 内で必ず同梱、ⓓDEC-018-008 (Sumi コード共有禁止) 継続厳守、ⓔ**commit step 漏れ防止 — Dev sub-agent への発注時に「実装完了 → commit → push まで一貫実施」を明示** (e0b64c1 で発覚した教訓、a54f1b1 hotfix では遵守確認済)。⑦**次 action**: ⓐdashboard 更新、ⓑオーナー手動 smoke 依頼 (Codex CLI 0.128.0 実機で expiry warning toast / spawn retry badge / 30min idle shutdown / Smoke C 「今すぐ確認」連打 500ms 集約 を確認)、ⓒsmoke PASS 確認後 PM (Tomoe) へ M2.1 (F2/F5/F6/F7) 設計発注。 | (1) **Dev (Yuto) sub-agent 完遂報告 (commit `e0b64c1` 22 files / +2214 / -57) を CEO 自己検証で実態一致確認**: cargo test --lib --release 89/0/1 + vitest 65/65 + schema 生リテラル grep clean (contract.rs のみ) + cargo fmt --check PASS。(2) **Review (Hayato) sub-agent (`a5ce396245558345c`) Conditional Approval verdict** (271 行報告書、Critical 0 / Major 2 / Minor 3 / 設計優位点 5)、CEO ゲート ③ ブロッキング条件 = M-1 hotfix 1 commit。(3) **AS-HOTFIX-QW1 (Dev (Yuto) 派遣 0.5h / 1 commit / 30 分制約) 完遂**: commit `a54f1b1` で `auth_watchdog.rs` に `in_flight: Arc<tokio::sync::Mutex<()>>` + try_lock 早期 return + state-changed 再 emit なし、`use-auth-watchdog.ts` に shared Promise + unmount/projectId 切替 cleanup の 500ms trailing-edge debounce、新規テスト 2 本 (Rust `test_force_check_concurrent_call_returns_busy` + TS `force-check is debounced`) 追加、新規 dep 追加なし (DEC-018-045 厳守事項 ⑦ 遵守、純粋 setTimeout 採用)、push 済 (origin/main)。CEO 自己検証で cargo 90/0/1 + vitest 66/66 PASS 実態確認。(4) **M-2 (AS-201.4 Settings UI carryover)**: Review 指摘の通り Dev の事前承認なし carryover は手続き不備、しかし carryover 自体の合理性は CEO として承認 (M2.1 F5 Settings drawer 拡充と統合実装が効率的、24h hard-limit 遵守、in-chat badge で daily-driver UX 既に充足、Settings 詳細設定群 logger/model selector/MCP toggle と一括実装が PR 数最小化) → **(b) AS-CLEAN-15 正式起票**で手続き正常化 + F5 内同梱で M2.1 実装計画に組込確定。(5) **commit step 漏れ防止教訓**: Dev (Yuto) sub-agent は M2 QW Phase 実装 22 ファイルを完遂したが commit step を省略、CEO が大型 squash commit `e0b64c1` で代理実施。AS-HOTFIX-QW1 の発注時に「最後に commit + push まで実施」を明示した結果 commit `a54f1b1` で正常完遂 → 今後の Dev sub-agent 発注テンプレに「commit + push 必達」明記を厳守事項 ⑤ⓔ として組込確定。(6) **M2 全体 60h 計画整合性**: M2 QW 24h (実態 wall-clock 42 分、Dev sub-agent + CEO 統合作業含む) で R-QW-1〜3 mitigation 全実装完了、残 36h で F2/F5/F6/F7 着手 → Pro 5x boost 期間 (~2026-05-31) 内完遂見込み堅持、Sumi 6 週 150h と同等の Asagi 4 週 60h milestone 予定通り達成見通し。(7) **設計優位点 5 件は Review (Hayato) が独立で認定したもの**であり M2.1 以降の退化禁止対象として明文化 → 厳守事項 ⓑ に組込。retry.rs の `SPAWN_RETRY_MAX=3` golden test (AWS pseudo-rust max=5 汚染防止) は研究根拠と実装の乖離を CI で防ぐ pattern として今後の F-feature 設計のリファレンスとなる。 |

---

## 起案前提（Sumi 知見の翻訳マッピング）

| Sumi の差別化軸 | Asagi での再現方針 |
|---|---|
| A. 日本語 UI ファースト | next-intl で全 UI 日本語デフォルト、英語切替可。Codex CLI の英語出力を整形して日本語コンテキストに馴染ませる |
| B. Linear / Arc / Raycast 級デザイン | shadcn/ui + Tailwind + framer-motion。**primary accent は浅葱色 oklch(0.72 0.10 200) 相当（青緑）**。Sumi の Claude オレンジに対する識別色 |
| C. Slack 風 Multi-Project + Multi-Sidecar | ProjectRail（Discord 風 48px 縦アイコン列）+ Codex CLI プロセスを project ごとに独立起動。Tauri event prefix `agent:{projectId}:*` |
| D. ローカル永続化 + FTS5 | rusqlite bundled + FTS5、`~/.asagi/history.db`、外部送信なし |

## 確定技術スタック（暫定、リサーチ後に DEC-018-009 で正式化）

| レイヤ | 採用 |
|---|---|
| デスクトップシェル | Tauri 2.x（Rust + WebView2/WKWebView/WebKitGTK） |
| フロント | Next.js 15 App Router + React 19 + TypeScript |
| UI | shadcn/ui + Tailwind 3.4+ |
| エディタ | @monaco-editor/react DiffEditor |
| ターミナル | xterm.js 5.5+ |
| AI 統合 | **Codex CLI sidecar**（ChatGPT サブスク認証、Tauri shell plugin で spawn） |
| ローカル DB | rusqlite bundled + FTS5 |
| 認証保管 | keyring 2（Windows Credential Manager / macOS Keychain / Linux Secret Service） |

---

**起案日**: 2026-05-01 ／ **次回 DEC**: リサーチ完了後の DEC-018-009（Codex CLI 統合仕様確定）
