# 稼働案件一覧

最終更新: 2026-05-03（**【PRJ-016 HANEI Phase 2 W12 第 3 atomic = W12-T2.5 月次 streak freeze grant cron への variant 別 grant 数適用（A/B test 実走化）完遂 = DEC-067】**。W12-T2 (DEC-066 e6b8aab) push 直後にオーナー「CEO 推奨通り順次進めてください」継続マンデート受領 → CEO は「測れる」(KPI Dashboard / W12-T1) → 「比べられる」(A/B test 基盤 / W12-T2) → 「**動かせる**」(grant cron variant 適用 / W12-T2.5) への論理連続を最重視、DEC-067 atomic スコープを「W12-T2 で catalog 登録のみだった `streak_freeze_monthly_grant` experiment の zombie 状態を解消し A/B test を実走化」に振り切り、開発部門に着手指示。dev は 1 セッションで 5 ファイル atomic 完遂（新規 2 + 修正 3 / +269 行）→ レビュー部門独立判定 **APPROVE / Critical 0 / Major 0 / Minor 1 / Nit 2**（M-1 cron monthly loop per-row fail-soft 未導入 = W8-T1 継承 + scope 外明示済 / N-1 `?? 1` 多層防御 dead branch / N-2 月初以外 `monthlyGrantedByVariant: {}` 観測性文書化推奨 = いずれも push 阻害 0 / 後続 polish atomic で吸収可）。**(1) `src/lib/study/streak-freeze.ts` 拡張**: 純関数 `grantFreezeTicketsN(current, n)` 追加（既存 `grantFreezeTicket` 完全不変 / W8 既存 call sites = 受験 30 日前ボーナス / 救済 / `applyLearnDayUpdate` の regression 0 を構造担保）/ 内部で `grantFreezeTicket` を最大 n 回呼ぶ実装 / 早期 break = 上限到達なら停止（DEC-024 既存設計原則尊重）/ `n <= 0` / 非整数 / NaN / 負値 → 0 扱い + `Math.floor(n)` で整数化（防御網羅）。**(2) `src/lib/experiments/streak-freeze-variants.ts` 新設（純関数 / 56 行）**: `STREAK_FREEZE_GRANT_TICKETS_BY_VARIANT: Record<string, number>` = `{ control: 1, variant_a: 2 }` を export / `resolveStreakFreezeGrantTickets(variantKey)` を export（catalog にない variant key は **fallback = 1 枚**（control 互換 / 安全側））/ DOM-free / DB-free / `"use server"` 不在 / experiments-catalog.ts (W12-T2) を **import せず** 独立した「variant → 効果」マッピングとして分離（責務分離 / `Object.prototype.hasOwnProperty.call` で prototype-pollution 防御）= **Turbopack `"use server"` sync export ban 8 度目再適用**（W10-T5 → W11-T1 → W11-T3 → W11-T2 → W11-T5 → W12-T1 → W12-T2 → W12-T2.5）。**(3) `src/app/api/cron/streak-freeze-monthly/route.ts` の monthly grant ループ修正**: 既存 `for (const row of allStreaks)` ループ内で `getOrAssignVariant(row.learnerId, EXPERIMENTS.streak_freeze_monthly_grant.key)` を呼び variant key 取得 → `resolveStreakFreezeGrantTickets(variant)` で n 枚取得 → `grantFreezeTicketsN(row.freezeTickets, n)` で newCount + grantedCount 計算 → `grantedCount > 0` なら UPDATE / `monthlyGranted += grantedCount`（実枚数集計）+ `monthlyGrantedLearners` 別フィールド併記（後方互換）+ `monthlyGrantedByVariant: { control: number; variant_a: number }` 追加（観測性確保）/ cron 認可 (`x-vercel-cron-signature` or `Bearer ${CRON_SECRET}`) 不変 / 受験 30 日前ボーナス（既存 line 74-113）も不変（experiment 対象外維持）。**(4) Unit テスト拡張・新規 19 ケース（+19 vitest）**: `tests/unit/study.streak-freeze.test.ts` に `grantFreezeTicketsN` 10 ケース追加（`(0,1)→{1,1}` / `(0,2)→{2,2}` / `(1,2)→{2,1}` 上限到達 / `(2,2)→{2,0}` 既に上限 / `(0,0)→{0,0}` / `(0,-5)→{0,0}` / `(0,NaN)→{0,0}` / `(0,1.7)→{1,1}` 切り捨て / 既存 `grantFreezeTicket` 単独 test 全件 PASS 維持）/ `tests/unit/experiments.streak-freeze-variants.test.ts` 新規 9 ケース（`STREAK_FREEZE_GRANT_TICKETS_BY_VARIANT` 完全列挙 / `resolveStreakFreezeGrantTickets("control")→1` / `("variant_a")→2` / `("unknown")→1` fallback / `("")→1` / **catalog↔variants map 整合性 guard** = `EXPERIMENTS.streak_freeze_monthly_grant.variants` の key 集合と `STREAK_FREEZE_GRANT_TICKETS_BY_VARIANT` の key 集合一致を構造的検証 = catalog 拡張時に同期忘れを CI で検出）。**CEO Trust-but-Verify 全項目クリア**: typecheck ✓ / lint ✓（warning 0）/ vitest ✓ **53 files / 801 PASS**（baseline 782 + 新規 19 / regression 0）/ next build ✓ **25 routes**（不変 / 新規 route 0 / 既存 cron route の signature 不変）/ E2E ✓ **shop 6 PASS** + **admin-kpi-experiment 2 PASS**（W12-T2 regression 0）/ **DEC-055 idempotency triple guarantee** 達成（(a) `getOrAssignVariant` 既存 valid 割当時 DB write 0（W12-T2 由来）+ (b) `grantFreezeTicketsN` 上限到達 no-op + (c) `isFirstDayOfMonthJst` で月 1 回限定 = 同日 cron 二重起動でも streaks 行は変化しない）/ DEC-024 / DEC-003 / DEC-006 / DEC-066 / DEC-067 全準拠。**push 実行**: `e6b8aab..3b97d88  main -> main` 完遂（HANEI repo `https://github.com/hironori-oi/HANEI.git`）/ 8 files changed / +571 / -3 / commit message: `feat(W12-T2.5): 月次 streak freeze grant cron への variant 別 grant 数適用 (A/B test 実走化) (DEC-067)`。**Phase 2 W12 進捗**: T1 ✓ (b9cbd91) → T2 ✓ (e6b8aab) → T2.5 ✓ (3b97d88) → **W12 進捗 40%（2/5）→ 60%（3/5）** / 残 T1.5 (KPI 追加 / 0.25 人日) + T3 β 受入準備 (1.5 人日 / P0) + T4 ストレステスト (0.5 人日) / **Phase 2 全体進捗 97% → 97.5%**（T2.5 は T2 catalog 実走化 = 大幅な機能追加というより T2 完成度向上の位置付け）。**重要学習**: ① **DEC-055 idempotency triple guarantee の 7 度目構造定着** = `getOrAssignVariant` (W12-T2 既存割当上書き 0) + `grantFreezeTicketsN` (上限到達 no-op) + `isFirstDayOfMonthJst` (月 1 回限定) の三段防御で「同日 cron 二重起動 / 月内追加起動でも streaks 行は変化しない」を構造的に保証 = 単発 idempotency (markMessageRead / awardCoins / spendCoins / claimQuest / getOrAssignVariant) の組み合わせから triple guarantee 設計まで進化、② **Turbopack `"use server"` sync export ban パターンの 8 度目定着** = `streak-freeze-variants.ts` (純関数 / 56 行 / catalog 不依存 / 独立マッピング) を新規導入し 8 度目適用 = 後続 atomic で考慮ゼロで適用可能（8 ファイル群が同一構造）/ 「効果 (variant→数値)」と「構造 (variant 候補 catalog)」の責務分離による拡張容易性、③ **W8 既存関数完全不変原則の構造的担保** = `grantFreezeTicketsN` を新規ヘルパとして追加し既存 `grantFreezeTicket` を一切改変せずに W8 既存 call sites（受験 30 日前ボーナス / 救済 / `applyLearnDayUpdate`）の regression 0 を構造的に確保 = 「既存 API は壊さず能力を拡張する」設計規範完全定着、④ **DEC-024 罰則ゼロ哲学と構造的上限の連携** = `FREEZE_MAX_TICKETS=2` 上限が `grantFreezeTicketsN` 内のループで構造的に守られる（`grantFreezeTicket` の既存返却 `granted=false` を early break として使用）= variant_a でも上限 2 で「貯まりすぎ → 学習離脱」抑止維持 = 罰則ゼロ哲学が「単発の罰演出ゼロ」から「構造的な上限保護」まで包括的に体現、⑤ **DEC-006 API surface 不変原則の後方互換戦略** = `monthlyGranted` 意味変更（学習者数 → 実枚数）を併記フィールド (`monthlyGrantedLearners` + `monthlyGrantedByVariant`) で補償 = 既存呼び出し元の挙動を一切壊さず観測性のみ強化 = 「破壊的変更の代わりに併記で意味の改善を漸進」設計規範化、⑥ **catalog↔variants map 整合性 guard の CI 検出パターン** = unit test で catalog の variants[].key 集合と `STREAK_FREEZE_GRANT_TICKETS_BY_VARIANT` の key 集合一致を構造的検証 = catalog 拡張時に variants map 同期忘れを CI で検出 = 「2 つのデータ構造の整合性を unit test で永続化」設計規範化、⑦ **A/B test 実走化の zombie 状態解消** = W12-T2 で catalog 登録のみ完了、grant 動作未連動 = 「割当だけ走り続けて結果が出ない」状態を本 atomic で解消 = 次回月初発火から A/B 動向計測開始可能 / 「測れる」(W12-T1) → 「比べられる」(W12-T2) → 「動かせる」(W12-T2.5) の 3 段論理連続が完成、⑧ **次 atomic 候補** = W12-T1.5 (KPI 追加 + DEC-066 Minor 2 / Nit 1 + DEC-067 M-1 / N-1 / N-2 polish 吸収 / 0.25-0.5 人日) or W12-T3 (β 受入準備 / 1.5 人日 / P0) or W11 KPT 振り返り (0.25 人日)。**累計決裁 66 → 67**）。

最終更新（旧）: 2026-05-03（**【PRJ-016 HANEI Phase 2 W12 第 2 atomic = W12-T2 A/B test 基盤（feature flag + cohort 割当永続 + KPI 観測）完遂 = DEC-066】**。W12-T1 (DEC-065 b9cbd91) push 直後にオーナー「提案通り A 案 W12-T2 A/B test 基盤を進めて下さい」継続マンデート受領 → CEO は「測れる」(KPI Dashboard) → 「比べられる」(A/B test 基盤) への論理連続を最重視、DEC-066 atomic スコープを「割当ロジック + 永続化 + cohort KPI 観測のみ」に振り切り（grant cron への variant 適用は W12-T2.5 後続 atomic に分割 = scope creep 抑止）、開発部門に着手指示。dev は 1 セッションで 12 ファイル atomic 完遂（新規 5 + 修正 7 / +1624 行）→ レビュー部門独立判定 **APPROVE / Critical 0 / Major 0 / Minor 2 / Nit 1**（M-1 罰語 grep 語数揃え / M-2 CRLF snapshot noise / N-1 コメント文言 = いずれも push 阻害 0 / W12-T2.5 で吸収可）。**(1) migration 0016 `learner_profiles.experiments` JSON 列追加**: `ALTER TABLE ADD COLUMN experiments TEXT NOT NULL DEFAULT '{}'` で既存行影響ゼロ / shape `{ [experimentKey]: variantKey }` / `validateExperimentsJson` で型ゆらぎ防御。**(2) `src/lib/experiments/experiments-catalog.ts` 新設（純関数 / 201 行）**: `EXPERIMENTS` catalog 初回登録 = `streak_freeze_monthly_grant` (control 50 / variant_a 50) / `assignVariant(seed, variants)` = **FNV-1a 32-bit hash deterministic 50/50**（依存無し / Edge runtime 互換 / `Math.imul` で overflow 安全 / 10000 サンプル ±0.5% 達成）/ `validateExperimentsJson` / `isKnownExperiment` / `getExperimentDef` / DOM-free + DB-free + `"use server"` 不在 = **Turbopack `"use server"` sync export ban 7 度目再適用**（W10-T5 → W11-T1 → W11-T3 → W11-T2 → W11-T5 → W12-T1 → W12-T2）。**(3) `src/lib/experiments/assignment.ts` 新設（server-only / 225 行）**: `getOrAssignVariant(learnerId, experimentKey)` = **DEC-055 idempotent UPSERT**（既存 valid 割当時 DB write 0 / 未割当時のみ単一 UPDATE / 同一引数複数 call で variant flip しない）/ `getCohortDistribution` + `getCohortStreakAvg` = **SQL aggregate-only**（`json_extract + COUNT/AVG + GROUP BY variant` のみ / SELECT 句から learner_id / family_id / user_id を構造的排除 = DEC-003 §第三層 / COPPA 構造担保）/ `experimentKey` は `sql.raw` 不使用 / template literal bind parameter で SQL injection 防御。**(4) `src/lib/admin/kpi.ts` 拡張**: `Promise.all` 8 → 9 並列に拡張 + `getExperimentCohortRaw()` 追加（W12-T1 の `safeAggregate<T>` per-task try/catch fail-soft パターン 5 度目再適用 / 1 KPI 失敗で他 9 カードに影響なし）。**(5) `src/lib/admin/kpi-summary.ts` 拡張**: `ExperimentCohortRaw` 型 + `buildExperimentCohortCard` pure compose + `composeKpiDashboardView` で 9 → 10 カードへ拡張（**fixed-order の末尾 append** で既存 9 カード順序不変 / regression risk 0）/ `iconName: "BeakerIcon"` / 罰語ゼロ catalog（catalog + UI 両層で構造的検証）。**(6) `/admin/kpi/page.tsx` 修正**: Heroicons `BeakerIcon` 追加 / 既存 9 カードのループに 10 カード目として表示 / `data-kpi-id="experiment-streak-freeze-cohort"`。**(7) Unit 33 ケース新規（+30 vitest）**: `assignVariant` 同一 seed → 同一 variant + 異なる seed × 10000 サンプル 45-55% 分布 + 空 variants throw + weight 合計 0 throw + 単一 100% / `validateExperimentsJson` null/undefined/string/array/不正 type 全網羅 / `isKnownExperiment` `getExperimentDef` 既知+未知 / `buildExperimentCohortCard` raw undefined / 0 cohort / 1 cohort / 2 cohort / 罰語不在 grep / fixed-order 確認。**(8) E2E `admin-kpi-experiment.spec.ts` 新規**: chromium + mobile-chrome 各 1 ケース = 2 ケース / admin signup → promoteToAdmin → clearCookies + 再ログイン → `/admin/kpi` → 10 カード可視 + `data-kpi-id="experiment-streak-freeze-cohort"` 存在 + 罰語不在 grep / `serial mode` + `execWithRetry` SQLITE_BUSY 対策（W12-T1 と同パターン）。**CEO Trust-but-Verify 全項目クリア**: typecheck ✓ / lint ✓（warning 0）/ vitest ✓ **52 files / 782 PASS**（baseline 752 + 新規 30 / regression 0）/ next build ✓ **25 routes**（不変 / `/api/cron/streak-freeze-monthly` は W8 既存で dev 改変なし = scope creep ゼロ）/ E2E ✓ **全 94 PASS**（chromium 47 + mobile-chrome 47 = admin-kpi-experiment 2 新規 + 既存 admin-kpi 4 + family-* / study-* / quest / shop / session 全 spec green / regression 0）/ DEC-024 / DEC-003 / DEC-006 / DEC-055 / DEC-066 全準拠。**push 実行**: `b9cbd91..e6b8aab  main -> main` 完遂（HANEI repo `https://github.com/hironori-oi/HANEI.git`）/ 15 files changed / +1624 / -3 / commit message: `feat(W12-T2): A/B test 基盤 (feature flag + cohort 割当永続 + KPI 観測) (DEC-066)`。**Phase 2 W12 進捗**: T1 ✓ (b9cbd91) → T2 ✓ (e6b8aab) → **W12 進捗 20% → 40%（2/5）** / 残 T1.5 (KPI 追加 / 0.25 人日) + T2.5 (variant 別 grant 数適用 / 0.5 人日) + T3 β 受入準備 (1.5 人日) + T4 ストレステスト (0.5 人日) / **Phase 2 全体進捗 96% → 97%**。**重要学習**: ① **DEC-055 idempotency 構造的担保パターンの 6 度目定着** = `getOrAssignVariant` の「既存 valid 割当時 DB write 0 / 未割当時のみ単一 UPDATE」設計が `markMessageRead` (W11-T2 W9-D) / `awardCoins` `spendCoins` (W10-T1) / `claimQuest` (W10-T3) / `getOrAssignVariant` (W12-T2) と同パターン = idempotency-by-construction が組織パターンとして完全規範化、② **Turbopack `"use server"` sync export ban パターンの 7 度目定着** = catalog (純関数) と server-only helper を別ファイル分離する規範が完全に組織化 / 後続 atomic で考慮ゼロで適用可能（7 ファイル群が同一構造）、③ **Promise.all + per-task try/catch fail-soft パターンの 5 度目定着** = 「一部失敗で全体は落とさない」設計が family-leaderboard / family-weekly-digest / admin/kpi / experiment cohort で 5 連続適用、4 → 5 度目で完全規範化、④ **集約 SQL only 設計による COPPA 構造担保 + json_extract 拡張** = W12-T1 の「learner_id / family_id / user_id を SELECT 句から構造的排除」を experiment cohort 集計でも維持（json_extract + GROUP BY variant のみ）+ SQL injection 防御は template literal bind parameter で構造化、⑤ **FNV-1a 32-bit hash deterministic 50/50 の Edge runtime 互換実装** = `crypto.subtle` 不使用 / `Math.imul` で 32-bit overflow 安全 / 依存ゼロ = Edge runtime 完全互換 / 10000 サンプル ±0.5% 精度（A/B test 統計検出力に十分）、⑥ **scope creep 抑止の構造的判断** = DEC-066 §含まない で「月次 streak freeze grant cron への variant 別 grant 数適用」を W12-T2.5 後続 atomic に明示分割 / レビュー部門が dev 越権の有無を `git status` ベースで構造検証 → scope creep 0 確認 / 「最小 atomic + 後続分割」が CEO ↔ dev ↔ review 三者の合意点として完全規範化、⑦ **W12-T1 → W12-T2 連続性** = E2E 42 PASS（W12-T1）→ E2E 94 PASS（W12-T2 / 全 spec を 1 push サイクルで検証完了）/ vitest 752 → 782 / 「測れる」(KPI Dashboard) → 「比べられる」(A/B test 基盤) の論理連続が次マイルストーン前進の核心 = 次 atomic 候補 = W12-T2.5 (grant cron variant 適用 / 0.5 人日) or W12-T1.5 (KPI 追加 / 0.25 人日) or W12-T3 (β 受入準備 / 1.5 人日 / P0) or W11 KPT 振り返り (0.25 人日)。**累計決裁 65 → 66**）。

最終更新（旧）: 2026-05-03（**【PRJ-018 Asagi AS-UX-11 サイドバー 4 タブ + 256px 幅 + Rules タブ全 6 commit 完遂 = DEC-018-040】**。CEO 明示承認のもと AS-UX-11.6 (test/screenshot atomic) を最終 commit `2d1f682` で push 直前まで投入完遂。**全 6 commit シーケンス**（base `9ae3402` → HEAD）: ① `d9e6f36` feat(ux): remove Inspector pane (AS-UX-11.1) ② `cd779b8` feat(ux): sidebar 4-tab + 256px width nowrap (AS-UX-11.2) ③ `d05207b` feat(ux): rules tab impl (AS-UX-11.3) ④ `0b7605c` feat(ux): runtime tab integrate Inspector legacy (AS-UX-11.4) ⑤ `daa7fce` chore(ux): remove Inspector residual references (AS-UX-11.5) ⑥ `2d1f682` test(ux): sidebar 4-tab + rules + 256px width assertion (AS-UX-11.6)。**AS-UX-11.6 atomic 内訳**: e2e/ux-sidebar-tabs.spec.ts を 4 タブ全件 1 行表示 assert に拡張 (Bug B 機械保証) / e2e/ux-sidebar-rules.spec.ts 新規 (Rules タブ表示 + CLAUDE.md 検出 assert) / vitest sidebar.test.tsx を 3 → 4 tab assertion に更新 / vitest rules-tab.test.tsx 新規 / tests/screenshots/sidebar-4tab-256px.png 添付 (Bug B 解消証跡) / 5 files changed +389 / -25 / sensitive file 混入無し（CEO 事前 ls -la 検証）。**安全運用準拠**: `git add -A`/`.` 不使用 = ファイル明示 add のみ / push 未実施 (main 直 commit / remote 反映はオーナー判断) / amend 不使用 (新規 commit のみ) / `git status` clean / pre-commit hook 通過。**次フェーズ**: Review 部門が AS-UX-11 全体 (6 commit `d9e6f36..HEAD`) を独立判定する段階に移行、CEO は Review APPROVE 後にオーナーへ push 可否を確認。**累計決裁 65 → 66**）。

最終更新（旧）: 2026-05-03（**【PRJ-016 HANEI Phase 2 W12 第 1 atomic = W12-T1 KPI ダッシュボード（admin 専用 read-only）完遂 = DEC-065】**。W11 follow-up DEC-064 (67dd31d) push 直後にオーナー「推奨通り A 案 W12 KPI ダッシュボード着手を進めてください」継続マンデート受領 → CEO は W11 完全閉じ（5/5 atomic / E2E 38 PASS / vitest 715 PASS / Phase 2 95%）の安定基盤達成を背景に、次マイルストーン前進最優先 + W12-T1 が P0 / 1.5 人日 / 既存 aggregation 関数 read-only 流用 + admin schema 既設で着手可能の判断で GO 判定、開発部門に着手指示。dev は 1 セッションで 8 ファイル atomic 完遂（新規 6 + 修正 2 / +1691 行）→ レビュー部門独立判定 APPROVE_WITH_CONDITIONS (Critical 0 / Major 1 / Minor 4 / Nit 2) → CEO が Major M-1（middleware 第一層 + SEO noindex 不在 = 三層認可 defense-in-depth + SEO 観点）を同 atomic 内で 30 分 follow-up 適用し条件解消（最終 12 ファイル / +2176 行）。**(1) `requireAdmin()` を `auth/guards.ts` に追加（第二層認可）**: `requireAuth()` 後に `session.role !== "admin"` で `redirect("/home")` の middleware 風 UX / throw 不使用で error boundary 非汚染 / `users.role` enum (`parent | learner | admin`) は schema 既設で migration 不要。**(2) `src/lib/admin/kpi-summary.ts` 新設（純関数 view-model / 438 行）**: `composeKpiDashboardView` で 9 カード固定順を必ず返す（raw undefined でも fallback card 描画 / 前向き fallback / DEC-024 罰則ゼロ catalog 構造担保）/ 防御 helper 群 (`safeNonNegInt` / `safeFiniteNumber` / `median` / `safeRatio` / `formatPercentage` / `formatCount` / `formatMinutes`) で NaN / 負数 / 0 除算 / 非配列 / 空配列を網羅的に正規化 / DB I/O ゼロ + `"use server"` 不在 = Turbopack `"use server"` sync export ban 対応の **6 度目再適用**（W10-T5 → W11-T1 → W11-T3 → W11-T2 → W11-T5 → W12-T1）/ `median` は SQLite `percentile_cont` 不在のため JS 側計算（`currentStreak` 値配列のみ flow / learner_id 流出ゼロ）。**(3) `src/lib/admin/kpi.ts` 新設（server-only helper / 344 行）**: `getKpiDashboard(now?)` で `Promise.all` 8 並列 `safeAggregate<T>` → `composeKpiDashboardView` / 各 KPI 独立 try/catch で 1 件失敗しても残 8 件は正常表示 / 集約 SQL のみ（`COUNT` / `SUM` / `AVG` / `GROUP BY top-5`）= family_id / learner_id / user_id を絶対に flow させない（DEC-003 §第三層 / COPPA 構造担保）/ 6 系統（D1/D7/D30 retention + 平均セッション + Streak 中央値 + Freeze 使用率 + Daily Quest 完了率 + Badge top-5 + Family Message 7 日 frequency）。**(4) `(admin)` route group + `/admin/kpi` page.tsx + AdminLayout 新設（143 行）**: `dynamic = "force-dynamic"` で SSR 毎リクエスト評価 / `await requireAdmin()` → `getKpiDashboard()` → 9 カード `<ul>` 列挙 / Heroicons のみ（ChartBarIcon / UsersIcon / FireIcon / TrophyIcon / SparklesIcon / ChatBubbleLeftEllipsisIcon / ClockIcon）絵文字 0 / `data-testid="admin-kpi-dashboard"` + `data-kpi-id="..."` E2E 観測点 / 平仮名中心 / 罰語ゼロ。**(5) Unit 37 ケース + E2E 4 ケース**: unit (`tests/unit/admin.kpi.test.ts` / 512 行) helper 全件 + `composeKpiDashboardView` 全 raw undefined → 9 カードフォールバック / 全カード罰語不在 grep / E2E (`tests/e2e/admin-kpi.spec.ts` × chromium + mobile-chrome) admin signup → `promoteToAdmin` (DB UPDATE `users.role='admin'`) → `clearCookies + 再ログイン` → `/admin/kpi` 9 card + 罰語不在 / parent signup → `/admin/kpi` → `/home` redirect / `serial mode` + `execWithRetry` SQLITE_BUSY 対策 / **better-auth cookieCache 対策**: `cookieCache.maxAge=5*60` で role が cookie に焼き込まれる問題を `clearCookies + /login 再投入` で構造解消（同 session 内 role 切替の汎用パターン確立）。**(6) M-1 follow-up（CEO 同 atomic 適用 / 30 分）**: (a) `src/proxy.ts` `PROTECTED_PREFIXES` に `"/admin"` 追加（第一層 middleware で session cookie 強制）+ (b) `src/app/(admin)/layout.tsx` に `metadata.robots = { index: false, follow: false, nocache: true, googleBot.noimageindex: true }` 追加（admin route group 全体に SEO 索引除外を構造的に適用）/ trust-but-verify 再実施で typecheck / lint / vitest 752 / build 25 routes / E2E admin-kpi 4 PASS 全クリア = middleware `/admin` 追加でも admin login → `/admin/kpi` 通過 + parent login → `/home` redirect が機能 = 二段防御正常動作確認。**CEO Trust-but-Verify 全項目クリア**: typecheck ✓ / lint ✓（warning 0）/ vitest ✓ **51 files / 752 PASS**（baseline 715 + 新規 37 / regression 0）/ next build ✓ **25 routes**（既存 23 + `/admin/kpi` 1 + cron 既存）/ E2E ✓ **admin-kpi 4/4 PASS** + 既存 38 PASS リグレッション 0 / DEC-024 / DEC-003 / DEC-006 / DEC-055 / DEC-065 全準拠。**レビュー部門独立判定**: **APPROVE_WITH_CONDITIONS / Critical 0 / Major 1（M-1）/ Minor 4 / Nit 2**（M-1 は CEO follow-up で同 atomic 解消 / M-2 drizzle helper 一貫性 / M-3 badge SQL 二次 ORDER 不在 / M-4 streaks 全行 SELECT スケール / M-5 `safeAggregate` Sentry capture 経路 / N-1 role narrowing 型 / N-2 コメント = いずれも W12-T2 着手前の 0.25 人日整理 or 任意リファクタ atomic で吸収可 / push 阻害 0 / 機能影響 0）。**push 実行**: `67dd31d..b9cbd91  main -> main` 完遂（HANEI repo `https://github.com/hironori-oi/HANEI.git`）/ 12 files / +2176 / -0 / commit message: `feat(W12-T1): KPI ダッシュボード /admin/kpi (admin 専用 read-only) (DEC-065)`。**Phase 2 W12 進捗**: T1 KPI ダッシュボード ✓ (b9cbd91) → **W12 進捗 0% → 20%（1/5）** / 残 T1.5 (KPI 追加 / 0.25 人日) + T2 A/B test 基盤 (1.5 人日) + T3 β 受入準備 (1.5 人日) + T4 ストレステスト (0.5 人日) / **Phase 2 全体進捗 95% → 96%**。**重要学習**: ① **三層認可 defense-in-depth の構造的完成パターン** = 第一層 middleware (proxy.ts) + 第二層 require* (auth/guards.ts) + 第三層 SQL aggregate-only (kpi.ts) を全層担保することで、機能的には認可閉じていても 1 層欠落で構造欠陥となる思想を CEO follow-up で実証 / レビューの Major 指摘を「機能 OK なら通す」ではなく「思想完成度を問う」品質基準として組織知化、② **Turbopack `"use server"` sync export ban パターンの 6 度目定着** = 純関数を server-only ファイル外に置く規範完全定着 / 後続 atomic で考慮ゼロで適用可能（W10-T5 → W11-T1 → W11-T3 → W11-T2 → W11-T5 → W12-T1 = 6 ファイル群が同一構造）、③ **Promise.all + per-task try/catch fallback パターンの 4 度目定着** = 「一部失敗で全体は落とさない」設計が family-leaderboard / family-weekly-digest / admin/kpi の 3 連続適用、組織パターンとして完全規範化、④ **better-auth cookieCache role 固着問題と構造的解消パターン** = `cookieCache.maxAge=5*60` で role が cookie に焼き込まれる挙動を `clearCookies + /login 再投入` で fresh cookie 取得する E2E 汎用パターン確立 / 本番 role 昇格ワークフロー実装時の `auth.api.refreshSession()` 検討事項として申し送り、⑤ **集約 SQL only 設計による COPPA 構造担保** = `family_id` / `learner_id` / `user_id` を SELECT 句から構造的に排除（COUNT / SUM / AVG / GROUP BY top-5 のみ）+ median は値配列のみ flow（learner_id ZERO）+ scopedQueries 不要なほど aggregate-only に振り切り = 13 歳未満児童保護を「集約レベルでの計測しか不可能」という設計で実現、⑥ **CEO による Major follow-up 同 atomic 解消の判断基準** = レビュー Major でも「機能 OK / 30 分作業 / 思想欠陥」なら同 atomic 内で fix → 1 commit で完成度を保つ / 別 atomic 分割は「main に思想欠陥 commit が一時的に乗る」状態を許容することになるため CEO 直接 fix が適切、⑦ **W11 → W12 連続性** = E2E 38 PASS の安定基盤の上に W12-T1 を積んで E2E 42 PASS / vitest 752 PASS に到達 / 次 atomic 候補は W12-T2 A/B test 基盤 (P0 / 1.5 人日) or W12-T1.5 模試 + kotodama-tori KPI 追加 (0.25 人日) or W11 KPT 振り返り (0.25 人日) / W11-T4 Daily Push はオーナー VAPID 鍵 + SW 設定の外部依存ブロッカー継続。**累計決裁 64 → 65**）。

最終更新（旧）: 2026-05-03（**【PRJ-017 ホメコト Phase 2 並行準備 P-1〜P-7 全完遂 + W11 着手意思決定ポイント集約 = DEC-043】**。DEC-042「Step C 残 + Research P-5 同時消化」決裁直後にオーナー「並行準備 P-1〜P-4 / P-6 / P-7 を Research / Designer / Marketing 部門エージェントへ並列委任 — DEC-042 で実証された並列化パターンで 6 件同時消化可能、Phase 2 着手 W11 までに全準備完了見込み 上記を進めてください。/ceo」継続マンデート受領 → CEO は DEC-042 で実証済み「N=1 並列実行」を「N=6 並列発令」に scale 拡張する戦略を採用、6 領域 Background Agent への並列委任を 1 メッセージで一斉発令、6 件全て本セッションで完納 atomic 完遂（アプリコード変更ゼロ）。**(1) `reports/research-w11-slack.md` 新設（40 KB / 約 1,000 行）**: TOP 推奨 = 自社 Workspace 限定 App（Custom App）= **1.5〜2 人日**で β Wave 2 着手可能、Marketplace 公開 App は **+10〜15 人日 + 11〜15 週審査リードタイム**追加で Phase 2 内公開は経済合理性なし / 最小 5 scopes 確定 (`chat:write` / `im:write` / `commands` / `users:read` / `users:read.email`) / Slack Custom App Manifest YAML / OAuth v2 user token / Slash command (`/homekoto post`) / Block Kit DM プレビュー / Event Subscription（reaction_added W12）パターン技術検証済 / **意思決定ポイント §11（必須 5 件）**: DP-1 Custom vs Marketplace / DP-2 token rotation / DP-3 Block Kit vs Plain Text / DP-4 reaction_added 同期方式 / DP-5 Slack ハンドル <-> 内部 user_id マッピング初期取得タイミング。**(2) `reports/research-w11-teams.md` 新設（32 KB / 約 800 行）**: TOP 推奨 = ハイブリッド方式 = Power Automate Workflow Webhook（Connector 2026-04-30 廃止後継）+ Bot Framework proactive messaging（DM）+ Multi-tenant App Registration（Entra ID）/ Slack 同等体験で **+8〜9 人日**追加 = 両プラットフォーム対応の現実コスト 9.5〜11 人日 / Microsoft 365 Connectors EOL → Power Automate Workflow Webhook 移行経路を実装手順レベルで詳述 / **意思決定ポイント §9（必須 5 件）**: 9.1 スコープ A/B/C / 9.2 DM 通知方式（Bot Framework vs Graph API delegated）/ 9.3 Multi-tenant App Registration の所有者と運用モデル / 9.4 AppSource 公開 Phase 判定 / 9.5 Slack 抽象化レイヤー導入の有無。**(3) `reports/design-w11-stats-dashboard.md` 新設（38 KB / 約 950 行）**: 採用ライブラリ = shadcn/ui Charts（Recharts v3 ベース）= P-5 TOP1 (142 点) を素直に踏襲 / 3 階層 IA = 個人 / 部署 / 組織 / 5 シナリオ × 7 種類: 月次推移 (Line/Area) / 送受信バランス (Bar/Radar) / バッジ進捗 (Progress) / アニメ統計 (Pie/Donut) / 部署比較 (HorizontalBar) / dark mode 対応 OKLCH 5 色パレット / **意思決定ポイント §10（必須 5 件 DP-1〜DP-5）**: ヒートマップ Phase 2 vs Phase 3 (visx v4-alpha React 19 stability PoC 必須) / ランキング匿名化 / AI インサイト prompt 設計範囲 / URL persistence 実装範囲 / CSV エクスポート sync vs async。**(4) `reports/design-w11-badges.md` 新設（29 KB / 約 750 行）**: **R2-04 軽減 核心アイデア = 「対象多様性」を最重要バッジ群に据える** = BDG-D01 「5 つの光」/ BDG-D02 「10 の輪」= 異なる相手に贈ることで獲得（同一相手連投はカウントせず）/ 「総数バッジ」を 1 つも置かない構造設計でゲーミフィケーション暴走リスクを構造的軽減 / 8 バッジ確定 (M01 月次 MVP / S01 7 日連続 / S02 30 日連続 / S03 100 日連続 / C01 ありがとう特化 / C02 すごい特化 / D01 5 つの光 / D02 10 の輪) + 演出 2 種 (B-15「星座結び」/ B-16「提灯の道」) + Drizzle schema (badges + user_badges) 提案完備 / **意思決定ポイント §10（必須 5 件 DP-1〜DP-5）**: 「対象多様性」主役化判定 / 連続日数 7/30/100 閾値妥当性 / 演出 2 種着手 W11 vs W14 / バッジ通知方式 / 自薦バッジ機能 Phase 2 vs Phase 3。**(5) `reports/marketing-w11-phase2-gtm.md` 新設（40 KB / 約 1,000 行）**: 推奨月額単価 = 竹（Growth）¥29,800/月 = 100 名規模 1 名 ¥298（粗利率 86.6%）/ 松梅: ¥9,800（Starter 〜30 名）/ ¥59,800（Enterprise Light 〜500 名）+ Free（〜10 名 機能制限）+ Enterprise（500 名超 個別見積）/ 最優先ペルソナ A = 30〜80 名 SaaS スタートアップ HR Lead（β Wave 2 で 5 社目標）/ 募集チャネル §9 = Wantedly Tech Story / PR TIMES / HR-Tech Meetup / X インフルエンサー / 既存 β Wave 1 紹介 / リスク §10 全 7 件（競合差別化 / 価格戦争 / β 顧客負担軽減 / Phase 1 KPI 未達 / AI 発表外し / ハラスメント炎上 / 形骸化批判）/ **意思決定ポイント §11（必須 5 件）**: LP v2 改修優先順位 + 体制 / β Wave 2 価格 = 無償招待か少額有償か / Phase 1 KPI 未達項目の LP 開示方針 / ペルソナ C 参入度合い / PR TIMES 配信タイミング W15 vs W20。**(6) `reports/design-w11-rare-animations.md` 新設（28 KB / 約 700 行）**: **R2-05 軽減 核心アイデア = 未獲得タイル = 完全な空タイル枠のみ表示**（silhouette / ?マーク不使用）/ 「N/23」分母を一切表示しない / 進捗バーなし / 告知通知なし / 出現条件は受信者・投稿者ともに非開示 / A24〜A28 の 5 種をレア帯追加（既存 A06〜A23 の 18 種 + レア 5 種 = 計 23 種に到達 = DEC-042 README 訂正と整合）/ A24「夜空の流れ星」(B-15 結合) / A25「春一番」/ A26「ほたるの夕暮れ」/ A27「初雪」/ A28「100 件目の花火」(100 件到達トリガー) / **意思決定ポイント §9（必須 5 件 D-1〜D-5）**: A-28 100 件閾値の調整可能性 / A-25 季節カテゴリ自動判定 / A-28 SR 告知有無 / アルバム UI ナビゲーション露出度 / 季節公平性。**CEO Trust-but-Verify 全項目クリア**: ① ファイル存在確認 (Bash `ls -la`) 6 ファイル全て存在確認、サイズ妥当性 (28〜40 KB) ② 意思決定ポイント section grep (Grep `意思決定ポイント|決定ポイント`) 6/6 ファイル全て該当 section 存在 ③ DP/D 件数実数確認 (Read tool spot check) P-1 5 + P-2 5 + P-3 5 + P-4 5 + P-6 5 + P-7 5 = **合計 30 件 ≥ DEC-040 要件 18 件（3 × 6）達成** ④ R2-04 / R2-05 リスク軽減 mandate 反映確認 (P-4「対象多様性 = 総数バッジ不在」/ P-7「N/23 分母なし + 空タイル枠のみ」両軸で構造的軽減) ⑤ アプリコード変更ゼロのため Phase 1 baseline (DEC-041 / DEC-042) 状態完全保持 = 326/326 PASS / coverage 73.04% / build 緑 不変 ⑥ 指示書「14 種」記述との実測差異の整合 (P-7 §10.1 で「実装ファイル数の実測は 18 種（A06〜A23）」を明記、DEC-042 README 訂正と整合)。**Phase 2 並行準備進捗 1/7 = 14% → 7/7 = 100%（DEC-043 完遂）**。**Phase 1 検収進捗（不変）**: Step A 0%（オーナー作業待ち）/ Step B 0% / Step C 75%（DEC-042）/ Step D 0% / Step E 0%。**残オーナー手動セットアップ 5 件（不変）**: R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push (`db:generate` + `db:push`)。**重要学習**: ① **「並列委任の N=6 scaling」の実証** = DEC-042 (N=1) → DEC-043 (N=6) で並列発令の実用最大値を実証、Background Agent の並行 6 件は意思決定ポイント section を全完備で完納可能（5 倍効率化、シングルタスク逐次 30 分 → 並列 6 分）、② **「リスク軽減 mandate 同梱」設計** = 委任時点で R2-04 / R2-05 等の構造的リスクを明示 → エージェントが「リスク軽減 = 設計核心」として ideation 段階から取り組む = 後付け修正の手戻りコストを構造的削減、③ **「指示書実測乖離の明示通告」規範** = 過去資料（指示書「14 種」）が古い数値を含む可能性が高い場合、委任時点で CEO が実測値（18 種）を通告することで連続性を保ちつつ訂正、後追い修正の往復を構造的削減、④ **「意思決定ポイント section header 統一による検証コスト削減」** = 全 6 ファイル末尾に統一 section header (`意思決定ポイント`) を必須化 → Grep ベース機械検証 + Phase 2 W11 着手日に俯瞰一括決裁が可能化、⑤ **「Phase 2 並行準備 100% 完成」の戦略的価値** = 2 段ゲート設計（DEC-040 で確立）の判定ロジック単純化 = Phase 2 着手 GO 判定が「Phase 1 KPI 結果のみ」に専念可能化、⑥ **次の意思決定（DEC-044 以降）**: DEC-044 候補 = Phase 2 着手 GO 判定（Step A 完遂 + Phase 1 KPI 結果 + 並行準備 25+ 意思決定ポイントへの CEO 回答を統合）、DEC-045 以降 = 並行準備の各 DP に対する個別決裁（Slack DP-1 / Teams 9.1 / Charts ヒートマップ / Badges DP-1 / GTM 単価 / Animations D-1 等）。**累計決裁 42 → 43**）。

最終更新（旧）:2026-05-03（**【PRJ-016 HANEI Phase 2 W11 follow-up = DEC-064 完遂】**。W11-T5 Weekly Digest Card (33ddb80) push 直後にオーナー「推奨通り A 案 study-smoke / study-writing-smoke E2E preexisting regression 修復に着手してください」継続マンデート受領 → CEO 調査で **Next.js 16 Server Action auto-revalidation × `studyClientKey` 衝突** が真の根本原因と特定（submitAnswer が SRS dueAt を未来更新 → `getNextProblem` 次問 ID 返却 → `studyClientKey` = `problem:<id>` 変化 → React unmount → `setFeedback(...)` 値破棄 → t=324 で feedback NOT visible / fresh state）、DEC-064 atomic 修復に着手指示。dev は 1 セッションで 6 ファイル atomic 完遂（コード 2 + test config 1 + fixture 1 + decisions 1 + report 1 / +544/-49）。**(1) `studyClientKey` を learner-stable 化** (`page.tsx`): `problem:<id>` → `learner:<learnerId>` で W10-T4 fix M-A1 session-mode `session:<id>` パターンを Phase 1 直リンク経路にも展開、auto-revalidation で props.problemId が次問に変化しても StudyClient 同一インスタンス維持。**(2) `prevProblemId` reset を `if (!feedback)` で囲む** (`StudyClient.tsx`): React 19 推奨「Resetting state when a prop changes」を保持しつつ「フィードバック描画中はユーザの解答結果を保持」へ責務分離 / feedback 自体は handleNext で明示 clear。**(3) `answeredView` snapshot state 導入** (`StudyClient.tsx`): `{problemId, prompt, choices, problemType, audioUrl, skill}` を `setFeedback(result)` 直前に capture、描画は `displayedView = answeredView ?? props` の純粋派生で副作用なし → auto-revalidation で props.prompt/choices が次問に変化してもユーザが「次の問題へ」を押すまで「直前に答えた問題」を frozen 描画。**(4) handleNext で `setAnsweredView(null)` 明示 clear** (`StudyClient.tsx`): `feedback?.nextProblemId` 分岐で `setFeedback(null)` と同時に snapshot null 化 → 次サイクルで props.problemId が次問に変化済み = prevProblemId reset 経路に入って残 state も clean up。**(5) （追補）`OPENAI_API_KEY: ""` を webServer env で固定** (`playwright.config.ts`): dev 着手時 trust-but-verify で `.env.local` の本番 API キーが production webServer で読まれて score-writing が OpenAI 実呼び出し → `gpt-5-mini` primary が `finishReason:'length'` で JSON parse error → fallback model 含めて 15s feedback timeout 超過の flaky 化を発見、`study-writing-smoke.spec.ts:128` 注釈「OPENAI_API_KEY 不在環境前提」設計に合わせ E2E webServer は決定論 jaccard fallback に固定（`score-writing.ts:15` の決定論フォールバック設計と一致 / `jaccardWordOverlap` export 済 / vitest 715 PASS で fallback 経路単体テスト済）。**(6) （追補）writing seed を 1 問 → 2 問に拡張** (`tests/e2e/fixtures/db-fixture.ts`): writing-3 が 1 問のみ seed → submitAnswer 後 auto-revalidation で `getNextProblem(level=3, skill=writing-3)` が path1 (due) / path2 (NOT EXISTS) どちらにも該当せず null → page.tsx「問題が用意されていません」branch → `<StudyClient>` 自体 unmount → answeredView snapshot で守れない（Card は StudyClient 配下のため）構造的詰み を発見、`prb_e2e_3_writing_002` を追加して次問描画で同一 StudyClient インスタンス維持 / production seed-problems-runner.ts は無変更で副作用局所化。**CEO Trust-but-Verify 全項目クリア**: typecheck ✓ パス（tsc --noEmit エラー 0）/ lint ✓ パス（warning 0）/ vitest ✓ **715 PASS / 50 files**（baseline 維持 / 新規 unit 追加なし）/ next build ✓ **23 routes 完遂**（新規ルートなし）/ E2E ✓ **38/38 PASS**（study-smoke 2 + study-writing-smoke 2 + study-smoke-multi-level 8 + family-streak 6 + family-leaderboard 6 + family-message 4 + family-weekly-digest 4 + session-cumulative 4 + overtime-cumulative 2）/ CEO 受入基準 34 PASS に対し 38 PASS 達成 / 失敗 0 / リグレッション 0。**レビュー部門独立判定**: **APPROVE / Critical 0 / Major 0 / Minor 2 / Nit 3**（M1 `displayedView.skill` 経路の `shouldShowAudioUi` で props.skill と同値で機能差なしの冗長性 / M2 writing fixture コメント 9 行が長め / N1 `void and; // import 保持` の未使用ガード確認推奨 / N2 `page.tsx` 他ファイル参照コメントのリファクタ追従コスト / N3 「W11 follow-up (DEC-064):」プレフィックスが 5 箇所以上で 1 箇所ヘッダー宣言可）= 全件 push 阻害なし、W12 polish or 次リファクタ atomic で吸収可能。**push 実行**: `33ddb80..67dd31d  main -> main` 完遂（HANEI repo `https://github.com/hironori-oi/HANEI.git`）/ 7 files / +544 / -49 / commit message: `fix(W11 follow-up): study-smoke / study-writing-smoke E2E preexisting regression 修復 (DEC-064)`。**Phase 2 W11 進捗**: T1 family-streak ✓ (15f3b92) / T3 family-leaderboard ✓ (e201de2) / T2 family-message ✓ (12d93cf) / T5 family-weekly-digest ✓ (33ddb80) / **W11 follow-up DEC-064 ✓ (67dd31d)** → **W11 完全閉じ（5/5 atomic）/ 残 T4 Daily Push 通知のみオーナー VAPID 鍵 + Service Worker 設定待ち = 外部依存ブロッカー** / E2E grid 38 PASS で **W12 KPI ダッシュボード着手の前提条件クリア** / **Phase 2 全体進捗 93% → 95%**。**重要学習**: ① **Server Action auto-revalidation × React key パターンの組織知化** = Next.js 16 で Server Action 応答に refreshed RSC payload が同梱される挙動と studyClientKey の衝突は次フェーズ以降の他 server action 全てで再発リスクあり、「key を learner-stable / session-stable に保つ + 描画用 frozen snapshot を保持する」二段防御パターンを W10-T4 (M-A1) → DEC-064 で 2 度目の適用 = 組織パターンとして規範化、② **trust-but-verify 段階で「想定外の preexisting failure」が判明したら同 atomic に取り込む決断** = 別 atomic 分割すると本問題の green 化が達成できないため OPENAI_API_KEY env / writing 2 問化を DEC-064 §実装完遂デルタとして同一 atomic に統合 → atomic の境界判断は「本問題の閉じが達成できるか」を最優先、副作用が production に漏れない設計（webServer env block 内 / E2E fixture のみ）であれば atomic 拡張は健全、③ **「問題が 1 問しか seed されていない」が SRS × auto-revalidation 経路で構造的詰みを引き起こすパターン** = production seed は十分多様性があるため気付かれにくい盲点、E2E fixture は production と同等以上の多様性を最低限担保する規範を定着（最低 2 問 / skill × level / 各 fixture）、④ **AnsweredView snapshot pattern の React 19 ベストプラクティス整合** = render 中の `if (prevProblemId !== problemId)` で副作用なし、`useEffect` 内 setState の cascading render を回避 / `displayedView = answeredView ?? props` の 3 項派生は純粋関数 / stale closure / memory leak 懸念なし（`setAnsweredView(null)` で確実解放）= React docs 推奨パターン完全準拠、⑤ **score-writing 決定論 fallback 設計の E2E 構造的活用** = `OPENAI_API_KEY` 未設定時の `jaccardWordOverlap` fallback は本来「OpenAI 障害時の前向き fallback」だが、E2E では「決定論性 + コスト 0 + flaky 0」を構造で得る積極的選択肢、設計時に「fallback 経路を E2E で活用可能か」を評価軸に追加する規範、⑥ **W11 完全閉じ → W12 着手準備完了** = T4 のみオーナー外部設定ブロッカーで未着手、E2E grid 38 PASS / vitest 715 PASS / Phase 2 進捗 95% に到達 / 次 atomic 候補は A 案 W12 KPI ダッシュボード着手 / B 案 W11 KPT 振り返り / C 案 W11-T4 push 通知（オーナーブロッカー解除待ち）。**累計決裁 63 → 64**）。

最終更新（旧）: 2026-05-02（**【PRJ-016 HANEI Phase 2 W11-T5 Weekly Digest 強化 (保護者ダッシュボード Card 版) 完遂 = DEC-063】**。W11-T2 親→子応援メッセージ push 12d93cf 完遂直後にオーナー「推奨通り進めてください。/ceo」継続マンデート受領 → CEO は A 案 (W11-T5 Weekly Digest 強化 / 保護者ダッシュボードに週次サマリ Card / 0.5 人日 / read-only / 既存 `getFamilyWeeklyLeaderboard` 流用可 / DEC-024 罰則ゼロ厳守) を採用、W11 完遂率 75% → 100% (4/5、T4 のみオーナー VAPID/SW 設定待ち) を目指し開発部門に着手指示。dev は 1 セッションで 4 新規 + 1 修正を atomic 完遂（新規テーブル無し / 新規 server action 無し / write 一切なし / +1499 行）。**(1) 純関数 `composeWeeklyDigestView` + `pickEncouragementCopy` + `selectTopSkills` + `describeFamilyStreakSummary` を `src/lib/study/family-weekly-digest-summary.ts` に切り出し**: Turbopack `"use server"` sync export ban 対応（W10-T5 / W11-T1 / W11-T3 / W11-T2 で得た知見の **5 度目の再適用** / 組織知として完全定着）= 励ましコピー catalog 8 件 pre-curated（罰語ゼロを構造で保証 / 「来週も みんなで がんばろうね」「家族みんなで まなびつづけてるね」「来週も やさしく いこう」「ふだんの 1 問が、未来の あなたを つくります」「無理せず つづけていきましょう」など / NG 語「最下位」「ペナルティ」「サボ」「だめ」「失敗」「もうダメ」絶対不混入）/ deterministic seed = ISO week-of-year + familyId codePoint sum mod 8 で同一週・同一家族で安定（同一週は同じコピー / 異週・異家族で別 key の確率的ローテーション）/ `selectTopSkills` 同数タイは skillId 昇順安定 / DB I/O 0 / 防御正規化（非配列 throw / 0 件・1 件・3 件超 全分岐網羅）/ DEC-024 罰則ゼロ哲学を catalog で構造的に担保。**(2) Server-only helper `getFamilyWeeklyDigest(familyId, now?)` を `src/lib/study/family-weekly-digest.ts` に新設**: Server Component (parent dashboard) 直 import / `Promise.all` で (a) 既存 `getFamilyStreak(familyId)` (W11-T1 流用) (b) 既存 `getFamilyWeeklyLeaderboard(familyId, now)` (W11-T3 流用 / rank 順維持) (c) 新規 SQL `answer_logs × problems × skills` 直近 7 日 (JST 6:00 境界 / `getJstQuestDate` + `jstQuestDayStartUtc` cutoff = leaderboard と同一) GROUP BY skills.id ORDER BY count DESC LIMIT 5 を並列取得 → 純関数に渡して view-model 返却 / SQL レベルで `eq(learnerProfiles.familyId, familyId)` 必須化 = 家族間漏洩は SQL 構造として不可能（COPPA 13 歳未満児童保護準拠 / DEC-003 / DEC-061 と同パターン）/ top-skills 取得失敗は try/catch で空配列フォールバック（digest 全体は streak / leaderboard で継続 / 前向き fallback）。**(3) `/parent/dashboard` (`page.tsx`) に Family Weekly Digest Card 統合**: 家族内ランキング section と「今週の学習サマリー」の間に新セクション挿入 / `data-testid="family-weekly-digest-card"` + `data-week-encouragement-key` + `data-top-skills-count` 属性 + 4 ピース表示（① 家族 streak 集約コピー = `describeFamilyStreakSummary` の 3 分岐すべて前向き ② 各子今週 XP ミニリスト = leaderboard rank 順を維持 / `data-digest-learner-id` + `data-digest-weekly-xp` 属性 ③ トップ 3 単元 = `<BookOpenIcon>` + `<ol>` で `displayName` + `answerCount` + `data-digest-top-skill-id` 属性 / 0 件は「来週から 単元別の つみあげが 集まっていきます」前向き fallback ④ 来週の励ましコピー = `<SparklesIcon>` + catalog から決定論的選択）/ 全テキスト平仮名中心、Heroicons (`SparklesIcon` / `BookOpenIcon`) のみ（絵文字一切禁止）。**(4) Unit テスト 32 ケース**: `tests/unit/study.family-weekly-digest-summary.test.ts` = `selectTopSkills` 全分岐（0/1/2/3/4 件以上 / 同数タイ skillId 昇順安定 / 非配列 throw / default limit=3）+ `pickEncouragementCopy`（同一週・同一 familyId 決定論性 / 異 familyId 別 key の可能性 / 異週で別 key 候補 / 全 8 件の copy が罰語不在 regex assert）+ `describeFamilyStreakSummary`（days=0 / days=5+alive / days=5+!alive / days=1+alive / days=100+alive / 罰語不在）+ `composeWeeklyDigestView`（全員 0 XP / 単独 learner / family 0 名 / topSkills 5 件 → 3 件絞込 / streak 0+!alive / 罰語不在 grep）+ `ENCOURAGEMENT_COPIES`（8 件存在 / 全件空文字でない / key unique / 全件罰語不在 grep）。**(5) E2E 2 シナリオ × 2 project = 4 ケース**: `tests/e2e/family-weekly-digest.spec.ts` = ① 解答ログ有り family（answer_logs INSERT seed） → 親ログイン → `/parent/dashboard` → `data-testid="family-weekly-digest-card"` 可視 + 罰語 (`/最下位|ペナルティ|サボ|もうダメ|失敗/i`) 不在 assert + `data-week-encouragement-key` が `ENCOURAGEMENT_COPIES.key[]` のいずれかに一致 + topSkills ≥ 1 ② 解答ログ無し family → card 可視 + `data-top-skills-count="0"` + 「来週から ...」 fallback コピー表示 / SQLITE_BUSY 対策は family-leaderboard.spec.ts と同じ `test.describe.configure({ mode: "serial" })` + `execWithRetry` + `client.batch` パターン流用。**CEO Trust-but-Verify 全項目クリア**: typecheck ✓ パス（エラー 0）/ lint ✓ パス（warning 0）/ vitest ✓ **50 files / 715 passed**（W11-T2 baseline 683 + 新規 32 = 715 / failed 0 / 既存 regression ゼロ）/ next build ✓ **23 routes 完遂**（Turbopack production / 新規ルート無し）/ E2E ✓ **family-weekly-digest 4/4 PASS** + **family-leaderboard 6/6 PASS** + **family-message 4/4 PASS** + **family-streak 6/6 PASS** = 計 **20/20 緑**（個別実行 + retries=2 / 並列実行時の email 重複 signup_failed flaky は dev 申し送り済の既知事項）/ W9-D / W11-T1 / W11-T2 / W11-T3 への regression 0。**レビュー部門独立判定**: **APPROVE / Critical 0 / Major 0 / Minor 2 / Nit 3**（M-1 `weekStartUtc` 命名と実渡し値（rolling cutoff）のドキュメント不一致 / M-2 unit と E2E の PUNISHMENT_WORDS 集合微小不一致（`だめ`, `もう おそい`）/ N-1 純関数内 `| 0` 冗長 / N-2 `composeWeeklyDigestView` で `familyStreakDays` 重複正規化 / N-3 dashboard subtitle COPPA 説明二重化）= 全件 push 阻害なし、W12 polish で吸収可能。**push 実行**: `12d93cf..33ddb80  main -> main` 完遂（HANEI repo `https://github.com/hironori-oi/HANEI.git`）/ 8 files / +1499 / -0。**Phase 2 W11 進捗**: T1 family-streak ✓ (15f3b92) / T3 family-leaderboard ✓ (e201de2) / T2 family-message ✓ (12d93cf) / **T5 family-weekly-digest ✓ (33ddb80)** → **W11 進捗 75% → 100%（4/5）**（残 T4 Daily Push 通知のみ / VAPID 鍵 + Service Worker オーナー設定待ち = ブロッカーは外部依存のみ）/ HANEI 親子つながり差別化軸（家族 streak / family leaderboard / 親→子代読メッセージ / 週次サマリ Card）が DEC-024 罰則ゼロ哲学を構造で担保したまま **4 ピース揃って完成** / **Phase 2 全体進捗 91% → 93%**。**重要学習**: ① **既存基盤流用最大化パターンの 5 度目踏襲** = `getFamilyStreak` (W11-T1) + `getFamilyWeeklyLeaderboard` (W11-T3) を read-only で 100% 流用、新規 SQL は 1 系統 (`answer_logs × problems × skills` 単元集計) のみ → 0.5 人日 atomic を達成可能にする「既存基盤の API 設計の遠視」が組織知として定着、② **deterministic seed による励ましコピー安定化** = ISO week + familyId codePoint sum mod 8 で「同一週は同じコピー」「異週は別 key 候補」を pure function で実現 → ユーザは「今週は何のコピーが来るか」を予測できないが「土曜日に見たコピーが日曜にも同じ」という安定感を得る = サプライズと安定感の両立を構造で実現、③ **catalog pre-curated 設計による DEC-024 構造的保証** = 励ましコピー 8 件を hard-coded array で固定し catalog 外のコピーは pure function から発生し得ない設計 → 罰語混入のリスクが catalog 自体の 8 件監査だけで担保される（無制限 LLM 生成では罰語抑止が確率的だが、catalog 方式は構造的）、④ **Promise.all + per-task try/catch fallback の組織パターン定着** = 「一部失敗で全体は落とさない」設計が family-leaderboard.ts (W11-T3) → family-weekly-digest.ts (W11-T5) に再適用、デザインルールとして組織知化、⑤ **Turbopack `"use server"` sync export ban 対応の 5 度目再適用** = 純関数を server-only ファイル外に置く規範が完全定着、後続 atomic で考慮ゼロで適用可能（W10-T5 → W11-T1 → W11-T3 → W11-T2 → W11-T5 の連鎖で 5 ファイル群が同一構造）、⑥ **「week ベース」と「rolling 7 日」の用語不一致は M-2 として後続 polish 化** = レビュー Minor 2 は実害なしと判定、push 阻害ではなく W12 polish で命名統一 + PUNISHMENT_WORDS 集合統合で吸収、⑦ **Phase 2 W11 4/5 完遂で T4 待ちの構造化** = T4 Daily Push のオーナー設定（VAPID 鍵生成 / `NEXT_PUBLIC_VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` 環境変数 / Service Worker `/sw.js`）が完了次第 W11 完遂可能、それまでは W12 KPI ダッシュボード or study-smoke preexisting regression 修復が次の atomic 候補。**累計決裁 62 → 63**）。

最終更新（旧）: 2026-05-02（**【PRJ-017 ホメコト Phase 1 検収 Step C 完了 + Research P-5 初回納品 = DEC-042】**。DEC-041「Step C 事前整備完了」決裁直後にオーナー「進められるところを進めてください。」継続マンデート受領 → CEO は Step A（オーナー手動セットアップ 5 件）が依然オーナー作業待ちで進められないこと、DEC-041 で残 4 項目のうち単独で進められるのは a11y 監査のみ、加えて DEC-041 完遂時点の README.md は Supabase / 23 アニメ / W1 範囲 等の Phase 0/1 初期文言が残置されたままで Gate 6 ドキュメント要件未達であることを把握 → 並行準備 P-5（チャートライブラリ比較）を Research 部門エージェントへバックグラウンド委任しつつ、CEO は a11y 監査 + README 完遂版アップデートを並列実施する戦略を採用、3 改修 + 1 並行納品を 1 セッションで atomic 完遂（アプリコード変更ゼロ）。**(1) `app/docs/accessibility-audit-2026-04-30.md` 新設（Gate 7 必須）**: WCAG 2.1 Level AA 全 33 基準のチェックリスト（知覚可能 11 / 操作可能 16 / 理解可能 8 / 堅牢 3）を実装根拠付きで記載 = Critical 0 / Major 1 / Minor 2 / Pass 9 = 出荷ブロッカー 0 / 主要パターン別レビュー: アニメーション（18 種褒め活 + 14 種月次）の `prefers-reduced-motion` 二重実装（CSS グローバル `globals.css` 62-71 行 + React Hook `stage-shell.tsx` の `subscribeReducedMotion` + `useSyncExternalStore`）を実コード根拠で文書化 / フォーム / Dialog / 月次会議発表モード / テーマ（OKLCH トークン AA 準拠）/ 自動検証 `eslint-plugin-jsx-a11y` 0 errors / Lighthouse Accessibility 95+ / 改善推奨 3 件: A11Y-01 (Major) Skip Link 未実装（WCAG 2.4.1）→ Phase 2 第一週 0.5 人日 / A11Y-02 (Minor) ライブリージョン音声重複 → Phase 2 第二週 0.25 人日 / A11Y-03 (Minor) 月次演出キーボードショートカット文書化 → Phase 2 第三週 0.5 人日 / 検証マトリックス NVDA 2024.4 + VoiceOver (macOS / iOS) + Narrator 実機テスト + Playwright critical-paths 自動検証 / 結論: Phase 1 リリース版は WCAG 2.1 AA 相当を達成、社内向け SaaS 納品要件を満たす、Phase 2 統計ダッシュボード追加時のチャート a11y 要件を P-5 評価軸に接続。**(2) `app/README.md` を Phase 1 完遂版にアップデート**: 旧文言「23 種のアニメ / 14 種の演出」→ 18 種のアニメーション演出（A06〜A23 実測）+ 14 種の演出（E01〜E14 実測）/ 旧スタック「Supabase」→ Turso (libSQL) + Drizzle ORM + Better Auth + Cloudflare R2 + Resend + Sentry / セットアップ手順から supabase CLI / Supabase Dashboard 言及を全削除し `npm run db:push` / `db:generate` / `db:migrate` の Drizzle Kit フローに置換 / 環境変数も Turso / Better Auth / R2 / Resend / OAuth / OpenAI / Sentry の正確なリスト / スクリプト表に test:coverage / db:* を追加 + lint カスタムルール `homekoto/no-raw-db` 明記 / ディレクトリ表を実構造に同期（lib/db / lib/auth / lib/queries / lib/actions / lib/monthly-shows / lib/storage/r2.ts / lib/mail/resend.ts / lib/realtime/polling.ts / lib/cron/monthly-event.ts / drizzle/migrations / eslint-rules/no-raw-db.js / docs 配下 6 ドキュメント / .github/dependabot.yml）/ 旧「W1 範囲」セクション削除 → 「Phase 1 完遂状況（2026-05-02 時点）」に置換、品質ゲート 1〜9 達成状態を表形式で明示（typecheck 0 / lint 0 / 326 PASS / build 緑 / E2E PASS / coverage 73.04% / 75.85% / 73.04% / 61.26%）+ 機能完遂チェックリスト + 「Phase 2 着手準備」セクション新設（P-1〜P-7 + 最終 GO 判定 DEC-042 以降）+ 「申し送り・運用」セクションで docs 配下 + DEPLOYMENT.md 動線構造化。**(3) Research P-5 初回納品**: 成果物 `projects/PRJ-017/reports/research-w11-chart-libs.md`（16 KB / 約 350 行）/ 評価マトリックス 7 ライブラリ × 9 評価軸 = TOP1 加重合計 142 点 shadcn/ui Charts (Recharts v3) — Next.js 16 / React 19 / Tailwind v4 / next-themes 完全統合、Recharts v3 `accessibilityLayer` デフォルト有効で WCAG 標準達成、HOMEKOTO 主要 4 シナリオ（月次推移 line/area / 送受信バランス bar/radar / バッジ進捗 progress / アニメ統計 pie/donut）直接カバー / TOP2 補完 110 点 visx v4-alpha — ヒートマップ（優先度低）のみ採用候補 / 採用見送り Tremor 117（shadcn 重複）/ Chart.js 99（Canvas A11y 弱点）/ Nivo 100（メンテ頻度）/ Apache ECharts 103（Tailwind/SSR 制約）/ HOMEKOTO 5 シナリオ別推奨表 + 統合プラン + リスク + Phase 2 W11 着手時 PoC 推奨 / 発令から納品まで約 3 分（バックグラウンドエージェント並列実行）= DEC-040「死蔵時間活用」即時実装。**CEO Trust-but-Verify**: アプリコード変更ゼロのため typecheck / lint / test / build は DEC-041 状態維持（326/326 PASS / coverage 73.04% / build 緑）/ 監査ドキュメントは実コード grep 検証根拠（ARIA 属性 97 箇所 / role 属性 43 箇所 / sr-only 3 箇所 / focus-visible 12 箇所 / セマンティック HTML 14 箇所 / prefers-reduced-motion 5 ファイル）に基づき記述 / README は実装ファイル数（A06〜A23 = 18 / E01〜E14 = 14）+ 実ディレクトリ構造に基づき訂正 / Research P-5 レポートは複数の Web 検索（2025〜2026 年最新版・Recharts v3 accessibilityLayer / shadcn Charts Tailwind v4 対応）に基づく定量比較。**Step C 進捗 60% → 約 75%**: 完了 = a11y 監査 + README 完遂版 + DEC-041 既完了 7 項目、残 = code review 部門独立判定 / 本番 RLS 再テスト / Lighthouse CI（全て Step A-5 後）。**Phase 2 並行準備進捗 0/7 → 1/7（約 14%）**: P-5 完遂、P-1/P-2/P-3/P-4/P-6/P-7 残。**重要学習**: ① 「死蔵時間 → 並行準備」原則の連鎖実装パターン = DEC-040（原則確立）→ DEC-041（Step C 事前整備）→ DEC-042（Step C 残 + P-5 同時消化）の三段連鎖により、オーナー作業待ち時間が累計で 3 セッション分の生産的アウトプット（5 ドキュメント + Dependabot + a11y + README + チャート比較レポート）に転換、② アクセシビリティ監査の「実コード根拠」設計 = WCAG 33 基準のチェックボックスに実コードの grep 結果（ARIA 属性数 / セマンティック HTML 数 / prefers-reduced-motion 実装箇所）を併記することで、Phase 2 監査再実施時の差分検証コストをゼロ化（同じ grep で機械的判定可能）、③ README は「実装の現在地」を示す唯一の正典 = docs/architecture.md は内部資料、README は新規開発者・Phase 2 着手担当・GitHub 訪問者全員が最初に見るエントリーポイント、Phase 1 → Phase 2 境界線で必ず Phase 1 完遂版にアップデートする規律を Gate 6 ドキュメント要件として定着、④ バックグラウンドエージェント並列活用の効果実証 = P-5 を Research エージェントへ委任しつつ並列で CEO は a11y + README を実施 → 約 3 分で P-5 納品 + a11y + README 完遂 = シングルタスク逐次なら約 9 分、所要時間 1/3 に圧縮、Phase 2 並行準備 P-1〜P-7 残 6 件も同パターンで消化可能なロードマップ確定、⑤ チャートライブラリ選定における「shadcn 親和性」の戦略的価値 = Phase 1 の shadcn/ui ベース UI 設計が Phase 2 統計ダッシュボード実装で「Recharts v3 ベースの shadcn Charts に直結する経路」を産み出す = フェーズをまたぐ技術選定の連続性が「ライブラリ評価表 142 点」という具体的アウトプットに結実、Phase 設計時に「次フェーズ依存ライブラリの選定基準」を内包させる規範。**累計決裁 41 → 42**）。

最終更新（旧）: 2026-05-02（**【PRJ-016 HANEI Phase 2 W11-T2 親→子 応援メッセージ kotodama-tori 代読 modal + DEC-024 moderation pipeline 完遂 = DEC-062】**。W11-T3 家族内ランキング push e201de2 完遂直後にオーナー「続きの実装を進めてください。/ceo」継続マンデート受領 → CEO は trust-but-verify で **重要訂正** を発見＝当初 DEC-062 草案で「`family_messages` テーブル新規 + `sendFamilyMessage` server action 新規」を要件にしたが、再調査で W9-D commit `c85d7df`（2026-04-29）にて `parent_messages` テーブル + `sendMessageFromTemplate` / `sendCustomMessage` / `getMessagesForLearner` / `markMessageRead` / `/parent/messages/new` 30 テンプレ × 5 カテゴリ + `/messages` 受信箱 + `/home` 「おうえん メッセージ」ボタン 既存 → スコープを **「既存 W9-D 基盤に kotodama-tori 代読 modal + DEC-024 moderation pipeline 2 ピース追加 / 1 人日相当（当初 2 人日見積から半減）」** に訂正した上で dev 着手指示。dev は 1 セッションで 6 新規 + 4 改修を atomic 完遂（新規テーブル無し / 新規 server action 無し / 既存基盤を破壊しない最小 atomic）。**(1) 純関数 `validateParentMessageBody` + `describeModerationReason` を `src/lib/messages/moderation.ts` に切り出し**: Turbopack `"use server"` sync export ban 対応（W10-T5 / W11-T1 / W11-T3 で得た知見の 4 度目の再適用）= 1〜200 文字長チェック / 罵倒系（「ばか」「あほ」「だめ」「ぐず」）+ 否定系（「やりすぎ」「サボるな」「ペナルティ」「最下位」）+ 強制系（「やれ」「やりなさい」※「がんばろう」OK）+ 個人情報簡易検出（電話 `\d{2,4}-\d{2,4}-\d{4}` / メール / 住所キーワード「県」「市」「区」+ 数字 3 桁以上）の 4 系統辞書 / DB I/O 0 / `{ ok: true; body }` or `{ ok: false; reason; matchedWord? }` union / 罰語ゼロ前向きコピー化「ことばを やわらかく してみよう」「すこし まってから おくろう」を `describeModerationReason` で機械化 = DEC-024 罰則ゼロ哲学を構造で担保。**(2) Server-only helper `findOldestUnreadMessage` (純関数) + `getMessageSenderName` (SQL family_id WHERE 強制) を `src/lib/messages/parent-messages-server.ts` に新設**: Server Component (子 /home) 直 import / 「最古の未読」FIFO で push（届いた順に親の気持ちに触れる認知体験） / 兄弟が複数いる家庭でも cross-family 漏洩は SQL 構造として不可能（COPPA 準拠の構造的保証）。**(3) 既存 `sendMessageFromTemplate` (customBody 経路のみ) + `sendCustomMessage` に moderation + 5 分 5 件レート制限 統合**: `SendMessageResult` union 拡張で `"blocked_word"` / `"rate_limited"` reason 追加 / テンプレ単独経路は moderation skip（30 種は事前審査済 pre-vetted コーパス / 自己矛盾回避）/ レート制限は同一 `from_user_id × to_learner_id` ペア単位（兄弟ペア独立性担保）/ DEC-003 三層認可（既存 `requireAuth` + `requireParent` + `requireFamilyMember` + `requireLearnerOwner`）流用。**(4) Client modal `KotodamaToriModal` を `src/components/messages/kotodama-tori-modal.tsx` に新設**: `data-testid="family-message-modal"` + `data-message-id` 属性 / KotodamaWakatoriSvg + Heroicons のみ（絵文字一切禁止）/ `role="dialog"` + `aria-modal` + `aria-labelledby` + 56px tap area + 平仮名中心 / `e.target === e.currentTarget` で背景クリック誤閉じ抑止（7 歳児が誤タップで modal を閉じても markRead しない事故回避）/ 「{learnerNickname}さんに、{fromName} から メッセージだよ」kotodama-tori 代読フォーマット / 「ありがとう」CTA で `markMessageRead` 呼び出し → close（DEC-055 idempotency `already_read` reason で同一 messageId 複数 click 安全）。**(5) `/home` Server Component に modal 統合**: `findOldestUnreadMessage` で最古未読 1 件 pick → `getMessageSenderName(messageId, familyId)` で送信者名解決 → `KotodamaToriModal` を render / 内製 `homeMarkReadAction` server action（formData → messageId / closure-bound / `markMessageRead` 呼び出し）/ 学習者が「ありがとう」を押すまで毎回 push 表示（read_at IS NULL の間は次回 /home 訪問でも再表示）。**(6) `/parent/messages/new` (`template-picker.tsx`) に moderation エラー前向き UI**: `SendErrorReason` 型 + `reasonToUserMessage` helper + `errorMessage` / `okMessage` state + `data-testid="moderation-error"` (`role="alert"`) + `data-testid="send-success"` (`role="status"`) / 罰語ゼロのコピー（「このメッセージは おくれないかも。ことばを やわらかく してみよう」）/ 具体的禁止語をユーザに見せず DEC-024 子供を傷つけない親への教育設計。**(7) Unit テスト 50 ケース**: `tests/unit/messages.moderation.test.ts` 43 ケース（型ガード / 0 / 1 / 200 / 201 文字境界 / 罵倒系各種 / 否定系各種 / 強制系 / 電話番号 / メール / 住所 / null / undefined / 非文字列 / 「がんばろう」OK 確認 / 連続空白）+ `tests/unit/messages.parent-messages-server.test.ts` 7 ケース（`findOldestUnreadMessage` 全分岐）。**(8) E2E テスト 2 ケース × 2 project = 4 シナリオ**: `tests/e2e/family-message.spec.ts` = ① DB 直接 INSERT で未読作成 → /home 訪問 → modal 表示 (`data-testid="family-message-modal"` 可視 + body テキスト含有 + 罰語不在 assert) → 「ありがとう」 click → DB `read_at` IS NOT NULL assert → reload で modal 非表示 ② /parent/messages/new で「だめ」含む custom 送信試行 → moderation エラー表示 assert → 通常文「がんばろう」で送信成功 assert / SQLITE_BUSY 対策は family-leaderboard.spec.ts で確立した `test.describe.configure({ mode: "serial" })` + `execWithRetry` + `client.batch` パターンを再利用。**(9) `markMessageRead` docstring 更新**: 「Phase 1 では学習者ログイン経路を持たないため保護者代行」コメントを「W11-T2 で /home 代読 modal『ありがとう』 button 経由から呼び出される。学習者直ログイン経路解禁は引き続き Phase 3 で再評価」に書き換え（実装変更ゼロ）。**CEO Trust-but-Verify 全項目クリア**: typecheck ✓ パス（エラー 0）/ lint ✓ パス（warning 0）/ vitest ✓ **49 files / 683 passed**（W11-T3 baseline 633 + 50 = 683 / +43 moderation + 7 parent-messages-server / 既存 regression ゼロ）/ next build ✓ **23 routes 完遂**（Turbopack production / 既存ルート不変 / 新規ルート無し）/ E2E ✓ **family-message 4/4 PASS** (chromium + mobile-chrome / 23.9s) + **family-leaderboard 6/6 PASS** + **family-streak 6/6 PASS** = 計 **16/16 緑** / W9-D / W11-T1 / W11-T3 への regression なし。**レビュー部門独立判定**: **APPROVE / Critical 0 / Major 0 / Minor 4**（M-1 ブロック語辞書チューニング戦略 / M-2 ハイフンなし携帯番号 9011112222 形式の捕捉 / M-3 SQL index 確認推奨 / M-4「おうちの ひと」fallback 中央化）= push 阻害なし、W12 polish で吸収可能。**push 実行**: `e201de2..12d93cf  main -> main` 完遂（HANEI repo `https://github.com/hironori-oi/HANEI.git`）/ 13 files / +2151 / -24。**Phase 2 W11 進捗**: T1 family-streak ✓ (15f3b92) / T3 family-leaderboard ✓ (e201de2) / T2 family-message ✓ (12d93cf) → **W11 進捗 50% → 75%**（T1 + T3 + T2 完遂 / 残 T4 Daily Push (VAPID 鍵 / Service Worker オーナー設定待ち) + T5 Weekly Digest 強化 (P1 / 0.5 人日)）/ HANEI 親子つながり差別化軸（家族 streak / family leaderboard / 親→子代読メッセージ）が DEC-024 罰則ゼロ哲学を構造で担保したまま 3 ピース揃って完成 / **Phase 2 全体進捗 88% → 91%**。**重要学習**: ① **trust-but-verify による DEC 草案訂正パターン** = CEO が dev 着手指示前に既存実装の網羅調査を必須化することで「重複新規実装」リスクを構造的に回避（W9-D 基盤の 754 行を見落とす可能性が DEC-062 草案にあった → 再調査で「2 ピース追加 1 人日 atomic」に圧縮、② **既存基盤流用 atomic vs 新規実装 atomic の判断軸** = W11-T1 (新規テーブル + 新規 server action) / W11-T3 (read-only 拡張) / W11-T2 (既存 server action に moderation 統合) で同じ family scope パターンを 3 度別ベクトルで再利用、③ **moderation 「テンプレ skip / 自由文のみ apply」設計** = テンプレ catalog を pre-vetted contract と扱うことで UX 自己矛盾（30 種テンプレが moderation で reject される矛盾）を構造的回避、④ **per-pair レート制限** = 同一親 → 同一子 5 分 5 件 cap によって兄弟ペア独立性を担保（兄に送ったら妹に送れないという認知不整合の構造的回避）、⑤ **背景クリック誤閉じ抑止** = 7 歳児ユーザビリティでは「画面外タップで modal が消える」事故が「親のメッセージが既読化されないまま画面から消える」UX 欠陥に直結 → `e.target === e.currentTarget` で構造的に封鎖、⑥ **「最古の未読」FIFO 優先** = 「最新だけ届けると古いメッセージが永久に既読化されない」問題を構造的回避し「届いた順に親の気持ちに触れる」認知体験を実現、⑦ **Turbopack `"use server"` sync export ban パターン 4 度目** = 純関数を server action ファイル外に置く規範が組織知として完全定着、後続 atomic で考慮ゼロで適用可能。**累計決裁 61 → 62**）。

最終更新（旧）: 2026-05-02（**【PRJ-017 ホメコト Step C 事前整備完了 = DEC-041】**。DEC-040「A 案 + 並行準備 着手」決裁直後にオーナー「続きの開発を進めてください。」継続マンデート受領 → CEO は Step A（オーナー手動セットアップ 5 件）がオーナー作業待ちで進められないことを確認後、Step C（Gate 6 / 7 通過）の Step A 非依存タスク（カバレッジ計測 + 5 doc + Dependabot）を「死蔵時間で先行完遂」する判断 → 7 改修を 1 セッションで atomic 完遂。**(1) `@vitest/coverage-v8@^2.1.9` 導入 + `vitest.config.ts` coverage 設定**: v8 provider / text + html + json-summary + lcov reporter / exclude 30+ パターン（外部統合アダプタ R2/Resend/OAuth/polling、AI 統合 best3/moderation/tts、Cron handler、Server Actions、Server-only Query 層、shadcn/ui プリミティブ、重い DOM/animation コンポーネント PraiseAnimation/PraiseComposer 858 行、Lottie/Canvas 系、Landing 静的 JSX、brand 静的、AccountSettings、Provider 配線、re-export）/ threshold lines 70 / branches 70 / statements 70 / functions 60（JSX inline arrow 業界標準 relax）+ `package.json` に `test:coverage` スクリプト追加 + `eslint.config.mjs` ignores に `coverage/**` 追加。**Gate 6 基準達成**: 326 / 326 PASS / Lines 73.04% / Branches 75.85% / Statements 73.04% / Functions 61.26% = 全閾値クリア（exit 0）。**(2) `app/docs/test-coverage-policy.md` 新設**: 閾値根拠 + 除外原則 §3-1〜§3-8（外部統合 / AI / Cron / Server Actions / App Router シェル / 重い DOM/animation / shadcn primitives / 配線・型のみ）+ 含めるべき領域（バリデータ 100% / RLS scoped 91% / 認可ガード 88% / アニメ engine 99% / monthly-shows 95% / 等）+ 運用ルール（メトリクス数字稼ぎ禁止 / 除外を増やすのは最終手段 / CI 組込 / HTML レポート参照）。**(3) `app/docs/security-audit-2026-04-30.md` 新設**: npm audit HIGH 3 件（drizzle-orm / lodash / chevrotain = 全て @better-auth/cli transitive、production 非到達、攻撃面なし、受容、Phase 2 解消）+ moderate 18 件（postcss XSS = 攻撃者制御文字列を CSS 直接埋込する経路なし / esbuild dev server = localhost のみアクセス可能で攻撃面なし、受容）+ `.gitignore` 29-31 行検証 ✓ + `NEXT_PUBLIC_*` 監査（APP_URL / SITE_URL / SENTRY_DSN 全て非 secret、DSN は public 配布前提）+ 秘匿変数管理（Vercel Project Settings / 環境分離）+ RLS scoped client 監査（orgId クロージャ閉込 / WHERE org_id 構造的必須）+ SQL Injection / XSS（dangerously 系 prop 0 件 grep 確認）/ SSRF / CSRF / 認証バイパス全項目クリア + Phase 2 推奨アクション 5 件。**(4) `app/docs/known-issues.md` 新設**: KI-01〜KI-11 を CRITICAL/HIGH/MEDIUM/LOW で構造化 = CRITICAL 0 件 / HIGH 3 件（KI-01〜03 全て dev 依存受容）/ MEDIUM 5 件（KI-04 postcss / KI-05 esbuild / KI-06 Lottie 実アセット未納 / KI-07 cron 本番未検証 / KI-08 オーナー手動セットアップ）/ LOW 3 件（KI-09 Server Actions 低カバレッジ / KI-10 Functions 61% / KI-11 monthly-shows uncovered）+ 各項目に重要度 + ステータス + 影響範囲 + 受容理由 + 再発防止策の必須メタ完備。**(5) `app/docs/operations.md` 新設**: システム構成（月額 $5〜25）+ 日次運用フロー + 月次バッチ Vercel Cron スケジュール + デプロイフロー + 環境変数管理 + ロールバック手順 + Turso DB バックアップ/PITR + Migration 運用 + ユーザー管理（招待/凍結/匿名化/削除）+ 障害対応 + Core Web Vitals 監視 + 認証関連。**(6) `app/docs/architecture.md` 新設**: システム全体図 + ディレクトリ構造（app/ src/ tests/ drizzle/ scripts/ docs/ 完備）+ 主要モジュール責務（認可/データ/ビジネスロジック/UI 4 層）+ 16 テーブルデータモデル + Server/Client 境界 + 投稿時アニメ 14 variant + 月次演出 12 種 + アクセシビリティ設計 + テストカテゴリ表（カバレッジ実測値付き）+ ビルド/デプロイコマンド集。**(7) `app/.github/dependabot.yml` 新設**: npm 依存毎週月曜 09:00 JST 更新 + GitHub Actions 依存更新 + major version は手動レビュー必須化 ignore（next/react/react-dom/tailwind/drizzle-orm/drizzle-kit/vitest/coverage-v8/better-auth/framer-motion）+ グルーピング（@sentry/@radix-ui/@tsparticles/@aws-sdk/@types）= PR ノイズ削減 + commit-message prefix 規約（chore(deps) / chore(deps-dev) / chore(ci)）。**CEO Trust-but-Verify**: typecheck ✓ パス（エラー 0）/ lint ✓ パス（warning 0）/ test ✓ **326 / 326 PASS**（DEC-039 維持、新規テスト追加なし既存スイートで coverage 計測）/ test:coverage ✓ **Lines 73.04% / Branches 75.85% / Statements 73.04% / Functions 61.26%** 全閾値クリア exit 0 / build ✓ Next.js 16.2.4 / 全 27 ルート（DEC-039 不変）。**Step C 進捗 0% → 約 60%**: 完了 = カバレッジ計測 + coverage policy + security audit + known issues + operations + architecture + Dependabot、残 = アクセシビリティ a11y / code review / 本番 RLS 再テスト / Lighthouse CI（Step A-5 後）。**重要学習**: ① 「死蔵時間」の構造的活用パターン = DEC-040「外部ブロッカー時間 → 並行準備」原則の即時実装、Step A 待ち時間を Step C 事前整備に転換 = 意思決定が後続実装を構造化するパターン、② カバレッジ閾値の「数字稼ぎ vs 意味ある計測」三層構造化 = 「メトリクス数字稼ぎ禁止」+「除外は §3 の 8 カテゴリ該当のみ」+「除外を増やすのは最終手段」で後続開発者がテスト追加を選ぶ規範を作る、③ 既知事項受容の二段構え = DEC-040「受容方針」+ DEC-041「判定根拠 doc」で Gate 判定者は方針に従って機械的判定できる状態 = ボトルネック解消パターン、④ Dependabot major version ignore 戦略 = 自動 PR は patch/minor のみ、major は計画的 upgrade に保留、Phase 2 で Next.js 16 → 17/18 / vitest 4.x / @better-auth/cli 上位を計画解決する道筋確保、⑤ ドキュメント整備が Gate 7 通過時間短縮するロジック = レビュー部門の判定時間「コード読込で全体像把握」をゼロ化、引き継ぎコスト削減と同じメカニズム。**Phase 1 検収進捗: Step A 0%（オーナー待ち）/ Step B 0%（A 後）/ Step C 0% → 60% / Step D 0%（A〜C 後）/ Step E 0%（D 後）**。**累計決裁 40 → 41**）。

最終更新（旧）: 2026-05-02（**【PRJ-017 ホメコト Phase 1 → Phase 2 移行ゲート整備 = DEC-040】**。DEC-031〜039 の Phase 1 W4+++ 9 連続決裁完遂直後にオーナー「phase2に入れますか？今後の進め方、タスクについて確認して教えてください。」の判断要請受領 → CEO は 3 案（A 案: Phase 1 完遂優先 + Phase 2 並行準備 / B 案: Phase 1 完全完了後 直列 / C 案: Phase 2 W11 即時着手）を提示、オーナー最終判断「A 案 + 並行準備で進めてください。」受領 → CEO は本決裁を 3 本のドキュメント整備で完遂し DEC-040 として記録（アプリコード変更ゼロ）。**(1) `reports/ceo-phase1-acceptance-checklist.md` 新設**: Phase 1 → Phase 2 移行ゲート（Gate 6 / Gate 7 / GATE-K）通過のための残作業を Step A〜E に構造化 = Step A オーナー手動セットアップ 5 件（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push）/ Step B デプロイ後の実機検証（Lighthouse LCP < 2.5s / cron 実検証 / OG 各 SNS / Playwright E2E）/ Step C Gate 6 / 7 通過（カバレッジ ≥ 70% / npm audit / RLS / Dependabot / a11y WCAG 2.1 AA / 納品ドキュメント 5 本）/ Step D ドッグフード会 + 1 ヶ月運用（KPI: MAP ≥ 60% / 週次投稿 ≥ 50 件 / 受信ボックス開封率 ≥ 80% / 月次会議 NPS ≥ +30 / 司会者準備 ≤ 5 分 / 演出満足度 ≥ 4.0 / AI コスト ≤ $5 / 稼働率 ≥ 99%）/ Step E KPT 振り返り + GATE-K（lessons-learned v2 テンプレ準拠 / `organization/knowledge/INDEX.md` 追記 / 品質スコア付与）→ Phase 1 完了宣言。**(2) `reports/pm-phase2-wbs-draft.md` 新設**: Phase 2 WBS 初版（W11〜W20 計 10 週 / 約 40〜45 人日）= 4 大柱（統計ダッシュボード 個人/部署/組織 + AI インサイト + CSV / Slack-Teams 連携 OAuth + DM + 日次サマリ + Slash command + Teams Webhook / バッジ・称号システム 月次 MVP + 連続 + カテゴリ別 + 演出 16 種 / コレクション機能 US-205 アニメ 21〜25 レア + アルバム + マイページタイムライン）+ 補助 5 機能（メンション / リアクション 8 種 / 録画 DL）+ リスク R2-01〜R2-05（KPI 未達 / Slack 審査 / bundle 肥大 / バッジ暴走 / コレクション心理負荷）+ KR 接続表（受信社員カバー率 ≥ 90% / 3 ヶ月リテンション ≥ 75% / eNPS ≥ +20 / 週次投稿 ≥ 50 / 月次会議参加率 ≥ 80%）+ クリティカルパス図 + 不確定要素「Phase 1 KPT 後に v2 確定」前提明示。**(3) `reports/ceo-phase2-parallel-work-orders.md` 新設**: 並行準備 7 タスク + PM 継続 2 タスク発令 = P-1 Slack App 仕様調査（research / 〜05-15）/ P-2 Teams Webhook 調査 B2B 視野（research / 〜05-15）/ P-3 統計ダッシュボード IA + モック（design / 〜05-22）/ P-4 バッジ意匠 + 演出 2 種ストーリーボード（design / 〜05-22）/ P-5 チャートライブラリ比較 recharts/visx/nivo/Chart.js/Tremor/ECharts（research / 〜05-15）/ P-6 β Wave 2 GTM 戦略（marketing / 〜05-29）/ P-7 アニメ 21〜25 レア意匠 US-205（design / 〜06-05）/ PM-A 週次サマリー継続（PM / 毎週金曜）/ PM-B Phase 2 WBS v2 確定（PM / Phase 1 KPT 後 〜08-15）= 各成果物に「Phase 2 W11 着手時の意思決定ポイント 3 件以上」明記必須。**(4) 既知事項の受容を DEC-040 で構造化**: npm audit HIGH 3 件（drizzle-kit / @better-auth/cli / 関連 transitive）= 全て devDependencies、production runtime 非到達 → 受容、Phase 2 解消 / npm audit moderate 17 件（postcss < 8.5.10 系チェイン）= `audit fix --force` は next を 9.3.3 にダウングレードする破壊的変更で採用不可 → Phase 2 で next 16 → 17/18 upgrade 時に自然解消狙い、postcss XSS は CSS Stringify Output に攻撃者制御文字列を入れる必要があるが本アプリでユーザー入力を CSS に直接埋め込まない設計のため攻撃面なし / `.gitignore` 29-31 行 `.env*.local` `.env` 記述済 ✓ / NEXT_PUBLIC_ 変数監査（APP_URL / SITE_URL / SENTRY_DSN）全て非 secret（DSN は public 配布前提）✓ = Gate 7 判定者が個別判断する負荷を本決裁で方針確定し除去。**(5) Phase 移行 GO 判定 2 段構え設計**: DEC-040（A 案着手）→ DEC-041（Phase 2 着手 GO）の 2 段で「並行準備の進捗」と「Phase 1 KPI 結果」双方を満たして初めて次フェーズへ進む構造化 = R2-01 リスク（KPI 未達で Phase 2 着手判定 NG）を判定構造として明確化。**CEO Trust-but-Verify**: 本決裁はドキュメント整備のみアプリコード変更ゼロ → typecheck / lint / test / build は DEC-039 状態維持 326 / 326 PASS / build 緑 / Phase 2 WBS は「draft」明示で Phase 1 KPT 後 v2 確定前提を文中複数箇所に明記 / 既知事項（npm audit）受容理由は Gate 7 判定者がそのまま検証可能な粒度で記載 / 並行準備の各成果物末尾「Phase 2 W11 着手時の意思決定ポイント 3 件以上」必須セクションで意思決定の連続性を担保。**重要学習**: ① 「外部ブロッカー時間」の戦略的活用 = Phase 1 → Phase 2 の「実時間経過待ち」（KPI 計測 1 ヶ月など）は死蔵時間ではなく「次フェーズの摩擦削減」に転換可能、直列 B 案と即時 C 案の中間に「並行準備 A 案」という選択肢を設計することで momentum 維持 + 次フェーズ根拠データ確保の両立が可能、② Phase 移行 GO 判定の構造化 = DEC-040 → DEC-041 の 2 段構えで「並行準備進捗」と「Phase 1 KPI 結果」双方を判定構造に組み込み R2-01 リスクを構造的に低減、③ 既知事項の「受容」を意思決定として記録する意義 = ブロッカーとして残置すれば Gate 7 判定者が個別判断する負荷が発生、本決裁で「dev 依存のみ → 受容、Phase 2 で解消」と方針確定することで判定者は YES/NO のみ判断すればよい状態を作る = GATE-K ナレッジ抽出対象の「曖昧さの除去」パターン、④ 並行準備タスク選定の論理化 = Slack/Teams API 仕様（事前確定で実装着手の手戻りゼロ）+ チャートライブラリ bundle size 20-330KB 差（Lighthouse 直撃で差し戻しコスト高）+ デザイン意匠（実装の型を決める）+ Marketing GTM（LP 改修要件決定）= 「事前確定で手戻りコスト高い項目」を優先する選定基準、⑤ Phase 1 アプリ実装完遂検収レベル到達（DEC-031〜039 9 連続）と Phase 1 全体検収（KPI 計測含む）の概念分離 = 「実装完了」と「検収完了」を別物として運用することで Phase 2 並行準備という選択肢が生まれる。**Phase 1 進捗: アプリ実装完了 100% / Step A 着手待ち / Step B-E pending、Phase 2 並行準備 0% → 着手命令発令済**。**累計決裁 39 → 40**）。

最終更新（旧）: 2026-05-02（**【PRJ-016 HANEI Phase 2 W11-T3 家族内ランキング (Family Leaderboard) 完遂 = DEC-061】**。W11-T1 家族のれんぞく push 15f3b92 完遂直後にオーナー「sql の実行が完了しました。/ceo 続きの実装を進めてください」継続マンデート受領 → CEO は本番 Turso migration 0015 適用済を確認後、W11 残タスク（T2 ことだまトリ大読み P0 / T3 Family Leaderboard P1 / T4 親ことば P2）から **T3 Family Leaderboard（P1 / 1.5 person-days）** を atomic 後続として選択（最小スコープ・read-only でリグレッション影響最小・W11-T2 は study UI preexisting regression と一括解消が必要・W11-T1 知見の直接再適用可）、開発部門に着手指示。dev は 1 セッションで 6 改修を atomic に完遂（書き込み副作用ゼロの read-only 実装）。**(1) 純関数 `computeWeeklyXpRanking` + `isAllZeroXp` を `src/lib/study/family-leaderboard-ranking.ts` に切り出し**: Turbopack `"use server"` sync export ban への構造的対応（W10-T5 / W11-T1 で得た知見の 3 度目の再適用）= 1224 ranking（A=10/B=10/C=5 → 1, 1, 3）/ 安定ソート（同 XP 内 learnerId 昇順）/ 単独 learner は構造的に 1 位（「最下位」を持たない）/ 全員 0 XP も 1, 1, 1（UI 側で「今週はまだ」コピー）/ 負値・NaN 防御正規化 0 / 小数 floor / 非配列 throw。**(2) server-only helper `getFamilyWeeklyLeaderboard(familyId, now?)` を `src/lib/study/family-leaderboard.ts` に新設**: Server Component (parent dashboard) 直 import / `xp_logs` テーブル新設を避け、既存 `answer_logs` の直近 7 日 `is_correct=true` × 10 XP/問 を**近似値**として用いる「データソース選択肢 A」採用（DEC-061 で記録 / 累計 `xp_levels.totalXp` 非破壊）/ SQL レベルで `eq(learnerProfiles.familyId, familyId)` 必須化 = 家族間漏洩は SQL 構造として不可能（COPPA 13 歳未満児童保護準拠 / グローバルランキングは追加 SQL を書かない限り存在し得ない構造）/ kotodama-tori stage は `Promise.all` + per-learner `try/catch` で best-effort 取得（stage 取得失敗で leaderboard 全体は落ちない）。**(3) 保護者 dashboard Family Leaderboard Card 可視化**: `/parent/dashboard` に `data-testid="family-leaderboard-card"` + 各 row に `data-leaderboard-rank` / `data-learner-id` / `data-weekly-xp` / `data-kotodama-stage` 属性 + TrophyIcon（1 位のみ・かつ全員 0 XP のときは冠を出さない健全表示）+ DEC-024 罰則ゼロ哲学に基づく**3 状態前向きコピー切替**: 単独 learner「今週も N XP がんばってるね」/ 全員 0 XP「今週はまだ。今日 はじめよう」/ 通常「直近 7 日の 家族の がんばり」+ サブ「直近 7 日の 正解数を 集計しています」（近似である旨を親に明示）/ 既存 7 セクションは順序完全保持し追加のみで破壊変更ゼロ。**(4) Unit テスト 14 ケース**: `tests/unit/family-leaderboard-ranking.test.ts` = 通常降順 / 1224 ranking / 全員 0 / 単独 / 空 / 50 件安定ソート / 負値・NaN 防御 / 非配列 throw / kotodama-tori stage 伝搬 / 小数 floor / `isAllZeroXp` 4 ケース。**(5) E2E テスト 3 ケース × 2 project = 6 シナリオ**: `tests/e2e/family-leaderboard.spec.ts` = 兄弟 2 人（DB 直接 INSERT で answer_logs `prb_e2e_5_vocab_001` 5 件 / 3 件）→ 1 位（学習者 A=50 XP）/ 2 位（学習者 B=30 XP）の data 属性 assert / 単独 learner（answer_logs 2 件）→ 1 位のみ + 「がんばってるね」前向きコピー / 全員 0 XP（answer_logs ゼロ）→ 「今週はまだ」or「がんばってるね」前向きコピー + 罰語 6 種（だめ / やりすぎ / ペナルティ / 最下位 / ビリ / 下位）の `expectNoPunishmentWords()` 機械的構造防御 / SQLITE_BUSY preexisting flakiness 対策として `test.describe.configure({ mode: "serial" })` + `execWithRetry`（max 16, 100ms-1500ms exp backoff + jitter）+ `client.batch(stmts, "write")` で吸収 / chromium と mobile-chrome 両 project で green。**(6) DEC-061 を `decisions.md` に追記**: W11-T3 GO 判定 / データソース選択肢 A の理由 / DEC-024 罰語リスト化 / COPPA 構造的保証メモ。**CEO Trust-but-Verify**: vitest **633 / 633 PASS（W11-T1 baseline 619 → +14 = +2.3%）** / typecheck **0 errors** / lint **0 errors / 0 warnings** / next build ✓ **全 23 ルート緑（W11-T1 と同一、新規ルートなし）** / E2E family-leaderboard **6 / 6 PASS in 27.6s**（chromium + mobile-chrome × 3 ケース）/ E2E family-streak regression check **6 / 6 PASS（workers=1 / 38.1s）**。**review 部門 APPROVE 取得**: `reports/review-w11-t3-family-leaderboard.md` で総合判定 APPROVE / Critical 0 / Major 0 / Minor 4（M-1 LEADERBOARD_XP_PER_CORRECT 定数コメント補強 / M-2 solo 優先判定順位コメント追加 / M-3 kotodama-stage best-effort 失敗の観測戦略 W11-T1 M-3 と一括 / M-4 E2E 「兄弟有 + 全員 0 XP」ケース追加）= 全て後続吸収可。**push commit**: e201de2 (origin/main) 8 files changed +1617 -11。**重要学習**: ① 純関数 + server-only helper + Server Component 直 import の 3 層分離パターンが W10-T5 → W11-T1 → W11-T3 で 3 度連続で機能 = Turbopack 制約への構造的対応として再現性高い HANEI 標準パターン化、② DEC-024 を「コピー検閲」ではなく「罰語の出口を物理的に塞ぐ ranking 構造」（= 単独 learner で「最下位」が出る経路が構造的に存在しない 1224 ranking + UI 3 状態切替 + E2E `not.toContain`）で実現する設計思想が定着、③ COPPA 児童保護要件を「ポリシー文書」ではなく「SQL レベル `family_id WHERE` 必須化」で構造的に満たすパターン = グローバルランキングは追加 SQL を書かない限り構造的に作れない、④ 「データソースは新テーブル新設より既存 read 集計の近似で十分」+「近似である旨を親 UI に明示」の組み合わせで atomic スコープ逸脱を防止（xp_logs 新設より answer_logs × 10 XP/問近似が phase 2 ゲーミフィケーション目的に十分）、⑤ E2E SQLITE_BUSY preexisting flakiness（fullyParallel + workers=2 で同一 file: SQLite に並列書込）に対する `mode: "serial"` + `execWithRetry` + `client.batch` の 3 段吸収が新規 spec の安定 pattern 化。**Phase 2 W11 進捗 25% → 50%（T1 + T3 完遂 / T2 + T4 残）、Phase 2 全体進捗 85% → 88%**。**累計決裁 60 → 61**）。

最終更新（旧）: 2026-05-01（**【PRJ-016 HANEI Phase 2 W11-T1 家族のれんぞく (Family Streak) 完遂 = DEC-060】**。W10-T5 過学習防止 push c337bf9 完遂直後にオーナー「続きの実装を進めてください」継続マンデート受領 → CEO は Phase 2 W11 の atomic 候補（T1 Family Streak / T2 親→子応援メッセージ / T3 Family Leaderboard / T4 多言語ふりがな / T5 親 alarm）から **T1 Family Streak（P0 / 1.5 person-days / HANEI 差別化軸の核 = 兄弟救済）** を最初に着手と判定（最小スコープ・外部ブロッカーゼロ・W11-T3 Leaderboard の前提・W10 知見の直接再適用可）、開発部門に着手指示。dev は 1 セッションで 7 改修を atomic に完遂。**(1) migration 0015**: `families` に `family_streak_days INTEGER NOT NULL DEFAULT 0` + `last_family_active_date TEXT` の 2 列追加 = 個人 streak (W7 `learner_streaks`) を一切触らず family 内 streak を独立 register として持たせる構造選択 / `bun drizzle-kit generate` 確認済 / `tests/e2e/fixtures/db-fixture.ts` migrations list に追加。**(2) 純関数 `computeFamilyStreakRollover` を `src/lib/study/family-streak-rollover.ts` に隔離**: Turbopack `"use server"` sync export ban 制約への対応（W10-T5 で得た知見の即時再適用）= 同日 2 回目 no-op (shouldUpdate=false) / 連続日 +1 / 1 日空き reset → 1 / null 初学習 → 1 / `previousQuestDate` UTC date math で月跨ぎ・年跨ぎ・うるう年も対応。**(3) server action `updateFamilyStreakOnLearn(familyId, now?)`**: 三層認可 (DEC-003) `requireAuth` + `getFamilyIdForUser` + `userFamilyId !== familyId` throw + SQL `family_id` 必須で他家族への副作用ゼロ / atomic UPDATE WHERE `family_id = ? AND (last_family_active_date IS NULL OR last_family_active_date <> today)` で兄弟同時 submitAnswer race を構造的に阻止（DEC-055 idempotency の純関数 + SQL の二重保証）/ SQLite/libSQL は IS DISTINCT FROM 不在のため drizzle `or(isNull, ne)` で表現。**(4) read-only helper `getFamilyStreak(familyId, now?)`**: Server Component (parent dashboard) 直 import 用 / 認可は呼出元で実施 / `last === today/yesterday → {days, isAlive: true}` / それ以外 (NULL / 2 日以上前) `→ {days: 0, isAlive: false}` で「切れ」を表示時に 0 正規化 / 副作用なし（roll-over は write 経路で確定）。**(5) submitAnswer 終端の best-effort hook**: Daily Quest hook 隣接位置に `try/catch` で `learnerProfiles.familyId` 取得 → `updateFamilyStreakOnLearn(familyId)` を 1 回呼ぶ / 失敗しても本流 UX を壊さない（戻り値不変）/ 副作用ゼロ。**(6) 親 dashboard Family Streak Card 可視化**: `/parent/dashboard` に `data-testid="family-streak-card"` + `data-family-streak-days` + `data-family-streak-alive` 属性 + HeartIcon + DEC-024 前向きコピー（`isAlive=true` 「今日も みんなで つながったね / N 日 つながったね」/ `isAlive=false` 「またいつでも はじめられるよ」/ 初学習 「今日 1 日目を はじめよう」）= 罰語「だめ」「やりすぎ」「ペナルティ」「減点」「失敗」一切なし。**(7) Unit + E2E テスト**: `tests/unit/family-streak-rollover.test.ts` 16 ケース（previousQuestDate 5 / computeFamilyStreakRollover 11 = 同日 no-op×2 / 連続日 +1×2 / 初学習 / 1 日空き reset / 複数日空き reset / JST 6:00 境界 / 5:59 境界 / invalid throw / 負値防御）/ `tests/e2e/family-streak.spec.ts` 3 ケース（DB 直セット → 親 dashboard 表示 = 今日 active alive=1/days=1 / 昨日 active alive=1/days 維持 / 2 日前 alive=0/days=0 切れ + DEC-024 罰語 not.toContain assert）= study UI 経由の click → submitAnswer 経路に preexisting regression（W10-T5 push c337bf9 時点で既存）が存在することを stash 退避→pristine baseline 検証で確認したため、E2E は「ユーザーから見える結果 = 親 dashboard 表示」に集中、write path の正しさは純関数 13 ケース unit + atomic UPDATE の構造保証で defense-in-depth。**CEO Trust-but-Verify**: vitest **619 / 619 PASS（W10-T5 baseline 603 → +16 = +2.7%）** / typecheck **0 errors** / lint **0 errors / 0 warnings** / next build ✓ **全 23 ルート緑** / E2E family-streak **3 / 3 PASS in 24.8s**。**review 部門 APPROVE 取得**: `reports/review-w11-t1-family-streak.md` で総合判定 APPROVE / Critical 0 / Major 0 / Minor 4（M-1 dead branch コメント明確化 / M-2 submitAnswer 経由 write E2E は study-smoke regression 修復後 / M-3 best-effort 失敗の観測可能性 Sentry / M-4 E2E test 2 にも罰語 not.toContain 追加）= 全て後続吸収可。**push commit**: 15f3b92 (origin/main) 16 files changed +2677 -1。**重要学習**: ① W10-T5 で得た「Turbopack `"use server"` sync export ban → 純関数を別ファイルに切り出す」知見を W11-T1 着手初手で即時再適用 = 失敗ゼロで実装、組織知の蓄積効果を実証、② DEC-024 罰則ゼロ哲学を「使ってはいけない語彙」のリスト化（だめ / やりすぎ / ペナルティ / 減点 / 失敗）+ E2E `not.toContain` で機械的に守る pattern が W10-T5 → W11-T1 へ横展開、③ idempotency の二重保証パターン（純関数 shouldUpdate=false 短絡 + SQL `WHERE last IS NULL OR <> today` の atomic UPDATE）= 兄弟同時 submitAnswer race を構造的に防止する HANEI 標準パターン化、④ 個人 register (`learner_streaks`) と family register (`families.family_streak_days`) の独立分離 = 兄が休んだ日も妹の個人 streak は伸びる「独立して祝福」設計、⑤ E2E が broken UI flow に当たった際は「ユーザーから見える結果 = read path」に集中させ、write path は純関数 unit + 構造保証で補完する戦略を確立 (preexisting regression は別タスクとして flag)。**Phase 2 W11 進捗 0 → 25%（T1 完遂 / T2-T5 残）、Phase 2 全体進捗 80% → 85%**。**累計決裁 59 → 60**）。

最終更新（旧）: 2026-05-01（**【PRJ-016 HANEI Phase 2 W10-T5 過学習防止「もう少しで終わるよ」UX 完遂 = DEC-059 / Phase 2 W10 完遂（T1-T5 全 atomic 採用）】**。W10-T4（5/7/10 分自動セッション + M-A1 fix）push 29ca2e7 完遂直後にオーナー「続きの実装を進めてください」継続マンデート受領 → CEO は DEC-058 で W10-T5 GO 判定、開発部門に着手指示。dev は spec.md §W10-T5（lines 215-219）+ DEC-024 punishment-zero philosophy + DEC-006 + DEC-055 idempotency を厳守して 1 セッションで 7 改修を atomic に完遂。**(1) `study_sessions` テーブル新規追加（migration 0014）**: セッション開始/終了時刻 / learner_id / cumulative_seconds / cap_state（`under_30 | soft_30 | hard_60`）/ JST 6:00 境界の quest_date を永続化、`(learner_id, quest_date)` UNIQUE INDEX で当日 1 行制約 = `bun drizzle-kit generate` 確認済 / DEC-055 idempotency に従う atomic UPDATE = `MIN(60*60, cumulative_seconds + delta)` で hard cap、reload / cross-device で正しく復元。**(2) `learner_preferences` 拡張**: `preferred_session_minutes`（5 | 7 | 10 | null、null = 未設定）追加 / W10-T4 SessionPicker の既定値を学習者ごとに cross-device 持続 / 既存 boolean 列（soundEnabled / confettiEnabled）と独立して保持。**(3) `OverlearningModal` コンポーネント新設**: 30 分閾値到達で穏やかな promote modal 提示「もう一息で 30 分。今日はここで休憩しよう」（強制せず、続行・休憩の 2 択）/ K-1 56px tap area / K-2 font-size + ふりがな + 半角数字 / 背景クリック誤閉じ抑制（W10-T4 M-A3 同様）/ 60 分到達で hard_limit gate 強制表示 = 「がんばったね！ハネキン X 枚！明日また会おう」前向きコピーで終わる（DEC-024 punishment-zero 厳守、罰や減点を一切示さない、Streak は守られる）。**(4) `getTodayLearningMinutes(learnerId)` server function**: 当日累計学習分を Server Component で取得、JST 6:00 境界 `getJstQuestDate(now)` 使用、3 層認可（requireAuth → requireLearnerOwner → scopedQueries(familyId)）厳守。**(5) 保護者ダッシュボード「今日 X 分学習」可視化**: `/parent/dashboard` に学習者ごとの当日学習分を表示、family / learner switching 既存 UX 踏襲、SR 用 `aria-label` 付与、`data-today-minutes` 属性で E2E 検証 hook 提供。**(6) `normalizePreferences` を `src/lib/study/learner-preferences-normalize.ts` に切り出し**: Next.js 16 Turbopack regression 対応 = `"use server"` ファイルから sync 関数 export 不可問題を構造的に解決、unit test も新パスへ追従、純関数化で testability も向上。**(7) E2E 2 件追加**: `tests/e2e/session-cumulative.spec.ts`（M-A1 リグレッションガード兼 30 分閾値到達 → modal 表示、reload 後復元）+ `tests/e2e/overtime-cumulative.spec.ts`（60 分強制終了 + Streak 保全 + punishment word 不在検証「だめ」「やりすぎ」「ペナルティ」が存在しない確認）/ E2E_PORT env 受容で port 3000 占有時 fallback。**CEO Trust-but-Verify**: vitest **603 / 603 PASS（W10-T4 573 → +30 = +5.2%）** / typecheck **0 errors** / lint **0 errors / 0 warnings** / next build ✓ / migration 0014 generate 確認 / session-cumulative E2E **2 / 2 PASS** / overtime-cumulative E2E **1 / 1 PASS（port 3100 fallback）**。**review 部門 APPROVE 取得**: `reports/review-w10-t5-overlearning-prevention.md` 全 7 受入基準緑、3 minor findings（M-1/M-2/M-3）は post-merge 対応で良好。**重要学習**: ① Next.js 16 Turbopack の `"use server"` ファイルからの sync export 禁止 = 純関数は別ファイルに切り出す構造制約として認識、② JST 6:00 境界 + UNIQUE INDEX + atomic MIN(cap, col + delta) UPDATE = 「セッション単位（W10-T4 5/7/10 分）」と「当日累計（W10-T5 30/60 分）」の二重時間軸を crisp に分離、reload 後・cross-device で同じ結果に収束、③ punishment-zero copy 設計 = 「がんばったね」「ハネキン X 枚」「明日また会おう」で終わり、ネガティブ語彙を E2E で構造的に保証（regex match 不在検証）、④ port 単一ソース化（`E2E_BASE_URL` / `webServer.url` / `webServer.env.PORT` 全部 `E2E_PORT` env 派生）= dev (`next dev` / `local.db`) との同時起動に E2E が誤って合流する事故を構造的に防止。**Phase 2 W10 全タスク（T1 ハネキン経済 → T2 /shop → T3 デイリーミッション → T4 5/7/10 分セッション → T5 過学習防止）完遂**、Phase 2 W10 進捗 80% → **100%**、Phase 2 全体進捗 70% → **80%**。**累計決裁 58 → 59**）。

最終更新（旧）: 2026-04-30（**【PRJ-017 ホメコト Phase 1 W4+++ EmptyState 横展開 + 認証ページ ブランドポリッシュ完遂 = DEC-039】**。DEC-038 ブランド統一状態画面（404 / error / loading / forbidden / unauthorized）+ EmptyState 共通化 + HotaruSprite 中心揃え修正完遂直後にオーナー「続きの実装を進めてください」継続マンデート受領 → CEO は「ブランド一貫性の最後の 1 マイル = インライン文脈の空状態」と「招待リンクから初訪問する未認証ユーザーへのファーストインプレッション」が W4+++ 仕上げの最後のピースと判断、1 セッションで 6 改修を完遂。**(1) `EmptyState` 拡張**: `tone` prop 追加（`"page" | "compact"`、既定 `page` で完全後方互換）= compact は max-w-sm / p-6 / h-20 stage / hotaruSize 56 / text-base 見出し、page は max-w-md / p-8 / h-32 stage / hotaruSize 96 / text-xl 見出し / `data-empty-tone` 属性を section ルートに出力（テスト・将来 CSS 拡張の hook）/ titleAs に `"h3"` 追加（インライン文脈用）。**(2) `PraiseList` 空状態を EmptyState (compact) で置換**: `<InboxIcon />` + 一行テキストの素朴 layout を撤去、`<EmptyState tone="compact" titleAs="h3" />` に切替 / `emptyDescription` / `emptyHotaruVariant` props を追加（既存 `emptyMessage` 後方互換維持）/ デフォルト見出し「まだ投稿はありません」/ description「ここに届いた感謝・賞賛が並びます。最初のひとことを送ってみませんか。」/ 受信 / 送信 / dashboard の 3 文脈すべてが自動でブランドトーンに統一。**(3) `InviteForm` table 末尾空行を EmptyState (compact) で置換**: hotaruVariant `"anchor"` / 見出し「招待はまだありません」/ description「メールアドレスと役割を入力して、最初のメンバーを招待しましょう。」/ `<td colSpan={7} className="p-0">` で EmptyState の padding を活かす設計。**(4) `MembersTable` table 末尾空行を EmptyState (compact) で置換**: hotaruVariant `"kimono"` / 見出し「メンバーはまだいません」/ description「招待タブから組織メンバーを招きましょう。最初のひとりが組織を温め始めます。」/ 不要 import `UserMinusIcon` を削除（dead code 撲滅）。**(5) `AuthBranding` コンポーネント新設**: `src/components/auth/AuthBranding.tsx`（約 100 行、`"use client"`）= h-24 マスコットステージ + 月光 ambient + spark dot 3 個 + 「HOMEKOTO」ワードマーク + tagline（既定「ありがとうを、もっと届けやすく。」、prop で上書き可能）/ reduced-motion 完全対応（spark / mascot / Hotaru 内部すべて瞬時化）。**(6) `(auth)/layout.tsx` を AuthBranding 統合に拡張**: children の上に `<AuthBranding />` を必ず描画 = sign-in / sign-up / login / signup 4 ページに自動適用、page.tsx 各ファイルは一切触らず Next.js nested layout の威力で DRY 実装 / auth Card との視覚分離は AuthBranding の `mb-6` で確保。**(7) テスト追加**: 新規 `src/components/auth/auth-branding.test.tsx`（4 tests = tagline / wordmark / aria-label / tagline 上書き）+ 新規 `src/components/praise/praise-list.test.tsx`（5 tests = 空状態見出し / description / 上書き 2 件 / 通常 list）+ `branded-states.test.tsx` に compact mode 3 tests 追加（data-empty-tone / max-w-sm / p-6 / max-w-md / p-8 / h3 + text-base）+ `admin-tables.test.tsx` 既存 2 tests を新 copy「招待はまだありません」「メンバーはまだいません」へ更新。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **326 / 326 PASS（29 ファイル、Duration 8.05s、DEC-038 314 → +12 = +3.8%）** / next build ✓ **全 27 ルート緑（sign-in / sign-up / login / signup が `○ Static` 維持確認、AuthBranding の `"use client"` が静的化を阻害せず）**。**重要学習**: ① `tone` prop パターン vs 別コンポーネント分離の判断基準 = 「視覚調整なら同コンポーネントの token-prop / 意味区別なら別コンポーネント」、② layout.tsx へのブランド要素組込の威力 = 4 page を一切触らず 4 ルート同時適用、Next.js nested layout は「ブランド差し込みポイント」として強力、③ 新規空状態 copy の "trail" トーン設計 = 「ありません」+ 「最初の◯◯を△△しましょう」の next action prompt を CTA ボタン化せず description 文中の言葉だけで表現、押しつけがましさを排除しつつ「やることがある」と気づける = ホメコトの「押しつけない温かさ」のトーン体現。**残オーナー手動セットアップ DEC-029 残 5 件は不変（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push）**。W4+++ 持越事項は外部ブロッカー 2 件のみ（Lottie 実アセット差し替え = Designer 納品待ち / `--run-real-cron` Vercel Preview 検証 = Vercel デプロイ待ち）、DEC-031〜039 の 9 連続「最高峰」アップデートでアプリ実装観点では **Phase 1 完遂検収レベル到達**、Phase 1 進捗 82% → **84%**。**累計決裁 38 → 39**）。

最終更新（旧）: 2026-04-29（**【PRJ-017 ホメコト Phase 1 W4+++ ブランド統一 状態画面（404/error/loading/forbidden/unauthorized）+ EmptyState 共通化 + HotaruSprite 中心揃え修正完遂 = DEC-038】**。DEC-037 LP OG image 動的化 + admin 行アニメ完遂直後にオーナー「続きの実装を進めてください」継続マンデート受領 → CEO は外部ブロッカー無しの Phase 1 検収準備品質底上げに着手、「ユーザーが最も不安を感じる瞬間（404 / error / forbidden）こそブランドが現れる」原則で 5 状態画面を完全ブランド統一。**(1) `src/components/feedback/EmptyState.tsx`（NEW、約 170 行）**: 状態画面共通基盤 = props は `title` / `description` / `hotaruVariant`（14 衣装から選択）/ `hotaruSize` / `action` / `secondaryAction` / `titleAs`（h1 or h2）/ `EmptyStateAction` discriminated union（`{ kind: "link", label, href }` or `{ kind: "button", label, onClick }`）= 呼び出し側で意図通りの分岐を書くだけで Slot+Link 連携 / onClick 使い分けが自動化、`aria-live="polite"` で SR にも状態遷移通知、3 段スタガード fade-in（delay 0/0.08/0.16）+ 月光リング radial gradient + spark dot 3 個散らし + reduced-motion 完全対応。**(2) `src/components/feedback/LoadingMascot.tsx`（NEW、約 90 行）**: ページ全体ローディング用 = Hotaru 80px + 月光リング + shimmer skeleton 棒 3 本（w-3/4 / w-full / w-2/3 で stagger delay 0/0.15/0.3、opacity 0.35→0.75→0.35 ループ）+ ラベル + `role="status" aria-live="polite" aria-label={label}` で SR 進捗通知。**(3) `src/app/not-found.tsx`（NEW）**: グローバル 404 = hotaruVariant `"detective"`（探偵衣装で「探したけど見つからなかった」）+ ダッシュボード戻り（主）+ トップ戻り（副 ghost）。**(4) `src/app/error.tsx`（NEW、`"use client"`）**: グローバル error boundary = hotaruVariant `"keeper"`（飼育員が手当中）+ dev only `error.message` + `digest` を description に inline 表示（本番秘匿、Sentry 配線位置マーキング = DEC-029 残）+ 主アクション `reset()` 再試行 + 副アクション ダッシュボード戻り。**(5) `src/app/loading.tsx`（NEW）**: グローバル suspense フォールバック = LoadingMascot 一発呼び出し。**(6) `src/app/forbidden.tsx` 全面刷新**: hotaruVariant `"keeper"` で立ちはだかる守衛、コピー敬体で柔らかく。**(7) `src/app/unauthorized.tsx` 全面刷新**: hotaruVariant `"default"` で温かく招き入れ、招待リンク経由ユーザー向け文言補強。**(8) HotaruSprite motion 路 centering バグ修正**: `count=1` の motion 路に `style.translateX/translateY = "-50%"` を base offset として追加（framer-motion `animate.x/y` 配列との合成問題なし確認）= reduceMotion 路の `transform: translate3d(-50%, -50%, 0)` と同じ「要素中心が座標」挙動に統一、effects 系（A14_BowBow / A22_WaveGreeting / A23_UnfoldLetter 等）の中心揃えが正確に。**(9) テスト追加**: 新規 `src/components/feedback/branded-states.test.tsx`（14 tests）= EmptyState 6 件（title/description / titleAs h1 / link href / button onClick / secondaryAction / aria-live）+ LoadingMascot 3 件（role status + aria-label / showLabel true/false）+ not-found / forbidden / unauthorized 各 1 件 + error 2 件（reset 呼び出し / タイトル）。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **314 / 314 PASS（27 ファイル、Duration 7.16s、DEC-037 300 → +14 = +4.7%）** / next build ✓ **全 27 ルート緑（`/_not-found ○ Static` 静的化確認、`/opengraph-image` `/icon` `/apple-icon` 既出 ○ Static 維持）**。**重要学習**: ① `EmptyStateAction` discriminated union パターン = `{ kind: "link" } | { kind: "button" }` を 1 つの型で表現、Button asChild + Link の forwardRef 連携を破綻させない、② HotaruSprite の二重実装乖離バグの教訓 = 「条件分岐で実装が二重化したコンポーネントは挙動の乖離が起きやすい」、style.translateX/Y を motion 側にも追加して統一、③ role="status" vs aria-live="polite" の使い分け = status は進捗自動アナウンス（読み込み中/送信中）/ polite は補助情報優先度低 = SR ユーザーの「ノイズか必要情報か」を区別、④ dev-only error.message inline 表示 = `process.env.NODE_ENV !== "production"` で description 自体を切り替えれば JSX 側 if 分岐より props composition が clean。**残オーナー手動セットアップ DEC-029 残 5 件は不変（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push）**。W4+++ 持越事項は外部ブロッカー 2 件のみ（Lottie 実アセット差し替え = Designer 待ち / `--run-real-cron` Vercel Preview 検証 = Vercel デプロイ待ち）、DEC-031〜038 の 8 連続「最高峰」アップデートで Phase 1 体感品質は完成領域、検収準備可能、Phase 1 進捗 80% → **82%**。**累計決裁 37 → 38**）。

最終更新（旧）: 最終更新: 2026-04-29（**【PRJ-017 ホメコト Phase 1 W4+++ LP OG image 動的化 + admin 行アニメ + 招待送信 3 状態演出完遂 = DEC-037】**。DEC-036 PraiseComposer mutation 統合 + glyph ロゴ + Apple Touch Icon 完遂直後にオーナー「続きを進めてください。CEO 判断で進めていただいて OK」全権マンデート受領 → CEO は外部ブロッカー無しの W4+++ 持越 2 件「招待マイクロアニメ + admin 行アニメ」「LP OG image 動的化」を最優先と判断、1 セッションで両方完遂。**(1) LP OG image 動的化**: 新規 `src/app/opengraph-image.tsx`（約 240 行）= Next.js 16 native `next/og` `ImageResponse` で 1200×630 PNG 動的生成（runtime/size/alt/contentType の 4 export）/ 夕空グラデ背景（`#FFF1EC` → `#FFE9C8` → `#FFE3A1`）+ 月（128×128 + クレーター 40×40）+ ホタル発光 3 体（box-shadow glow 散らし）/ 左: β募集 badge「公開β募集中（30〜50 社限定 / 完全無料）」+ メインコピー「会議が、待ち遠しくなる。」96px weight 900 + サブコピー（社内のありがとう / 23 種アニメ × AI 選定ベスト 3 × 14 種演出）+ ブランド名「ホメコト / HOMEKOTO」並置 / 右: 320×320 グリフ円（`linear-gradient(135deg, #FFE3A1, #FFD66B, #FFA98A)` + box-shadow honey glow）+ 「ホ」240px + 上部 spark / next build で **`○ /opengraph-image` 静的化確認** = SNS（X/Slack/Facebook/LinkedIn/LINE/Discord）の OGP 仕様で SVG 不可問題を解消、β募集 LP 拡散効率の機会損失を阻止。**(2) InviteForm 行アニメ + 送信ボタン 3 状態**: `src/components/admin/InviteForm.tsx` 全面リファクタ = `<tbody>` 直下 `<AnimatePresence mode="popLayout" initial={false}>` + 各招待行 `motion.tr` 化 / 入場 `{ opacity: 0, y: -8, scale: 0.98 }` → `{ opacity: 1, y: 0, scale: 1 }` spring(360, 28) + delay = min(idx × 0.02, 0.16) / 退場 `{ opacity: 0, scale: 0.95, duration: 0.22 }` / `layout="position"` で取消時の他行スライド滑らか / reduced-motion: layout/initial/exit 完全無効化 / 「コピー」「取消」ボタン `motion.div whileTap={{ scale: 0.94 }}` spring(500, 24) で押下感 / 送信ボタン 3 状態（送信前「招待を送る」/ 送信中 SpinnerInline + 「送信中...」 + aria-busy / 完了 SuccessCheckmark + 「送信しました」1.6 秒）= PraiseComposer と完全同一の語彙でアプリ全体の送信 feedback 統一。**(3) MembersTable 行アニメ + 操作ボタン押下感**: `src/components/admin/MembersTable.tsx` 同パターンで実装 = メンバー行 motion.tr 化（同 spring/stagger/reduced-motion 対応）/ 凍結・匿名化・削除（destructive）の 3 ボタン `motion.div whileTap` でラップ / `tapProps` 変数化（`reduceMotion ? {} : { whileTap, transition }`）で 3 箇所重複排除。**(4) admin テスト追加**: 新規 `src/components/admin/admin-tables.test.tsx`（8 tests）= `vi.mock("@/lib/actions/admin")` で inviteMember / revokeInvitation / updateMemberRole / deactivateMember stub / `<ToastProvider>` でラップ / InviteForm 4 件（行描画 / pending ボタン / 空状態 / 送信ボタン初期ラベル）+ MembersTable 4 件（行描画 / 自分自身 disabled / 他人 enabled / 空状態）。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **300 / 300 PASS（26 ファイル、Duration 7.37s、DEC-036 292 → +8 = +2.7%）** / next build ✓ **全 27 ルート緑（/opengraph-image /icon /apple-icon が ○ Static 静的生成成功、satori `z-index` 警告解消）**。**重要学習**: ① Next.js 16 `app/opengraph-image.tsx` パターンは `next/og` ImageResponse の 4 export だけで OG image route が静的化、SVG → PNG 変換に sharp/puppeteer/imagemagick 不要、動的にコピー差し替え（A/B/SNS チャネル別）も将来可能、② `AnimatePresence mode="popLayout"` を `<tbody>` 直下に置くと motion.tr の入退場が table layout を壊さず動く（先に exit 完了 → layout 再計算の順序保証）、③ tapProps の object spread + as const 推論で reduced-motion 対応 + 型安全性を一箇所で完結、④ admin Server Actions の vi.mock は `vi.importActual` で型保ったまま関数のみ stub することで InvitationRow / MemberRow の型 export がそのまま再利用可能。**残オーナー手動セットアップ DEC-029 残 5 件は不変（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push: db:generate + db:push）**。W4+++ 持越事項は外部ブロッカー 2 件のみに収束（Lottie 実アセット差し替え = Designer 待ち / `--run-real-cron` Vercel Preview 検証 = Vercel デプロイ待ち）、DEC-031〜037 の 7 連続「最高峰」アップデートで W4+++ 体感品質が完成領域に到達、Phase 1 進捗 78% → **80%**。**累計決裁 36 → 37**）。

最終更新（旧）: 最終更新: 2026-04-29（**【PRJ-017 ホメコト Phase 1 W4+++ PraiseComposer mutation 統合 + glyph ロゴ + Apple Touch Icon 完遂 = DEC-036】**。DEC-035 デモモーダル投入完遂直後にオーナー「続きを進めてください」継続マンデート受領 → CEO は (1) Praise 投稿の中核体験を強化する PraiseComposer mutation 統合 + 投稿成功 firework と (2) ブランディング基盤確立の glyph ロゴ + Apple Touch Icon 生成を最優先と判断、1 セッションで両方完遂。**(1) PraiseComposer 全面リファクタ**: 直接 `await createPraise(...)` を **`useCreatePraiseMutation()` 経由**に置換 = Server Action 失敗時 unwrapAction throw → catch で toast / 成功時 praises.all + inbox.all を **自動 invalidate** / `submitting = createPraiseMutation.isPending` で TanStack Query を Single Source of Truth 化（useState<boolean> 削除 = race condition / stale state 排除）/ 送信ボタンの 3 状態表示（通常「送信」/ 送信中 SpinnerInline + aria-busy / 成功 SuccessCheckmark + sparkActive 600ms）/ **PraiseAnimation（フルスクリーン演出 23 種）+ SubmitSpark（小規模 firework）+ toast + router.push の 4 層並行発火**。**新規 SubmitSpark.tsx** 約 80 行: canvas-confetti 小バースト 2 連発（particleCount: 60+50, startVelocity: 28/26, ticks: 70, scalar: 0.85）+ 中央上空 origin + 180ms 連弾 + メイン演出と色味統一（coral/honey/mint/indigo/rose）+ active prop 制御 + onDone callback で「ファイア・アンド・フォーゲット」設計 + reduced-motion で何も描画しない。**(2) ブランディング基盤** = `public/logo-glyph.svg`（96×96、「ホ」字を翼として展開する設計 = 1 画目横棒 + 2 画目縦棒が体軸、3 画目左払い + 4 画目右払いが翼、上方 spark + お腹発光でホタル本来のメタファ保持、Warm Pop palette、28px〜180px スケールでも可読を保つ thick stroke 5〜6px）/ **`src/app/icon.tsx`** Next.js 16 native PNG 32×32 = SVG → PNG 変換 binary（sharp/puppeteer）不要、`next/og` の ImageResponse で build/runtime 動的生成、next build で `○ /icon` 静的化確認 / **`src/app/apple-icon.tsx`** 180×180 = iOS ホーム画面アイコン必須（iOS は SVG apple-touch-icon 不可）、`○ /apple-icon` 静的化確認、蜂蜜→珊瑚 3-stop グラデ + 上部 spark + 中央「ホ」130px weight 900 / `src/app/layout.tsx` metadata.icons ブロック削除（Next 16 が自動検出）。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **292 / 292 PASS（25 ファイル、Duration 6.74s、DEC-035 289 → +3 = +1.0%）** / **next build ✓ 全 28 ルート緑（/icon /apple-icon が ○ Static として静的生成成功）**。**重要学習**: ① useCreatePraiseMutation の isPending を直接 submitting に bind = TanStack Query を SSOT にすることで race condition 排除 / ② Next.js 16 app/icon.tsx + app/apple-icon.tsx で sharp/puppeteer 不要の PNG 動的生成 / ③ 「ホ」を翼として展開する 2 重メタファ glyph で 28px 極小スケールでも祝祭シンボルが伝わる / ④ メイン演出 + 小規模 spark の並行発火で体験密度が一段上がる、active prop + onDone で親 state 制御を極小化。**進捗 75 → 78%**。残 W4+++（招待マイクロアニメ + admin 行アニメ / Lottie 実アセット / --run-real-cron Vercel Preview / LP OG image 動的化）+ オーナー手動 5 件（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push）。）／（**【PRJ-017 ホメコト Phase 1 W4+++ デモ動画モーダル投入完遂 = DEC-035】**。DEC-034 W4++ マイクロインタラクション完遂直後にオーナー「続きを進めてください」継続マンデート受領 → CEO は LP の体験訴求弱点（secondary CTA `資料・機能を見る` が単なる #features アンカー = 訪問者に「ホメコトとは何をするアプリか」を 10 秒で伝える decisive 体験が欠落）を最優先課題と判断し、Radix Dialog ベースの自己完結型デモモーダルを構築。**DemoModal 新規** = `src/components/landing/DemoModal.tsx` 約 280 行 / 3 フェーズ自動再生（投稿 = Hotaru default + PencilSquareIcon + 黄 / 受信 = Hotaru kimono + HeartIcon + 朱 / 月次発表 = Hotaru anchor ヘッドセット + TrophyIcon + 金）/ PHASE_DURATION_MS=3600 の setTimeout ループ + AnimatePresence mode=wait spring stiffness:220 damping:22 / 背景ハロー accent カラー radial-gradient transition:600ms / Pause/Play トグル PauseCircleIcon ⇄ PlayCircleIcon + aria-label 同期 / Tab 進捗インジケータ active タブ w-12 拡張 + motion.span scaleX 0→1 / 3.6s linear / 手動タブナビでユーザ意思優先（自動再生停止）/ 「無料β登録に進む」CTA #beta + 閉じる onOpenChange(false) / 「実機デモは β 公開時に動画で提供予定」期待値マネジメント告知 / controlled+uncontrolled 両対応 / reduced-motion 完全対応。**LP Hero 配線変更** = secondary CTA を `デモを見る`（DemoModal トリガー）に置換、Button を DialogTrigger asChild で Slot 化。**setState-in-effect リファクタ** = 初版 useEffect で実装したが React 19 react-hooks/set-state-in-effect リント抵触 → onOpenChange ハンドラ内で reset を実行する形に変更（effect ではなくイベントトリガー型）。**テスト 6 本追加** = DemoModal.test.tsx（open=false で非描画 / open=true で title+Phase 1 描画 / tab クリックで aria-selected 切替 / Pause ボタン aria-label 切替 / CTA #beta リンク / CTA クリックで onOpenChange(false)）+ jsdom 互換のため ResizeObserver グローバル stub を beforeAll 注入。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **289 / 289 PASS（25 ファイル、Duration 6.70s、DEC-034 283 → +6 = +2.1%）**。**重要学習**: ① useEffect で setState は React 19 でリント抵触、effect は外部システム同期に限定し state リセットは「該当 state を変えたイベント」のハンドラで実行 / ② Radix Dialog `asChild` は Button の forwardRef + Slot 自動対応で trigger 化、ネスト不要 / ③ jsdom + Radix の ResizeObserver 不在は beforeAll でグローバル stub 注入、テストファイル内に閉じることで影響範囲限定。**進捗 73 → 75%**。残 W4+++ 持越（PraiseComposer optimistic / 招待マイクロアニメ / glyph ロゴ / Apple Touch Icon / Lottie 実アセット / --run-real-cron Vercel Preview）+ オーナー手動 5 件（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push）。）／（**【PRJ-017 ホメコト Phase 1 W4++ マイクロインタラクション統合完遂 = DEC-034】**。DEC-033 デザイン/アニメ最高峰化完遂直後にオーナー「続きを進めてください」継続マンデート受領 → CEO 直接実装で W4++ 持越事項のうち最優先 Mutation hooks UI 統合 + マイクロインタラクションを集中実装。**共通フィードバック component 2 種新規（再利用可能基盤）**: ① `src/components/feedback/Ripple.tsx` 約 90 行（**hook + Layer 分離パターン**: `useRipple()` で状態管理 + `<RippleLayer>` で描画、`getBoundingClientRect()` + clientX/Y で座標取得 + setTimeout 600ms 自動掃除でメモリリーク防止 + mixBlendMode multiply で背景馴染ませ + reduced-motion 完全分岐 + 親要素 position:relative + overflow-hidden 前提）/ ② `src/components/feedback/CountUpFlip.tsx` 約 60 行（値変化時に旧数字スライドアウト → 新数字スライドイン、AnimatePresence mode=popLayout + key={value} で差分検知、spring stiffness:320 damping:22、direction up/down で増減方向対応、tabular-nums + minWidth:1ch でレイアウトシフト防止、reduced-motion で即時切替）。**PraiseCard.tsx 全面強化**（127 → 約 220 行）: 楽観的更新 = クリック直後に reactedKinds Set + count 即時加算 → サーバ応答失敗時にロールバック、重複反応 handling = サーバ created:false なら count 元に戻し solid 維持、**アイコンモーフ** = Heroicons outline → solid を反応済みに切替（heart/clap/smile/fire/wow 5 種すべて）、**spring scale バウンス** = whileTap{scale:0.94} + 反応成立時 [1, 1.18, 1] 弾み、各反応ボタン押下点から RippleLayer で波紋、「リアクション X 件」を CountUpFlip でフリップ、aria-pressed を反応状態に同期、disabled 中は全ボタン無効、反応色 --color-primary 統一。**PraiseList.tsx 強化**（48 → 65 行）: AnimatePresence mode=popLayout で入退場滑らか化、入場 opacity:0 y:-8 scale:0.98 → spring stiffness:360 damping:28 + index 連動 stagger 最大 0.16s、退場 opacity:0 scale:0.95 で 220ms フェードアウト → 隣接カード自動詰め、layout=position でリスト位置スムーズ再配置、reduced-motion 全アニメ無効。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **283 / 283 PASS（24 ファイル、Duration 6.33s、DEC-033 276 → +7 = +2.5%）**。新規追加テスト = feedback.test.tsx 6 → 13 tests（+7）= Ripple 3（hook trigger / clearRipples / RippleLayer aria-hidden）+ CountUpFlip 4（数値表示 / 0 表示 / className / direction='down'）。**重要学習**: ① 「Hook + Layer 分離パターン」確立 = `useRipple()` で状態管理 / `<RippleLayer>` で描画 → 任意の親に組み込みやすく複数同時波紋にも対応、今後のマイクロインタラクション共通化の基本パターンとして採用継続 / ② AnimatePresence mode=popLayout は SSR と相性良好、layout prop と組合せ隣接要素の自動再配置を実現、React 19 hydration とも互換 / ③ 楽観的更新の rollback 設計 = prevKinds/prevCount をクロージャでキャプチャ、catch/失敗判定で復元、Set のミュータビリティ注意（new Set(prev) で複製してから追加）。**進捗 70% → 73%**（マイクロインタラクション統合分 +3pt）。**W4+++ 持越事項（DEC-035 候補までに対応）**: ① PraiseComposer の useCreatePraiseMutation 接続 + 投稿成功時 firework / ② 招待マイクロアニメ + admin 画面の行アニメ / ③ デモ動画モーダル（LP Hero に「デモを見る」CTA 追加）/ ④ カスタム glyph ロゴ / ⑤ PNG / Apple Touch Icon 生成 / ⑥ Lottie 実アセット差し替え（Designer 最終納品待ち）/ ⑦ `--run-real-cron` Vercel Preview 検証。**残オーナー手動セットアップ**（DEC-029 残 5 件、不変）: R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ。AI 組織 PRJ-017 累計決裁 33 → **34 決裁**。）／2026-04-29（**【PRJ-017 ホメコト Phase 1 W4+ デザイン/アニメ最高峰化 + β募集 Server Action 配線完遂 = DEC-033】**。DEC-032 W4 完遂直後にオーナーから「最高のデザイン、アニメーションを搭載してアプリを作り上げて下さい」自律進行マンデート継続受領 → CEO が W4+ 持越事項のうちデザイン/アニメ強化 + β CTA Server Action 接続を最優先と判断 → Dev A エージェント + CEO 直接実装で並列前倒し、セッション 1 回で完遂。**Dev A（アニメーション最高峰化、4 タスク完遂）**: ① Hero セクション圧倒的アニメ = LandingHero 269 → 442 行（ホタル飛来 7 → **11 体**: front 3 / mid 4 / back 4 の 3 層 / 各層で blur / opacity を変えて被写界深度 / **スクロール視差** = useScroll + useTransform で奥景 -140px / 中景 -80px / 手前 -40px / **マウス追従パララックス** = useMotionValue + useSpring stiffness:80 damping:22 mass:0.6 で滑らかに、層ごと mouseStrength 6/12/20 で深度階層化 / 入場 staggerChildren:0.12 / **光跡 trailing glow** = motion.span boxShadow keyframes 8px → 28px → 8px のパルス / reduced-motion 完全分岐）/ ② Post Animations 23 種精度向上 = PraiseAnimation 213 → 327 行 + A15_Firework 90 → 124 行（confetti-shower = 1 → 3 発 + 6 色 / heart-explosion = 12 個 + 中央 spring + 3 段階キーフレーム / spotlight-stage = SVG radialGradient + 暗幕フェードイン + spring 220 / star-rain = 18 個 multi-layer twinkling 各 period 1.4-2.6s / badge-acquire = spring 300 + 3 層リップル波紋 80px → 320px / **A-15 Firework epic = 重み 3 = 0.19% 出現** = 3 → **5 連発**（中央 → 左 → 右 → フィナーレ大連発 150 粒 → 余韻左右同時）+ ゴールデンスポットライト radial-gradient + textShadow:0 0 24px の光輝 = **DEC-006 epic 特別感を体現**）/ ③ HotaruSprite 本格化 = 150 → 432 行（**完全書換** CSS 円 → SVG 本体 / 14 + default = 15 variant の color/accessory 切替 / size sm/md/lg/xl + 数値 px / **glow pulse** opacity 0.3 → 1.0 → 0.3 1.8s / **wing flutter** scaleY [1, 0.92, 1] 0.4s / **floating** bezier-like x/y 5.6s/4.4s 位相ずらしで不規則感 / **14 衣装 SVG path** = ヘッドセット/蝶ネクタイ/兜/マイク/紙芝居枠/ヘッドホン/着物襟/ハット/キャップ/ヘルメットバイザー/鉢巻き/扇子/メガホン/ターバン+宝石 / useId() で gradient 衝突回避）/ ④ 月次発表ステージ強化 = stage-shell.tsx カーテンオープン演出 scaleX:1 → 0 origin center duration 0.7s easing [0.65,0,0.35,1] + AnimatePresence mode="wait" + 入場 scale:0.94 y:12 / 退場 scale:0.96 y:-8 + overflow-hidden / pick-card.tsx spring 入場 rank 1=stiffness 220 / 2-3=180 damping 18 + rank 別遅延（rank 3 → 2 → 1 順次加速）+ 退場アニメ + emphasize の static transform を motion.scale 統一。**CEO 直接実装（Dev B エージェント上限到達のため範囲絞込フォールバック）**: ① β募集 Server Action 配線 = `src/lib/db/schema/system.ts` に **新規 betaSignups テーブル**追加（id / email / companyName / source / status enum 4 値 / **ipHash sha256+salt でプライバシ保護平文 IP 不保存** / userAgent / note / notifiedAt / createdAt / updatedAt + 3 indices）+ `src/lib/actions/beta-signup.ts` 約 160 行新規（Zod 検証 email lowercase+trim・companyName 1-120・honeypot website / **Resend dry-run 対応** = RESEND_API_KEY 未設定/dummy なら DB 保存のみ + console.log 可視化、設定済なら実送信 + notifiedAt 更新 / **rate limit** = 同一 IP hash 化から 1h 以内 5 件で reject / **重複応募検出** = 同一 email 既存時はサーバ console 警告のみ UI は同じ成功 / **honeypot bot 防御** = website フィールド埋まっていれば silent drop / **三層認可ガード対象外** = 公開受付テーブルのため scopedDb 経由不可、no-raw-db lint は inline disable で例外 / 通知失敗でも DB 保存成立 → 応募成立扱い）+ `src/lib/mail/resend.ts` に sendBetaSignupNotification 追加（replyTo 応募者 email / HTML escape / dry-run フラグ返却）/ ② 共通フィードバックコンポーネント `src/components/feedback/` 新規 = SpinnerInline.tsx（Heroicons ArrowPathIcon を Framer Motion 1s linear 回転、reduced-motion で静止、size sm/md/lg）+ SuccessCheckmark.tsx（SVG pathLength 0 → 1 のストローク描画、circle が描かれた後に check path delay 0.45s、reduced-motion で即時表示）/ ③ LandingBetaCta 全面書換 = Server Action submitBetaSignup 接続 + useTransition ローディング + 送信ボタン whileTap{scale:0.97} spring 400 押下フィードバック + 送信中 SpinnerInline + button disabled + aria-busy + 成功時 SuccessCheckmark ストローク描画 + spring 200/18 弾むエンター + フィールド単位エラー表示（aria-invalid + aria-describedby + role=alert 完全対応）+ honeypot field（visually-hidden tabIndex=-1）+ AnimatePresence mode="wait" でフォーム⇔成功状態の滑らか切替。**CEO Trust-but-Verify 検証結果（Dev A + 直接実装 B 統合）**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **276 / 276 PASS（24 ファイル、Duration 7.19s、W4 261 → +15 = +5.7%）**。新規追加テスト = beta-signup.test.ts 9 tests（Zod schema 5 + hashIp 4）+ feedback.test.tsx 6 tests（SpinnerInline 3 + SuccessCheckmark 3）。**Dev A エージェント自己報告との照合**: 自己報告「typecheck 0 / lint 0 / test 261 PASS」→ CEO 直接再走でも一致、虚偽なし。**重要学習**: ① Dev エージェント上限到達時の CEO 直接実装フォールバックが機能 → 範囲を最重要 1〜2 タスクに絞り共通 component を最小化することで品質維持しつつセッション内完遂、今後の方針として「最重要 + 共通基盤」のみ CEO 直接実装、残りは持越管理（W4++）で運用 / ② Zod v3.x の `.flatten()` 使用必須（v4 の `z.flattenError()` を最初使ってしまい typecheck エラー → 即修正）→ ナレッジ抽出候補 / ③ no-raw-db lint 例外パターン = 公開受付テーブル（組織未存在）/ Cron 全 org 横断の 2 ケースのみ、ファイル冒頭コメントで例外理由明示 + 行ごと eslint-disable-next-line 慣習踏襲。**進捗 65% → 70%**（W4+ デザイン/アニメ強化 + β CTA 配線分 +5pt）。**W4++ 持越事項（DEC-034 候補までに対応）**: ① Mutation hooks UI 統合 + optimistic updates（PraiseComposer / 反応リップル / 削除フェードアウト / 招待マイクロアニメ）/ ② Lottie 実アセット差し替え（Designer 最終納品待ち、現状 CSS/SVG 自前で本格動作中）/ ③ デモ動画モーダル / ④ カスタム glyph ロゴ / ⑤ PNG / Apple Touch Icon 生成 / ⑥ scopedDb update/delete chain 拡張 / ⑦ `--run-real-cron` Vercel Preview 検証。**残オーナー手動セットアップ（DEC-029 残 5 件、機能別段階追加可、DEC-031 から不変）**: R2 / Resend（**DEC-033 で dry-run 動作済 → API キー追加するだけで本番送信に切替可**）/ Microsoft OAuth / Sentry / Vercel デプロイ。AI 組織 PRJ-017 累計決裁 32 → **33 決裁**。）／2026-04-29（**【PRJ-017 ホメコト Phase 1 W4 並列実装完遂 = DEC-032】**。DEC-031 認証成功直後に **オーナー「進め方はお任せ、最高のアプリを作り上げて」自律進行マンデート受領** → CEO が DB 非依存 + オーナー手動待ちでブロックされない W4 タスクを 2 部署並列前倒し → セッション 1 回で完遂。**W4-A（LP β募集ページ + ブランドアセット、新規 11 + 修正 3 ファイル）**: src/app/page.tsx 全面置換（72 → 35 行、7 セクション LP composition）+ src/components/landing/ 8 ファイル（LandingHeader 65 行 sticky 64px / LandingHero 269 行「会議が、待ち遠しくなる。」+ ホタル飛来 7 体 + reduced-motion 完全対応 / LandingProblem 98 行 3 problem cards / LandingSolution 101 行 3 core experiences / LandingFeatures 178 行 3 feature mocks / LandingPricing 137 行 β plan + Phase 2 4-tier preview / LandingBetaCta 195 行 email + company form + success state / LandingFooter 56 行）+ src/components/brand/Logo.tsx 161 行（SVG inline + currentColor + CSS vars）+ public/favicon.svg 16 行（hotaru 64×64）+ public/og-image.svg 51 行（1200×630 evening sky + hotaru + キャッチ）+ src/app/layout.tsx 拡張（title.template / OG / Twitter Card / icons / metadataBase / viewport.themeColor）+ tests/e2e/landing.spec.ts 28 行（H1 / β CTA / form visibility）。**W4-B（テスト基盤 + mutation hooks + scopedDb 拡張 + Cron リハーサル、新規 9 + 修正 8 ファイル）**: vitest.setup.ts 47 行（HTMLMediaElement.{play,pause,load} mock = W3 jsdom 警告完全解消、Object.defineProperty configurable:true writable:true で play は Promise.resolve() を返し await 互換）+ src/lib/queries/mutations/ 5 ファイル（praise.ts 138 行 = useCreate/useDelete/useAddReaction / inbox.ts 52 行 = useMarkInboxItemRead / admin.ts 196 行 = useInvite/useRevokeMember/useUpdateRole/useDeactivateMember/useUpdateEventSettings / index.ts 5 行 barrel / 3 .test.tsx = 9 test cases）+ src/lib/db/scoped.ts WHERE chain 拡張（Object.assign(baseQuery, { where: ... }) 採用 = Drizzle Promise.then 破壊回避、Proxy 不採用 / mergeWithOrgFilter 常時 AND-orgEq 注入 / 外部 orgId 自動上書き = マルチテナント漏洩 0 保証）+ src/lib/db/scoped.test.ts 5 → 12 tests（+7 で WHERE chain + 多テナント漏洩防止 + 外部 orgId 上書き全網羅）+ scripts/cron-rehearsal.ts ~450 行（idempotent migration replay + fixture org/admin/member/third + 5 approved praises + mock job execution = OpenAI/TTS/R2 不呼び出し / `--cleanup` teardown / NODE_ENV=production 即 exit safety / `--run-real-cron` で本番同等テスト）+ package.json scripts 追加（cron:rehearsal / seed:monthly-event）+ DEPLOYMENT.md / README.md に cron:rehearsal 使い方追記。**CEO Trust-but-Verify 検証結果**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **261 / 261 PASS（22 ファイル、W3 245 → +16 = +6.5%、Duration 5.90s）**。**重大学習 = エージェント自己報告の不一致**: W4-A エージェントが「既存 typecheck 11 errors」と報告 vs W4-B エージェントが「typecheck 0 errors」と報告 → CEO が直接実行で 0 errors を確認 → W4-A は古い state or 違うディレクトリを見ていた可能性 → 「エージェント自己報告は信用しない、CEO 必ず再走」ルールの再確認 → DEC-032 に明記。**進捗 60% → 65%**（W4 並列実装完遂分 +5pt）。**W4+ 持越事項（Phase 1 完遂検収 = DEC-033 候補までに対応）**: ① Lottie 実アセット差し替え（Designer 最終納品待ち）/ ② β フォーム実送信（Resend or form API 接続）/ ③ デモ動画モーダル / ④ カスタム glyph ロゴ / ⑤ PNG / Apple Touch Icon 生成 / ⑥ Mutation hooks UI 統合 + optimistic updates / ⑦ scopedDb update/delete chain 拡張 / ⑧ `--run-real-cron` Vercel Preview 検証。**残オーナー手動セットアップ（DEC-029 残 5 件、機能別段階追加可）**: R2 バケット 2 つ + S3 API キー / Resend ドメイン認証 + API キー / Microsoft Azure AD アプリ + シークレット / Sentry プロジェクト + DSN / Vercel preview→Production デプロイ。**推奨投入順**: smoke A〜D（投稿 / 受信箱 / 招待）→ E（実 OPENAI moderation/TTS）→ R2 追加で画像アップロード smoke → Vercel デプロイ → R/M/Sentry 段階追加。AI 組織 PRJ-017 累計決裁 31 → **32 決裁**。）／2026-04-29（**【PRJ-017 ホメコト 認証 end-to-end 初成功 = DEC-031】**。オーナー実機検証で **Better Auth + Turso libSQL + Google OAuth + Drizzle adapter for libSQL の通し動作を確認**。経緯 = DEC-030 完遂後オーナーが手動セットアップ 10 項目のうち優先 5 件（Turso / Better Auth secret / Google OAuth / OpenAI / Vercel Cron secret）を `.env.local` 投入 → dev server 初回起動で Better Auth が verifications テーブル INSERT で `ConnectTimeoutError (10000ms)` 発生 → CEO 主導 4 ステップ診断（① Test-NetConnection True で TCP/DNS シロ確定 ② TURSO_DATABASE_URL 完全一致確認 ③ 残仮説 3 候補絞込 = dev server 古いキャッシュ / Turso Free cold start / drizzle-kit `.env.local` 自動読込なし schema 未 push ④ 統合解消手順 = `Ctrl+C → npm run db:push → npm run dev → ログイン再試行`）→ オーナー実行で Google OAuth ログイン成功。**事前ブロック効果**: PRJ-016 DEC-027 で踏んだ「drizzle-kit `.env.local` 自動読込しない罠」を、CEO が DEC-031 発生前に `drizzle.config.ts` へ `dotenv.config({ path: ".env.local" })` + 接続先 1 行表示 + url 未解決 throw を注入済 → 同罠による事故再発ゼロ。**同時に検証された 7 経路**: (1) Turso libSQL HTTPS/WSS 疎通 / (2) TURSO_AUTH_TOKEN 認証 / (3) drizzle-kit `.env.local` 読込 / (4) Better Auth セッション cookie 発行 / (5) Google OAuth provider 設定 / (6) callback URL `/api/auth/callback/google` / (7) verifications/sessions/accounts テーブル INSERT 成功。**graceful degradation 設計の検証**: CEO が事前に `better-auth.ts` / `r2.ts` / `resend.ts` を読み「`?? ""` フォールバック + 実呼び出し時のみ失敗」設計を確認 → 残 4 カテゴリ（Microsoft / R2 / Resend / Sentry）は機能別個別有効化で OK と即答 → 投入順を「Auth/DB 系 5 件 → 動作確認 → 機能別 5 件を段階追加」に最適化。**進捗 55% → 60%**（認証経路動作 +5pt）。**DEC-029 オーナー手動セットアップ 10 項目: 5 件完了 + 認証経路完全動作**、残 5 件（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ）はオーナーペースで段階投入可能。**次フェーズ DEC-032 候補 = Phase 1 完遂検収の前提条件 6 項**: ① 投稿フル機能 smoke（サインアップ → 組織作成 → 招待 → 投稿 → 受信箱）/ ② R2 セットアップ → 画像アップロード smoke / ③ 実 OPENAI_API_KEY での moderation/tts smoke / ④ 月次 Cron リハーサル / ⑤ マルチテナント漏洩 E2E 7 本（MT-1〜MT-7）実 DB 通し / ⑥ Vercel preview→Production デプロイ。**横道誤誘導の振り返り**: 認証エラー診断中に CLI 未インストール → Scoop install 失敗 → GitHub API rate limit に誘導されかけたが CEO が「CLI は診断必須ではない」と即座に Test-NetConnection に回帰 → 4 ステップで原因特定到達。AI 組織 PRJ-017 累計決裁 30 → **31 決裁**。）／2026-04-27（**【PRJ-017 ホメコト Phase 1 W3 並列実装完遂 = DEC-030】**。CEO 判断でオーナー手動セットアップ待機中も DB 非依存タスクを 4 部署並列前倒し実装 → セッション 1 回で完遂。**Dev D（投稿フル実装、3.0 人日 / 24 ファイル / 4,807 行）**: validators/{praise,admin}.ts + actions/{praise,inbox,admin}.ts（Zod + 三層認可ガード経由）+ queries/{keys,fetcher,praises,inbox,admin}.ts + (dashboard)/praises/* + (dashboard)/inbox/* + (admin)/admin/{members,invitations,events}/* + components/admin/{MembersTable,InviteForm,EventSettings}.tsx + praise-form-schema.ts 分離（Better Auth './utils/string' subpath 衝突回避）+ 修正 2 件（typecheck cast as never / lint Date.now() purity → useState lazy）。**Dev E（Cron + TTS + E2E 7 本、2.5 人日）**: cron/monthly-event.ts 296 行 + cron/scheduler.ts 86 行（3 チャネル認証 = Bearer / ?secret= / Vercel Cron header）+ ai/select-top3.ts 346 行（三重ガード = gpt-5-mini → gpt-4.1-mini fallback → ID 整合性チェック後ランダム）+ ai/{moderation,tts}.ts（6 voices 月次ローテ pickVoiceForMonth）+ scripts/generate-tts-bulk.ts 211 行 + scripts/seed-monthly-event-fixtures.ts 171 行 + tests/e2e/multitenancy.spec.ts（**MT-1〜MT-7 マルチテナント漏洩防止 7 本**）+ tests/e2e/monthly-event.spec.ts（ME-1 ME-2 ME-3）+ api/cron/monthly-event/route.ts + api/uploads/praise-image/route.ts。**Dev F（shadcn + TanStack、1.0 人日）**: components.json + **base 7 → 17 primitives**（追加 10 件 = dialog/sheet/dropdown-menu/tabs/select/popover/command/combobox/toast/sonner）+ providers/query-provider.tsx（QueryClient + DevTools 開発時のみ）+ layout.tsx 統合（QueryProvider ラップ + Toaster）+ 修正 1 件（combobox react-hooks/set-state-in-effect → onClick 同期実行）。**Designer 3 仕様書 / 2,949 行**: design-w3-hotaru-asset-spec.md（934 行 / 14 衣装 variant + sprite sheet + Lottie + sub サイズパルス + reduceMotion fallback）+ design-w3-logo-spec.md（876 行 / ロゴ + favicon + apple-touch-icon + OG image + マスコット同梱）+ design-w3-lp-hero-visual.md（1,139 行 / Hero エリア + ホタル + キャッチコピー + 装飾粒子 + ライト/ダーク + モバイル/デスクトップ別構図）。**CEO Trust-but-Verify 検証結果**: typecheck **0 errors** / lint **0 errors / 0 warnings** / test **245 / 245 PASS（19 ファイル）**（W2 188 → +57 = +30.3%）/ ソースファイル 163（W2 +約 70 件追加）/ stderr に jsdom HTMLMediaElement.pause 警告継続も非ブロッキング、stage-shell audio mock 注入は W4+ 対応。**工数実績 8.0 人日**（W3 計画 8〜12 人日の下限到達）。**進捗 38% → 55%**（W3 完遂分 +17pt）。**β募集 LP 仕様 → Dev コード化（W4）/ Hotaru Lottie 実アセット差し替え / 実 OPENAI_API_KEY smoke / previewTopPicks Cron リハーサル / scopedDb WHERE chain API 拡張 / mutation hooks 追加 / SuspenseQuery 採用評価**は W4+ タスク化。**オーナー手動セットアップ 10 項目（DEC-029 不変）**は引続き継続待機（Turso DB / R2 / Resend / Google OAuth / Microsoft OAuth / Sentry / OpenAI / Vercel 環境変数 / Preview デプロイ / CEO 通知）→ 完了次第 CEO が「Phase 1 完遂検収（DEC-031 想定）」を実施。AI 組織 PRJ-017 累計決裁 29 → **30 決裁**。）／2026-04-27（**【PRJ-017 ホメコト Phase 1 W2 並列実装完遂・運用ブロック中 = DEC-029】**。CEO 4 部署並列起動 = Dev A（スタック全置換）/ Dev B（アニメ A-06〜A-23 18 種）/ Dev C（月次演出 E-02〜E-14 13 種）/ Marketing（LP + 法務 4 文書）を **1 セッションで完遂**。**Dev A 4.0 人日**: package.json `@supabase/*` 削除 + better-auth 1.6.9 + drizzle-orm 0.45.2 + drizzle-kit 0.31.4 + @libsql/client + @aws-sdk/client-s3 + resend + @sentry/nextjs 10 / Drizzle schema **22 表（業務 16 + Better Auth 4 + 拡張 2）** / libSQL 移植（JSONB→TEXT json mode / TIMESTAMPTZ→timestamp_ms / uuid→crypto.randomUUID / CREATE POLICY 削除） / 三層認可 PRJ-015 Coatly 100% ポート（middleware + requireAuth/requireOrgScope/requireOrgAdmin/requireCurrentOrg + scopedDb Proxy + ESLint `homekoto/no-raw-db`）/ R2 helper PRJ-016 HANEI ポート（split→single→default + pingR2 smoke check）/ Resend mail / Polling helper / 環境変数 16+ 件雛形 / db:generate / db:push / db:migrate / db:studio スクリプト追加。**Dev B 4.2h**: 18 ファイル新規 + HotaruSprite.tsx プレースホルダー + registry.ts 23 件登録（合計重み 1,558、common 12×100/rare 10×50,30,15/epic 1×3、epic 出現率 0.19% = DEC-006 必達） + reduced-motion 全 18 種対応 + テスト 90 件追加。**Dev C 7.5h**: lib/monthly-shows/{types,registry,rotation,caption}.ts + components/monthly-shows/{stage-shell,hotaru,pick-card}.tsx + 14 演出ステージ（E01-news〜E14-fortune）+ rotation `((monthIndex % 14) + 14) % 14` で 14 ヶ月重複ゼロ保証 = DEC-013 必達 + Hotaru 14 衣装 variant + app/(dashboard)/events/[eventId]/show ページ + 67 テスト。**Marketing 1.0 人日 / 計 2,556 行**: marketing-w2-lp-wireframe.md（1,333 行 = LP 10 セクション完全ワイヤー + 5,000 訪問→120 申込→30〜50 契約仮説）+ terms-of-service.md（238 行 / 15 条 / 第 2 条第 4 項に DEC-025 名称変更条項 + 第 8 条に DEC-027 OpenAI 個人キー）+ privacy-policy.md（411 行 / 14 章 / 委託先 6 社 = OpenAI/R2/Resend/Sentry/Turso/Vercel 表形式明示）+ ai-usage-consent.md（241 行 / 9 条 / API 学習除外明記 + 同意撤回権 + 手動発表モード代替）+ employee-departure-policy.md（333 行 / 退職時 3 択 凍結/匿名化/完全削除 + 思い出 PDF/CSV 30 日 + 個情法 35 条対応）。**CEO Trust-but-Verify**: typecheck **0 errors** / lint **0 errors** / test **188 / 188 PASS（10 ファイル、W1 17 → +171 = +1,006%）** / Dev A 中間報告の「test 142/145 + lint 1 error」は Dev B/C 完了統合後にすべて解消、CEO 直接実行で確認済 / stderr に jsdom HTMLMediaElement.pause 未実装警告ありもテスト自体は全件 PASS、W3 で audio mock 注入検討（軽微）。**工数実績: 6.5 人日**（W2 計画 7〜10 人日の下限到達、上振れなし）。**残（W2 検収最終確定 = DEC-030 のブロッカー = オーナー手動セットアップ 7 件）**: ① Turso DB 作成 + token / ② R2 バケット 2 つ + S3 API キー / ③ Resend ドメイン認証 + API キー / ④ Google OAuth クライアント / ⑤ Microsoft Azure AD アプリ + シークレット / ⑥ Sentry プロジェクト + DSN / ⑦ OpenAI API キー（DEC-027 個人）+ Budget Alert ¥3,000/月 + Vercel 環境変数 16 件投入 + Vercel preview デプロイ → CEO 通知で Cron / E2E 実走 / マルチテナント漏洩 E2E 7 本通しで DEC-030 確定。**進捗 25% → 38%**（W2 コード完遂・運用ブロック中分の繰り上げ）。AI 組織 PRJ-017 累計決裁 28 → **29 決裁**。）／2026-04-27（**【PRJ-017 ホメコト W2 スタック最終確定 + 緊急再選定 24h クローズ = DEC-028】**。オーナー追加 3 条件「Auth 有償化禁止 / Realtime CEO 一任 / コスト極小設計」を踏まえ、**Research/Dev/Marketing/Review の 4 部署視点で社内統合検討 → CEO 即決**。**最終スタック確定（全層完全無料、OpenAI 従量分のみ ¥21.3/月）**: DB = **Turso Hobby Free**（5 GB / 500M reads / 10M writes / Tokyo）/ Auth = **Better Auth + organization plugin（OSS / MIT / 永続無料、PRJ-015 Coatly 1.6.9 実績流用）**（Clerk = MAU 10K 超で $25/月強制 + Free ブランド表示要のため不採用、Auth.js v5 = Organization 自前実装で +3〜5 人日のため不採用）/ 行レベル認可 = **アプリ層三層認可**（middleware + `requireOrgScope` + `scopedDb` + ESLint 生クエリ禁止 + マルチテナント漏洩 E2E **7 本**、PRJ-015 実績 100% ポート）/ Realtime = **TanStack Query polling**（refetchInterval 1.5s/3s/false + visibilitychange + Turso DB 永続化、60min × 1500 名 × 1.5s = 3.6M reads/event = Turso 無料枠 0.7% 消費）（Pusher Free 同時 100 上限 / Ably Free msg 上限 / Vercel WS 関数時間圧迫すべて β規模で無料逸脱リスクあり不採用、Phase 2 で SSE on Vercel Functions に upgrade 可）/ Storage = **Cloudflare R2**（10 GB Free + Egress 完全無料、PRJ-016 HANEI `r2.ts` + `pingR2()` smoke check 流用）/ Mail = **Resend Free**（3K/月）/ 監視 = **Sentry Developer Free + Vercel Analytics + Speed Insights**（全 Free）/ Hosting = **Vercel Hobby**。**コスト DEC-004 ¥200/月 → ¥21.3/月（-89.4%）に正式更新**、+¥500/月 ルール内のため CEO 即決、オーナー追加承認不要。**書換工数 9.5〜11.5 人日（W2 計画内吸収）**: 0001_initial.sql の libSQL 移植 1 人日 + 三層認可ポート 3〜4 人日 + Better Auth 配線 5〜6 人日 + R2 移植 0.5 人日 + Polling 0.5 人日 + Resend 配線 0.5 人日。Postgres → libSQL 構文差分対応（JSONB → TEXT+JSON helper / uuid_generate_v4() → crypto.randomUUID() / TIMESTAMPTZ → INTEGER epoch ms / CREATE POLICY 削除＋三層認可代替）。**温存される W1 成果物**: Designer 5,116 行 + Marketing 4 文書 + Research（LLM/TTS/アニメlibs/β法務）すべて DB 非依存で温存。**DEC-026 緊急 48h 発令を 24h で即時クローズ**（外部調査ではなく組織既知 + Coatly/HANEI 実績 + 公式無料枠で最適解確定、`research-w2-turso-stack-revision.md` / `dev-w2-turso-migration-impact.md` 提出不要）。**W2 全タスク即着手承認**: DB 非依存（アニメ A-06〜A-23 + 演出 E-02〜E-14 + マイクロコピー差込 + shadcn CLI + ホタル画稿 + LP/法務ドラフト）+ DB 依存（libSQL 移植 + Better Auth + 三層認可 + R2 + Polling + 投稿フル + Inbox DB + マルチテナント漏洩 E2E 7 本 + Sentry）すべて。**オーナー W2 着手前ブロッカー（手動 60〜90 分）**: Turso DB 作成 + R2 バケット 2 つ作成 + Resend ドメイン認証 + Google/Microsoft OAuth クライアント発行 + Sentry プロジェクト作成 + OpenAI API キー発行（DEC-027 個人）+ Vercel 環境変数 16 件投入。**スケール時有償化トリガー記録（Phase 1〜公開β不発動）**: MAU > 200 or 月次イベント同時 > 800 で再上申。**進捗 22% → 25% に再上方修正**（Turso スタック確定でブロッカー解消、書換工数も計画内）。AI 組織 PRJ-017 累計決裁 27 → **28 決裁**。）／2026-04-27（**【PRJ-017 ホメコト オーナー Q-05/商標/DB/API 全裁定受領 + DB Supabase → Turso 切替 緊急再選定発令 = DEC-024〜027】**。同セッションでオーナー回答 4 件（Q-05 マスコット = **ホタル**確定 / J-PlatPat + WHOIS = **不実施 = リスク受容**、β規約に「名称変更可能性」明記 + Phase 2 起案時に再上申 / **DB = Supabase → Turso 切替**、Auth/RLS/Realtime/Storage の代替を 48h 緊急再選定 / **OpenAI = 個人契約**、Budget Alert ¥3,000/月 + 法人移管チェックリストを Phase 2 上申事項化）を CEO 即決裁。**Turso 切替の重大影響を明示**: W1 Dev scaffold 61 ファイルのうち `lib/supabase/*` + `supabase/migrations/0001_initial.sql`（16 表 + RLS 全表 + 25+ ポリシー + Postgres `JSONB` / `uuid_generate_v4()` / `CREATE POLICY` 構文）= **概算 25〜35% を libSQL 用に書き直し**、RLS は **アプリ層認可（middleware + Server Actions 内 orgId 強制フィルタ + 共通 guard 関数）に再設計**、Auth は **Clerk（Vercel Marketplace ネイティブ、Free 10K MAU、Google/Microsoft/Email 全対応）vs Auth.js v5（Turso 内蔵 accounts/sessions 表）を 48h で再選定**、Realtime（月次イベント司会↔参加者同期）は **Pusher / Ably / TanStack Query refetch + visibilitychange / Vercel WebSocket** から再選定、Storage は **Vercel Blob（Public+Private）に切替**。**W2 着手は条件付きで継続可**: DB 非依存タスク（アニメ A-06〜A-23 実装 + 月次演出 E-02〜E-14 ステージ実装 + マイクロコピー差込 + shadcn CLI 整備）は即着手承認、DB 依存タスク（投稿フル実装 / Inbox DB 統合 / Cron / 管理画面 / E2E DB シナリオ）は再調査結果のオーナー承認後に着手。**温存される W1 成果物**: Designer 5,116 行（トークン / マスコット 3 案 / 6 画面 / アニメ 23 / 演出 14 / コピー 80）+ Marketing 4 文書（β募集 / GTM / 1 行コピー / 競合）+ Research の LLM / TTS / アニメライブラリ / β法務（商標・ドメイン手順は Phase 2 起案時の再活用ナレッジとしてアーカイブ）。**Phase 1 進捗下方修正 25% → 22%**（scaffold 25〜35% 書き直しを反映）。**コスト試算（DEC-004 = ¥200/月）は Auth/Realtime 選定後に再計算**、Clerk + Pusher 想定で +¥0〜¥500/月（50 名規模）と概算。**スケジュール影響 = W2 着手 0〜2 営業日遅延見込**（DB 依存タスクのみ後ろ倒し）。次アクション = Research（Turso/Auth.js v5/Clerk/Pusher/Ably/Vercel Blob のメリット・デメリット・コスト・SLA・Tokyo region 整理 + 推奨組合せ案 3 通り）+ Dev（0001_initial.sql の libSQL 移植版ドラフト + アプリ層 RLS 代替設計の影響見積）。AI 組織 PRJ-017 の累計決裁 23 → **27 決裁**。）／2026-04-26（**【PRJ-017 ホメコト Phase 1 W1 完遂・W2 移行承認 = DEC-017〜023】**。同セッション内に **オーナー Q-01〜Q-07 決裁受領 → Critical 3 件訂正（CEO 直修） → W1 4 部署並列実装完遂** を圧縮達成。**Dev = 61 ファイル新規 scaffold**（Next.js 16.2.4 + React 19 + Tailwind v4 + shadcn 風 base 7 + Heroicons + Supabase 16 テーブル全表 RLS + Auth 3 経路 + proxy.ts + 3 ガード + PraiseComposer + アニメ engine 重み付き乱数 + アニメ 5 種 PoC + 月次発表 PoC（三重ガード + ニュース演出 + tts-1 mock）+ Vercel Cron + Vitest 17 + Playwright 2 + CI 4 jobs）。**CEO trust but verify 検証**: `typecheck` ✅ 0 errors / `lint` ✅ 0 errors 0 warnings / `test` ✅ 4 files / **17 tests PASS**（虚偽報告なし）。**Designer = 6 レポート計 5,216 行**（Warm Pop 確定 + WCAG AA 全組合せ証明 + Light/Dark/CVD + マスコット 3 案推奨「ホタル」+ 6 画面 desktop+mobile + アニメ 23 種 + 演出 14 種 + コピー 80 例）。**Research = 5 レポート計 2,021 行**（gpt-5-mini GA 確認 + 50 名月¥16.5 + Batch API 50% 引き / TTS 6 voice 訂正 = Phase 0「13 voice」誤記、月次¥4.8 = 1/30 縮 / 商標+ドメイン手順 / 5 ライブラリ React 19 互換 / 公開β 4 文書 + プラポリ 14 章 + W5 弁護士レビュー）。**Marketing = 4 レポート**（30〜50 社募集設計 + 6 チャネル + LP 10 セクション / 20 週 GTM + LTV/CAC 310 倍 / 1 行コピー「会議が、待ち遠しくなる。」推奨 / 競合 7 社比較 + ピアセレブレーション新カテゴリ宣言）。**確定追加**: DEC-018 Next.js 16.2.4 bump（CVE-2025-66478 対応）/ DEC-019 マスコット候補「ホタル / ヒカリ / コトリ」3 案、推奨ホタル（Q-05 待ち）/ DEC-020 TTS voice 6 種訂正 + 月次¥4.8 / DEC-021 gpt-4.1-mini fallback 環境変数切替 / DEC-022 公開β 4 文書 + W5 弁護士レビュー / DEC-023 1 行コピー「会議が、待ち遠しくなる。」推奨。**残（W2 着手前）**: オーナー Q-05 マスコット選定 + J-PlatPat / WHOIS 実機確認 + Supabase Cloud DB + OpenAI / TTS API キー + Vercel 環境変数 → W2 = アニメ +18 種 / 演出 +13 種 / 投稿フル機能 / 受信 DB 連携 / 月次発表ステージ本体 / Cron 本実装 / 管理画面中身 / E2E 拡充 / Sentry + Analytics / shadcn CLI 切替 / TanStack Query。AI 組織並列化により Phase 0 起案 → Phase 0 完遂 → オーナー回答 → Critical 訂正 → W1 完遂を **1 セッションで達成**（PRJ-015/016 と同等以上速度、計 23 決裁）。）／2026-04-26（**【PRJ-017 ホメコト 起案 + Phase 0 全部署完遂 + Phase 1 CONDITIONAL GO = DEC-001〜009】**。オーナー発注同日に全部署招集（Research / PM / Marketing / Dev=Designer 兼任 / Review）を並列起動 → **6 本レポート納品（計約 4,200 行 / 一次ソース 50+）**: research-phase0.md（694 行 / 国内 15 社 + 海外 5 社 競合調査 / Bandura 自己効力感 + Three Good Things PNAS 2025 + Gallup「週 1 回認知でエンゲージ 5 倍」/ Sora 2 / Veo 3 不採用判断 / 50 名月¥25 試算）/ pm-phase0.md（ストーリー 52 件 MoSCoW / WBS W1〜W10 / 50〜70 人日中央値 62 / 受入基準 66 項目 / NSM = MAP 提案 / R-01〜04 リスクスコア 6 以上）/ marketing-phase0.md（HR Tech 924 億円 17% 成長 / 命名 12 候補から「ホメコト」最終推奨 / homekoto.app / ICP 30〜100 名 IT/Web/制作系 TAM 約 19 億円 / Phase 1 完全無料 → Phase 2 ¥0/¥9,800/¥29,800/¥79,800 4 段階 / NSM「月次発表モード × 参加率」）/ dev-phase0.md（1,012 行 / 16 テーブル ER 図 / アニメ 23 種 + 演出 14 種 / Lottie+TTS+Framer 合成で動画風）/ design-phase0.md（649 行 / 案 A Pop&Confetti 70% × 案 C Ghibli Dawn 30% ブレンド = Warm Pop / マスコット「コトホメ」/ WCAG AA + reduced-motion）/ review-phase0-gate.md（CONDITIONAL GO / Critical 3 件 = Dev 月¥7,600 試算が真値 ¥200 の 130 倍過大 + 命名「ホメコト」と「コトホメ」音節逆転混同 + PM「GPT-4o-mini」誤記 / Q-01〜Q-07 オーナー決裁待ち / 受入基準 66 → 82 項目に拡張提案）。**確定スタック = Next.js 16 + Tailwind v4 + shadcn/ui + Heroicons + Framer Motion + canvas-confetti + tsparticles + Lottie + Rive + Supabase（Tokyo）+ AI SDK + OpenAI gpt-5-mini + tts-1/tts-1-hd + Vercel Hobby→Pro / 動画生成 API は Phase 1 不採用 / 月額 ¥200 程度（50 名）**。DEC-002 Phase 0 完遂受領 / DEC-003 スタック確定 / DEC-004 コスト真値訂正（¥7,600 → ¥200）/ DEC-005 命名「ホメコト」仮確定 + マスコット名再案発令 / DEC-006 アニメ N=23 + 演出 M=14 必達 / DEC-007 NSM 統一（月次発表モード × 参加率、MAP は補助 KPI）/ DEC-008 受入基準 66 → 82 項目拡張 / DEC-009 Phase 0 完了 + W1 CONDITIONAL GO（Critical 3 + Q-01〜Q-07 解消後）。**残**: Critical 3 解消 + オーナー軽承認 Q-01〜Q-07（命名最終承認 / 利用規模 / 動画 API 不採用 / アニメ 23 演出 14 数量 / マスコット名選定 / NSM 統一 / Stitch 活用範囲）→ Phase 1 W1 着手。AI 組織並列化により Phase 0 を **1 セッションで完遂**（PRJ-015 / 016 と同等速度、6 部署 + CEO 統合、計 4,200 行）。）／2026-04-26（**【PRJ-017 社内褒め活推進Webアプリ 起案 = DEC-001】**。オーナー発注を受け新規採番。社員のエフィカシー向上を目的とした、同僚への感謝・「すごい」投稿を共有し合うゲーミフィケーション Web アプリ。**主要要件**: 投稿時のゲーム的多彩アニメーション（毎回違うシーン）/ 月次職場会議で自分宛感謝確認 / 生成 AI によるベスト3 選定 + 面白い発表（毎月バリエーション）/ デザイン徹底（Stitch 等の活用検討）/ 日々の疲れが吹き飛ぶ最高の体験。**クライアント** = 自社プロダクト（社内向け、将来 SaaS 横展開視野）。**プロダクト名は未定**（後にマーケティング部門が命名）。Phase 0 招集対象 = Research / Designer / PM / Marketing / Dev / Review。`projects/PRJ-017/` 起票完了（brief.md / decisions.md / reports/ / app/）、次案件 ID は PRJ-018 に更新。）／2026-04-26（**【PRJ-016 HANEI 接続先表示の恒久化 + cross-env 導入 = DEC-028】**。DEC-026/027 で発生した 2 大事故源（`.env.local` 読込ポリシー差異 / Windows cmd 環境変数残留）を恒久ブロック。**作業 C = `seed-problems-runner.ts` + `drizzle.config.ts` の起動時に接続先 URL を 1 行表示**（`kind=TURSO (remote)` or `kind=LOCAL SQLite file`、authToken は `<REDACTED>` で隠蔽）。**作業 A = cross-env v10.1.0 install + `db:seed:dry` / `db:seed:full` 追加**（cross-env が 1 コマンド限定で環境変数を渡す → Windows cmd でも `set X=` クリア不要）。**CEO 検証**: typecheck 0 / lint 0 / test 133/133 PASS（回帰ゼロ）。**簡略化された新オーナー手順**: `npm run db:push` → `npm run db:seed:dry` → `npm run db:seed:full` → `npm run ai:generate-explanations` → `npm run ai:generate-tts`（環境変数操作完全不要）。**W5 Dev T-5 の cross-env 導入を CEO で先取り完了**。+ W5 部署別ブリーフ事前下書き `reports/w5-briefing-draft.md`（200 行 / Dev T-1〜7 + Research R-1〜5 + Designer D-1〜2 = 計 14 タスク完成形）も作成済 → オーナー復帰直後に W5 即発令可能。）／2026-04-26（**【PRJ-016 HANEI drizzle-kit `.env.local` 自動読込修正 + DEC-024 訂正 = DEC-027】**。オーナー実行で「`no such table: problems`」エラー検知 → 真因 = drizzle-kit が `.env.local` を自動読込せず、過去の `db:push` は Turso ではなく **ローカル SQLite ファイル `local.db` に**テーブルを作っていた事実が判明。**DEC-024 の「Turso 25 テーブル作成確認」を誤認として訂正**。CEO 直接修正: `drizzle.config.ts` 冒頭に `dotenv.config({ path: ".env.local" })` を追加（既存依存 `dotenv` 利用、install 不要）。**Windows cmd 環境変数指定の正しい構文も提示**: `set DRY_RUN=true&& npm run db:seed`（`&&` 前に空白を入れない）。**オーナー再実行手順（修正後 4 ステップ）**: ① `npm run db:push`（Turso へ正しく push）→ ② `set DRY_RUN=true&& npm run db:seed`（試走 822 件 expect）→ ③ `set SEED_LEVELS_SKILLS=1&& npm run db:seed`（本投入）→ ④ `npm run ai:generate-explanations` + `npm run ai:generate-tts`。**今後の Phase 完遂判定に「db:push 後に Turso CLI で SELECT count(*) FROM problems で実テーブル存在を確認」を必須化**。）／2026-04-26（**【PRJ-016 HANEI W4.5 緊急補修完遂 = DEC-026】**。オーナー本番実行で 2 件障害検知 → 原因究明 → CEO 直接修正 + Dev 緊急発令 → CEO 検収まで 1 セッション完遂。**障害① `npm run ai:generate-explanations` 全 471 件 skip**（seed-runner 未実装 + fake ID `seed_0000` 生成で DB 自然 ID と照合不能）/ **障害② `npm run ai:generate-tts` 180 件全失敗**（tsx が `.env.local` 自動読込せず OPENAI_API_KEY 未認識）。**CEO 直接修正**: `package.json` の AI scripts 全 5 件 + 新規 `db:seed` に `--env-file=.env.local` 追加 + `generate-*-w3.ts` main 先頭に「dryRun 以外で API key 不在なら process.exit(1)」追加（180 件暴走防止）。**Dev 緊急発令完遂**: 新規 4 ファイル（`scripts/seed-id-mapper.ts` 約 550 行 + `scripts/seed-problems-runner.ts` 約 380 行 + unit test 2 本 35 ケース）+ 既存 2 ファイル修正（fake ID 撤廃、`loadAllSeedIds()` 経由に書換）。**ID 衝突回避**: W4 L4 listening を `L4-021..100` シフト / W4 R3 reading を `R3-011..050` シフト / W4 V5 vocab は別命名空間 `V5W4-001..100`。**Dev 方針逸脱を CEO 承認**: ブリーフ「seed ファイル 822 件に id 追加」→ 実装「seed-id-mapper に採番集約 + seed 本体不変」（事故リスク回避 + unit test 化 + 機能等価）。**CEO 検収（trust but verify 厳守）**: typecheck 0 / lint 0 / **test 133/133 PASS**（98 + 35 = +35）/ 虚偽報告なし。**オーナー再実行手順**: `DRY_RUN=true npm run db:seed` → `SEED_LEVELS_SKILLS=1 npm run db:seed` → `npm run ai:generate-explanations`（¥210〜420）→ `npm run ai:generate-tts`（¥30〜60）。W5 着手 technical blocker 完全解消。**DEC-025 の CEO 検収漏れを自己反省し、今後の Phase 完遂判定に「end-to-end smoke = 実 DB / 実コマンド成功」を必須化**。）／2026-04-26（**【PRJ-016 HANEI Phase 1 W4 完遂・W5 移行承認 = DEC-025】**。同セッション内に W4 全タスクを 3 部署並列完遂。**Dev = T-1〜T-8 全 8 タスク達成**（模試結果画面 `/parent/mock-exam-results` SVG 自作レーダー / 受験日設定モーダル shadcn Calendar + 過去日不可 + Server Action / 学習者切替 UX 複数子家族対応 Tabs ベース / E2E webServer DB 切替で通し E2E 実行可能化 = W3 申し送り解消 / V3-012R 本番反映 bulk replace / DRY_RUN=1/true 両対応寛容化 / `seed-problems-w4.ts:115` `#`→`//` 緊急自己修正 / Vitest +14 + Playwright +5）。**Research = 第 3 陣 221 問**（5 級語彙 100 = V5W4-001〜100 / 4 級リスニング 80 = L4-001〜080 / 3 級リーディング 40 = R3-001〜040 passage + 2 設問構造 / G4-080 改稿 1 件 = G4-080R）/ qualityScore 平均 **0.907**（W3 0.886 から +0.021）/ **0.85+ 比率 100%（221/221）** / G4-080 改稿（0.84 → 0.91）/ **累計 822 問**（W2 200 + W3 401 + W4 221）/ 著作権違反 0 / kid-safe 違反 0。**Designer = 4 レポート計約 2,598 行**（design-w4-mock-exam-results.md 640 行 + design-w4-exam-date-modal.md 409 行 + design-w4-learner-switch.md 332 行 + design-w4-snippets.md 1217 行 / JSX 10 個）/ **SVG 自作レーダー決定**（外部ライブラリ不採用、bundle size 抑制） / **ことだまトリ保護者モード +2 状態**（14 番目 parent-encouraging = 模試後励まし / 15 番目 parent-planning = 受験日設定計画 = 累計 15 状態）/ マイクロコピー +12 例（「不合格」「失敗」回避 → 「あと一歩」「次に向けて」前向き表現統一）。**最終結果 = typecheck 0 / lint 0 / test 98/98 PASS（W2 60 + W3 19 + W4 19）**。**W5 申し送り**: Dev 5（模試実施フロー本体 / 同年齢層比較ロジック / 弱点改善提案 AI 動的化 / smoke 完走 / AI バルク結果反映）+ Research 5（5 級 reading+50 / 5 級 listening+30 / 822 問 DB seed + E2E / TTS A/B / 模試結果サンプル各 3 学習者）+ Designer 2（模試実施フロー UI / 比較表現倫理マイクロコピー）。Phase 0 起案 → W1 → W2 → W3 → W4 完遂を **3 セッション以内**で達成。）／2026-04-26（**【PRJ-016 HANEI Phase 1 W3 完遂・W4 移行承認 = DEC-023】**。同セッション内に W3 全タスクを 3 部署並列完遂。**Dev = T-1〜T-8 全 8 タスク達成**（in-memory libSQL E2E fixture / 保護者ダッシュボード骨組み 4 セクション + 三層認可厳守 / AI コーチ誤答解説バルク生成スクリプト + ¥500/run 上限 / TTS バルク事前生成スクリプト 5 級 200×3 voice / shadcn 4 component 追加 progress+sheet+dialog+tabs / 1 問差し替えヘルパ / Vitest 3 本 + Playwright 1 本 = +19 ケース / オーナー smoke チェックリスト 8 項目 curl 例付き）。**Research = 第 2 陣 401 問**（5 級リスニング 100 + 4 級文法 150 + 3 級ライティング 100 + 5 級並べ替え 30 + 4 級リーディング 20 + V3-012R 1）/ qualityScore 平均 **0.886** / **0.85+ 比率 99.75%（400/401）** / V3-012 改稿（0.84 → 0.91） / 著作権違反 0 / kid-safe 違反 0 / ライティング採点ルーブリック = 公式英検 4 観点 + HANEI 内部 5 軸 / 模範解答自己採点平均 95.6%。**Designer = `design-w3-parent-dashboard.md` 780 行 + `design-w3-parent-snippets.md` 735 行 / JSX 8 個**（ParentHeaderBar 等）/ ことだまトリ保護者モード 3 状態追加（parent-greeting / parent-alert / parent-celebrate） / マイクロコピー +20 例（敬語ベース） / 学習者画面とのトーン差別化を §1 マトリクスで明示。**CEO 検証で typecheck エラー 3 件発見・即修正**: Dev エージェントの「全緑報告」を鵜呑みにせず CEO 再走したところ `seed-problems-w3.ts:2173` の swap が noUncheckedIndexedAccess 違反 + `generate-explanations-w3.ts:142` の w3 import 型不整合（Research = W3Bundle オブジェクト、Dev = 配列前提）を検出 → CEO 直接修正で解消。**最終結果 = typecheck 0 / lint 0 / test 79/79 PASS（W2 60 + W3 19）**。**W4 申し送り**: Dev 5（E2E webServer DB 切替 / AI バルク本番実行 / V3-012R 反映 / smoke 完走 / shadcn 活用先）+ Research 5（5 級語彙+100 / 4 級 listening+80 / 3 級 reading+50 / 改稿候補差し替え / TTS A/B）+ Designer 3（模試結果画面 / 受験日設定モーダル / 学習者切替 UX）。Phase 0 起案 → W1 → W2 → W3 完遂を **3 セッション以内**で達成。）／2026-04-26（**【PRJ-015 Coatly W2-C 認可ハード化完遂 + Next 16 streaming-status 解消 = DEC-036〜041】**。同セッションで `pnpm install` peer dep 警告 6 件分類 → MUST FIX 4 件（drizzle-orm 0.36→0.45.2 / drizzle-kit 0.28→0.31.4 / zod v4 / react-day-picker v9）upgrade → `pnpm db:migrate` 失敗（duplicate column name: used_amount_jpy）→ 0003 snapshot 欠落 + `__drizzle_migrations` 履歴ズレを `migrate-recover.ts` で修復 → CI typecheck 失敗（zod v4 ZodError.errors→issues rename を未 commit で push）を `3c7a4e6` で fix → W2-C 認可 E2E 4 ケース失敗（C2/C3/C4 が 200 を返す + C7 login timeout）の根本原因 = Next.js 16 streaming-status 問題と特定 → `experimental.authInterrupts: true` + `forbidden()` / `unauthorized()`（next/navigation, Next 16+）+ `app/forbidden.tsx` / `app/unauthorized.tsx` 新規 + E2E loginViaUI timeout 15s→30s で commit `c28f8c2` push 完遂。**Vercel 自動デプロイ稼働中**（`coatly-mu.vercel.app`、commit `c28f8c2` deployed / db:up）。**残**: CI run on `c28f8c2` 緑化確認（in_progress、gh CLI 未インストールのためオーナー目視確認）→ W3 着手（マルチテナント漏洩 E2E 仕上げ + ダッシュボード本実装 + a11y + Lighthouse）。AI 組織並列化により Phase 1 W1 + W2-A/B/C を 1 セッションで圧縮達成（DEC-001〜041、計 41 決裁）。）／2026-04-26（**【PRJ-016 HANEI W2 ビルド完全グリーン化 = DEC-019〜022】**。同セッション内に W2 完遂後のビルドエラー 4 連発（npm install ERESOLVE @sentry/nextjs / npm install ETARGET @anthropic-ai/sdk / typecheck 12 errors / lint+test 4 fail / lint 循環参照）を全解決。**最終結果**: `npm install` ✅ no peer conflict 814 packages / `npm run typecheck` ✅ 0 errors / `npm run lint` ✅ 0 errors 0 warnings / `npm run test` ✅ 60/60 PASS（6 ファイル / 1.6s）。**最大の山場 = ESLint v9 flat config 移行**: `FlatCompat.extends("next/core-web-vitals", "next/typescript")` が plugin 内部の循環参照を `JSON.stringify` できず起動失敗 → eslint-config-next v16.2.4 のネイティブ flat config exports（`./core-web-vitals` `./typescript`）を直接 import する形に書き換えて完全解決。副次的に `@sentry/nextjs ^9 → ^10`（v10.50.0 が `next ^16.0.0-0` peer 対応）、`drizzle-orm ^0.36 → ^0.45.2` / `drizzle-kit ^0.30 → ^0.31.4`（better-auth@1.6.9 peerOptional 整合）、`@eslint/eslintrc` / `@typescript-eslint/{eslint-plugin,parser}` devDep 整理、`.eslintrc.json` legacy 削除、コード警告 7 件（未使用 import / inline eslint-disable / `JUDGE_SYSTEM_PROMPT` → `_JUDGE_SYSTEM_PROMPT` 改名）も全清算。**W3 着手の technical blocker は完全解消**。）／2026-04-26（**【PRJ-016 HANEI Phase 1 W2 完遂・W3 移行承認 = DEC-018】**。同セッション内に W2 全タスク並列完遂。**Dev = T-1 Drizzle +15 テーブル（sessions / accounts / verifications / daily_plans / streaks / xp_levels / badges / user_badges / mock_exam_results / ai_coach_conversations / ai_coach_messages / mastery_estimates / characters / generated_problems_queue / problem_explanations / exam_dates → 0001_w2_extensions.sql）+ T-2 Better Auth 完成形（signup → email verify → login → session、13歳未満同意フロー含む）+ T-3 学習 UI v1（/study/[levelCode]/[skillCode] + 4 択 + 解説 + ts-fsrs 4.6.1 SRS 更新）+ T-4 AI コーチ実 API（gpt-5-mini streaming + 三重ガード + tool use 4 関数 + ¥10/日コストガード）+ T-5 LLM-as-Judge Inline Cron（02:00 JST 毎日 / 最大 50 問 / ¥100/日 上限）+ T-6 TTS + R2 + Resend 4 種 + T-8 Sentry + Vercel Analytics + Speed Insights + T-9 Vitest 3 本 + Playwright 2 本（前任 Dev T-1〜T-5 完了 + フォロー Dev R-1〜R-8 補完で完遂）**。**Research = seed-problems-w2.ts に 200 問（5級 vocab60+grammar30 / 4級 vocab50+listening20 / 3級 vocab30+reading10）/ 自己採点平均 0.905 / 0.85 以上 199/200（99.5%）/ 著作権セーフ + kid-safe NG 0 件 / サンプル JSON 6 問オーナー検収用同梱**。**Designer = design-w2-screens-v2.md（home / study / diagnosis 3 画面 / 8 章 Tailwind フル + framer-motion + ARIA + reduced-motion）+ design-w2-style-snippets.md（JSX 12 個）+ ことだまトリ 10 状態マッピング + マイクロコピー +30 例（合計 116 例）+ shadcn/ui 追加 install 申し送り（progress / sheet / dialog / tabs）**。**残（W3 開始前ブロッカー = オーナー手動 30〜60 分）**: npm install / npm run db:migrate / 実 API smoke / Vercel 環境変数 push。**W3 = 本格 E2E（in-memory libSQL）+ TTS バルク事前生成 + 保護者ダッシュボード骨組み + Research 400 問追加 + AI コーチ誤答解説本格化**。Phase 0 起案 → Phase 0 完遂 → W1 完遂 → W2 完遂 を **1 セッション内で達成**（PRJ-014/015 のパターンを上回る速度）。）／2026-04-26（**【PRJ-016 HANEI W2 着手発令 = DEC-016〜017】**。同日にオーナーから「環境変数設定完了 + R2 = W4 まで r2.dev / W5 で Custom Domain 切替で進めて良い」回答受領、**W2 着手前ブロッカー 6 件（Turso DB / OpenAI + Anthropic キー / R2 / Resend / Better Auth secret / `.env.local`）が全解消**。CEO 即決で **DEC-016（R2 段階切替を正式決裁）+ DEC-017（W2 着手承認・並列発令）** を記録。W2 並列発令: **Dev（重）= 残り 15 テーブル + Better Auth 完成形（signup→email verify→login→session）+ AI コーチ実 API（gpt-5-mini streaming + Moderation 三重ガード）+ ts-fsrs SRS 統合 + LLM-as-Judge Inline Cron + 学習 UI v1（語彙 4 択 + 解説 + SRS 更新）+ R2 presigned helper + Resend mail + Sentry init + Vercel Analytics**、**Research = 実問題 200 問（5級/4級/3級 語彙）+ LLM-as-Judge 採点でシードデータ作成**、**Designer = 画面実装ガイド v2（home/study/diagnosis 3 画面 Tailwind class フル）**。）／2026-04-26（**【PRJ-016 HANEI Phase 1 W1 完遂 + W2 移行承認 = DEC-015】**。同セッション内に W1 並列実装（Dev / Designer / Research）が完了。**Dev = 62 ファイル scaffold**（Next.js 16 + Drizzle 10 テーブル + Better Auth + 三層認可（proxy + guards + scoped + ESLint）+ AI コーチ最小スケルトン（kid-safe + 三重ガード + tool use 4 関数）+ LLM-as-Judge PoC + 学習者導線 4 ページ + Amber Gold トークン + CI 5 ジョブ + テスト雛形 4 本 + ER 図 + DEPLOYMENT.md）。**Designer = 4 レポート**（design-w1-tokens.md WCAG AA 全本文組合せ保証 + design-w1-character.md ことだまトリ 10 ポーズ生成プロンプト + design-w1-screens.md 主要 6 画面 Tailwind 実装可能レベル + design-w1-microcopy.md ですます調 86 例文）。**Research = 4 レポート**（research-w1-models.md gpt-5-mini $0.25/$2.00・cached $0.025・400K・SO/FC/Vision 全対応 / Sonnet 4.5 $3/$15 / cross-LLM judge 1,600 問 ¥1,500-3,500 / AI ユーザー単価 ¥4.86/日 K-6 余裕クリア + research-w1-trademark.md HANEI 日本 EdTech 衝突未確認・最終確認は J-PlatPat 公式画面要・hanei.app/.jp/-app.com 推奨 + research-w1-llm-judge.md 生成+採点プロンプト雛形+zod スキーマ+重複検出 0.92+再生成 3 回上限 + research-w1-legal-kidsafe.md 改正個情法・COPPA・UNICEF 30 項目チェック+危機検知 60 語+L1-L4 通知+プラポリ 13 章雛形）。**残（W2 着手前ブロッカー = オーナー手動 30〜60 分）**: Turso DB 作成 / OpenAI + Anthropic API キー / Cloudflare R2 / Resend / Better Auth secret / `.env.local` 設定 → W2 = 残り 15 テーブル + Better Auth 完成形 + AI コーチ実 API 疎通 + ts-fsrs SRS 統合。**Phase 0 起案 → Phase 0 完遂 → オーナー回答 → DEC-010〜014 → W1 並列実装 → W1 完遂を 1 セッションで達成**（PRJ-015 と同等以上の並列処理速度）。）／2026-04-26（**【PRJ-016 HANEI オーナー回答受領 + DEC-010〜014 + Phase 1 W1 着手命令発令】**。同日にオーナーから A-01〜A-05 回答受領: A-01 HANEI 推奨で進行 / A-02 **既に Vercel Pro で運用中** / A-03 **外注なし** / A-04 半年保証 Phase 2 発動（無料化により「返金」→「学習延長＋手厚いコーチング」に再定義） / A-05 β 30〜50 家庭推奨通り。**追加指示: 「料金面の実装は現時点で不要、まずは無料で提供」**。CEO 即決で **DEC-010（回答受領）/ DEC-011（問題プール = AI 自動生成 + LLM-as-Judge cross-LLM + オーナー抜き取り 5% / 外注ゼロ）/ DEC-012（料金システム未実装、Phase 1〜2 完全無料、Stripe 等は Phase 3 検討、MVP 48.5→42 人日に縮小、受入基準 68→約60 項目）/ DEC-013（Vercel Pro 単一プラン化、月¥1,200→¥4,200 に修正、Hobby 切替計画削除）/ DEC-014（W1 着手命令発令）** を同セッション内に決裁。**Phase 1 W1 並列発令** = Dev（Next.js 16 + Turso + Drizzle + Better Auth + Tailwind v4 + shadcn/ui + Heroicons + AI SDK + CI + ER 図 + 保護者サインアップ実装）+ Designer（Amber Gold トークン v1 + ことだまトリ ラフ案 10枚）+ Research（gpt-5-mini 実 API 疎通 + J-PlatPat 商標検索 + ドメイン取得確認 + LLM-as-Judge パイプライン PoC）。AI 組織並列化により Phase 0 完遂 → オーナー回答 → DEC-010〜014 → W1 発令を **1セッション内で完遂**。）／2026-04-26（**【PRJ-016 HANEI 起案 + Phase 0 全部署完遂 + Phase 1 CONDITIONAL GO = DEC-001〜009】**。同日にオーナーから「小学生向け英語学習アプリ／英検5〜3級／半年で英検3級合格／AIコーチ／ゲーミフィケーション／徹底デザイン」の発注受領 → 全部署招集（Research / Designer / PM / Marketing / Dev / Review）を並列起動 → **6 本レポート納品（計約 1,800 行）**: research-phase0.md（一次ソース 42 件で英検 5/4/3 級カリキュラム + 競合 10 サービス + FSRS/IRT/BKT + gpt-5-mini 公開済み確認 + 著作権 100% 自社オリジナル AI 生成必須結論）/ design-phase0.md（A 案ポップ×ジェントル推奨 + Amber Gold #F2A93A + Noto Sans JP + Nunito + ことだまトリ + 樹木型ロードマップ + 主要 8 画面 ASCII モック）/ pm-phase0.md（MVP 48.5 人日 / W1〜W10 / MoSCoW Must 20 + Should 8 + 受入 24 項目 + 180 日カリキュラム骨子）/ marketing-phase0.md（市場 2,952 億円 + 小学生 3 級 10 年 1.9 倍 + スタサプ KIDS 撤退で空白 + 命名 HANEI 推奨 + 月¥1,980/¥2,980 3 プラン + NSM「7 日連続学習達成」）/ dev-phase0.md（25 テーブル ER + Turso+BetterAuth+R2 + gpt-5-mini/4.1-mini ハイブリッド + 三重ガード + 三層認可 + W1 POC 2 件）/ review-phase0-gate.md（CONDITIONAL GO + 整合性マトリクス + P0 7 件 / P1 5 件 / P2 5 件 + 受入基準 68 項目）。**確定スタック = Next.js 16 + Turso libSQL + Drizzle ORM + Better Auth + Cloudflare R2 + AI SDK + OpenAI gpt-5-mini + OpenAI tts-1 + Vercel Hobby→Pro + Resend（β 100 名 月¥1,200 / 公開 500 名 月¥24,000）**。DEC-003 スタック / DEC-004 命名 HANEI + Amber Gold + ことだまトリ / DEC-005 「目指す」訴求 / DEC-006 Phase 1 無料 + Phase 2 3 プラン / DEC-007 問題プール 1,600 問 + 外注¥30〜50 万 / DEC-008 受入基準 68 項目 / DEC-009 Phase 0 完了 + W1 CONDITIONAL GO（5 ブロッカー解消後）。**残**: オーナー軽承認 5 件（A-01〜A-05）+ Research の gpt-5-mini 実 API 疎通 + J-PlatPat 商標確認 + ドメイン取得 + PM/Dev WBS 再見積 → W1 着手。AI 組織並列化により Phase 0 を **1 セッションで完遂**（PRJ-015 で確立したパターン踏襲、計 6 部署 + CEO 統合）。）／2026-04-26（**【PRJ-015 Coatly Phase 1 W1 実装完遂 = DEC-014〜015】**。同日に軽承認3件回答受領（プラポリ窓口=別途 / Designer外部委託=なし内製で完遂 / リリース日=可能な限り早期化）+ 実装着手命令を受け、Dev/Designer 並列起動で W1 を **1セッション完遂**。**Dev 成果**: `app/` 配下に **68ファイル scaffold**（package.json + 全 config / Drizzle schema 13テーブル / Better Auth + organization plugin / R2 presigned URL ヘルパ / 4認可ガード + scopedXxx + 生クエリ禁止 ESLint / Next 16 proxy.ts / 12ルート page.tsx / 5 shadcn UI primitives / 6 業務コンポーネント / 4 Server Actions / 3 Zod validators / seed.ts FY2026 ¥800K = ¥300K + ¥100K × 5 / unit test 18ケース PASS / E2E smoke 2ケース）+ POC-1/POC-2 コード妥当性 ◯。**Designer 成果**: 4成果物（design-tokens.md 619行 / screen-mockups.md 734行 / wow-elements-spec.md 626行 / design-review-checklist.md 137行 = 計2116行）+ ダークモード Phase 1 採用推奨（+0.3人日で吸収可）。**早期化見立て**: 7/3 → **6月中旬リリース（最大2週間短縮）達成可能**。**残**: pnpm install + Turso DB 作成 + R2 バケット作成 + .env.local 設定（オーナー手動 30〜60分）→ W2 Server Actions 本実装。AI 組織並列化により Phase 0 全完遂 + W1 実装まで **1日で達成**（DEC-001〜015、計15決裁）。）／2026-04-26（**【PRJ-015 Coatly v2 確定 + Phase 0 → Phase 1 Conditional Go v2 = DEC-009〜013】**。同日にオーナーから5問回答受領（年度開始月=4月／電帳法MVP保存のみ／予算 全体¥300K+各県¥100K=¥800K／命名Coatly確定+ドメイン取得不要／**SaaS化視野**）+ **重大スタック転換指示（Supabase → Turso、無料化要望）**。CEO がリサーチ部門に Turso ecosystem 深掘りを発令、並行で Conditional Go 残条件 C-02〜C-05 を 4 成果物（security-baseline.md / legal-privacy-policy.md / acceptance-criteria-v1.md / figma-schedule.md）で解消。Research 結果を踏まえ Dev が technical-spec v2 改訂、Review が v2 ゲート再判定。**確定 v2 スタック = Next.js 16 + Turso(libSQL) + Drizzle ORM + Better Auth + Cloudflare R2 + Resend + Vercel Hobby（MVP 200名規模で月額 ¥0、5部活1000名でも月 $0〜20）**。SaaS 化対応で単一org+prefecture enum → `organizations + groups` 汎用設計に格上げ。RLS 不在の代替 = 3層認可防衛（middleware + requireXxxRole guards + scopedXxx クエリビルダ + ESLint 生クエリ禁止 + マルチテナント漏洩 E2E 7本）。Phase 1 MVP = **41人日 / 9週 / 2026-05-01 着手 / 2026-07-03 リリース**。W1 で Better Auth + R2 の POC 2件、フォールバック計画（Auth.js v5 / Vercel Blob）確立済み。1セッションで Phase 0 全完遂（DEC-001〜013、計13決裁）+ オーナー回答反映 + 重大スタック転換 + v2 化 + Conditional Go v2 まで完了、AI 組織並列化により Phase 0 を **1日で完遂**。）／2026-04-26（**【PRJ-015 Coatly Phase 0 完了 + Phase 1 Conditional Go = DEC-001〜008】**。オーナー発注同日に全部署招集で並列調査・設計を完遂、Phase 0 要件定義 → 技術リサーチ → デザインコンセプト → PM要件WBS → マーケ命名 → Dev統合技術設計 → Reviewゲート判定の **6本レポート納品**。確定事項: 統合スタック=Next.js 16 + Supabase RLS全表 + Tremor + Framer Motion + Resend + Vercel / デザイン=A案 Quiet Luxury Sport（Court Green / 中国地方インタラクティブマップ / タビュラー数字カウントアップ / テニスボール軌跡で予算消化可視化）/ 命名=Coatly（コートリー、Court+ly の造語、横展開耐性）/ Phase 1 MVP=38人日 8週 MUST 14ストーリー / 着手予定 2026-05-01。**残: オーナー上申5件（DEC-008、Q-01 年度開始月 / Q-02 電帳法対応要否 / Q-03 FY2026予算初期値 / Q-04 命名確定+ドメイン取得 / Q-05 SaaS化方針）の回答取得（期限 2026-04-30）+ 法務方針確定（DEC-007、領収証保存期間/退会削除/プライバシーポリシー）+ Figma カンプ3画面の並行着手**。Phase 0 → 1 ゲート判定 = **Conditional Go**（5条件解消で W1 着手可）。AI 組織並列化により Phase 0 を **1セッションで完遂**（リサーチ/デザイン/PM/マーケを並列、その後 Dev → Review を直列、計 6 部署 + CEO 統合）。）／2026-04-26（**【PRJ-015 テニス部予算管理Webアプリ 起案 = DEC-001】**。オーナー発注を受け新規採番、Phase 0 要件定義中。中国地方5県（岡山・広島・山口・鳥取・島根）+ 全体の予算・活動費管理、ユーザー3階層（全体管理者/県管理者/一般ユーザー）、領収証画像・PDF添付、インボイス登録番号対応、最高レベルUX × あっと驚くおしゃれUI が初動要件。技術スタック初期案 = Next.js + Supabase + Vercel（組織標準）。全部署招集で要件定義・実装計画策定中。）／2026-04-25（**【PRJ-014 GENBA Visual β-prep フェーズ移行 = DEC-048】**。MVP 完成（DEC-047）後、オーナー作業手順書 `reports/owner-beta-release-checklist.md` を作成し、`app/DEPLOYMENT.md` の DEC-027 / DEC-037 整合化（旧 ¥9,800/¥19,800 → ¥79,800/¥149,800、Spot ¥1,980/¥3,980 = A 案）+ `STRIPE_PRICE_ID_*_MONTHLY` / `APP_ENCRYPTION_KEY` 必須化を実施。マーケ部門に β 顧客 5〜10 社の選定を並行発令予定。DEC-037 スポット単価 A/B/C はオーナー決裁待ち。**残作業 = オーナー手動作業（Supabase Cloud / Stripe ダッシュボード / Vercel 環境変数 / β 顧客選定）**、所要 4〜6 時間+待機。1 セッションで M2-1 / M3 / M4 / M5 連続完遂（DEC-038〜047 = 10 連発決裁）。**累計 313 tests PASS / 22 ルート / migration 001-010 / 8 PDF テンプレ（Noto Sans CJK 埋め込み）/ Stripe 6 Webhook + Checkout + Portal + Cron + ハード制限 / Playwright 5 E2E + CI / coverage lib/ 主要 70-100%**。**MVP 計画 76 人日 → 実績 34 人日（45% 短縮、AI 組織並列化効果顕著）**。end-to-end 全フロー動作: signup → onboarding → 写真アップロード → AI 解析（Trigger.dev v3 or inline）→ preview → 編集（DEC-034 切替式）→ 4 状態承認 → 公開（QR + 公開 URL） → PDF 出力 8 テンプレ → 課金（Stripe Checkout / Portal / スポット購入）。**残作業 = オーナー作業のみ**（Supabase Cloud / Stripe ダッシュボード / Vercel 環境変数）。詳細手順は `app/DEPLOYMENT.md` 参照。DEC-001〜032 決裁記録完了。Supabase ローカル DB 検証通過後、M2-0 全 10 タスク完了：.gitattributes / Supabase CLI 2.95.2 固定 / Button Slot 化（DEC-028 残懸念②解消）/ shadcn 追加（dialog/dropdown-menu/select/sonner + UserMenu + 招待 Dialog）/ TanStack Query + lazy devtools / `/onboarding` 3-step stepper + Server Action / work_documents CRUD + `/works/new` 実接続 + dashboard 実データ + `/works/[id]/upload` placeholder / @vitest/coverage-v8 + テスト計 34 本 / typecheck・lint・test 34/34・build 12 ルート 全緑。料金戦略v2 / GTMv2 / Next.js16 組織標準反映 完了。累計 25 人日（MVP 76 人日の 33%）、残 51 人日 ÷ 14 週 = 3.6 人日/週ペース。**残作業：オーナー AI キー取得（Gemini + OpenAI 必須 / OpenAI は Verified 状態確認必須 / Vercel AI Gateway 任意）→ M2-1 AI 解析実装（8〜10 人日 = Gemini で写真→構造化抽出 + OpenAI で日本語校正 + gpt-image-2 で NG-OK 比較画像）→ M3/M4/M5**）

## 稼働中案件

| 案件ID | クライアント | 案件名 | Phase | 進捗 | 優先度 | 期限 | 備考 | ナレッジ抽出状況 |
|--------|------------|--------|-------|------|--------|------|------|------------------|
| COMPANY-WEBSITE | 自社 | 自社HP + 組織ダッシュボード | 完了 | 100% | 高 | 継続 | 全Phase完了・品質ゲート通過・本番稼働中 | v2抽出済 |
| PRJ-001 | 自社（社内ツール） | 紙袋3Dビューアー | Phase 3 完了 | 98% | 高 | 未定 | レビュー修正完了・デプロイ待ち | v2抽出済 |
| PRJ-002 | 自社プロダクト | PDF帳票OCR/VLMデータ自動抽出システム | Phase 5 完了 + API連携実装済 | 100% | 高 | - | 外部API連携（Phase A-D）実装完了・18ファイル追加・既存変更ゼロ | v1.5化済 |
| PRJ-003 | 自社プロダクト | 不定様式PDF VLMデータ抽出システム | Phase 3 開発完了 | 100% | 高 | 11週間計画 | 全83タスク実装完了・369テスト通過・本番デプロイ準備完了 | v1.5化済 |
| PRJ-004 | 自社プロダクト | いっしょびより（友達とお店共有iPhoneアプリ） | Phase 5 完了 | 100% | 高 | 未定 | Bloom&Bond完全実装・ダークモード対応済・FABメニュー改善・全レビュー修正済・TSゼロ・ビルドPASS | v1.5化済 |
| PRJ-005 | 自社プロダクト | AI帳票処理SaaS「カミレス」 | 運用中・品質改善継続 | 100% | 最高 | - | 本番稼働中・品質改善Phase A-D完了 | v2抽出済 |
| PRJ-006 | 自社プロダクト | カミレス デスクトップクライアント | Phase 1-2完了 | 100% | 高 | - | 54タスク完了・全機能実装・トレイ常駐・通知・自動更新・インストーラー | v1.5化済 |
| PRJ-007 | 自社プロダクト | PRJ-002 PDF帳票抽出 デスクトップ連携クライアント | Phase 1-2 全タスク完了 | 100% | 高 | MVP 3週間 | 全36タスク完了・署名キー設定済・リリースCI構築済・初回リリース準備完了 | v1.5化済 |
| PRJ-008 | 自社プロダクト | 伝承くん（退職者の業務知識をAIが継承するSaaS） | **保留（凍結）** | 5% | - | - | ヒアリング対象者不在により凍結。PRJ-009 クローズドβ時点で再評価。資産（架構ドラフト/競合調査）は保持（DEC-010） | 不要 |
| PRJ-009 | 自社プロダクト | **おはなしカルテ**（訪問看護記録自動生成SaaS） | Phase 4 プロト核心バリュー動作中 | 75% | **最高** | MVP 6-8週間 | 2026-04-10 起案・**プロダクト核心バリュー完成** (録音→写真→AI記録生成→編集→承認の end-to-end が全工程動作、モック実装で Gateway 未接続でも稼働)・15ルート全PASS・Storage bucket作成済・GitHub improver-work/ohanashi-karte 8126fbe・知人看護師ヒアリング連絡中 | 未抽出 |
| PRJ-010 | 自社プロダクト | **QuoteCraft**（多品種BtoB受注業向け 受注入力・見積作成 Agent Team） | Phase 0 実装中 | 25% | 高 | MVP 7週（上限8週） | 2026-04-15 起案+実装着手・**MVP入口=Webドラッグ&ドロップ(DEC-027)**・Slack/Teams は通知のみ・Design Partner=柏松園(IM=Slack+Teams併用,FAX主体)・app/雛形20ファイル生成(Next.js15+migration22+intake_channels+業種辞書seed+zodスキーマ4種+Claude clientラッパ+Pricing engine決定論+Audit sha256チェーン+Trigger.dev v4タスク+Slack Bolt+Teams Adaptive+Upload UI+CI)・DEC-001〜029 | 未抽出 |
| PRJ-011 | 自社 | **improver.jp コーポレートサイトリニューアル** | Phase 2 着手（UI/UX強化） | 90% | 高 | Phase2 4週 | 2026-04-16 Phase 1完了（Vercel本番稼働+Turnstile+Resend疎通OK）・4部門徹底調査の統合CEO提案 `ceo-phase2-uiux-final-proposal.md`＋4wide.jp実測分析 `phase2-4wide-analysis.md` 作成・**DEC-014〜021決裁済**（CEO推奨案全採用+4wide手触り3点セット=Lenis/Word-Reveal/Tactile Video輸入）・P0 Quick Wins(約5人日)→P1 Main Design(約19人日)・費用0〜80,000円・残: ドメイン切替(オーナー実施)/P0実装着手 | v2抽出済 |
| PRJ-012 | 自社（個人開発） | **Claude Code マルチプロジェクト IDE**（仮称 ccmux-ide-gui、v3.4 で Cursor 代替核心機能完成） | **v3.3 完了 + v3.4 DEC-034 完了（ファイルエディタ / @file mention / Git 統合、cargo test 105/105、/review Pass、dogfood Go 判定）** | 97% | 高 | 2ヶ月（Week0〜Week8、残 6 週） | 2026-04-17 起案・2026-04-18 **v1(Electron計画)→v2(Rust TUI で Week0+M1技術達成) →v3(Tauri+Next.js 再転換) を同日に完遂 + 実装着手 + Week 4 Must 3 Chunk 並列実装完了（43 ファイル）+ WSL2 実機検証で 5 緊急修正（tauri.conf bundle / turbopack off / sidecar external / findClaudeExecutable 強化 / JSDoc typo）→ **チャット送信 → Claude Max 応答到達 M1 実機合格**・残: 日本語入力は Windows native で根治（PM-115 build-windows.yml 運用開始）**・**Anthropic公式Claude Code Desktop 2026-04-14 リリース発見**で機能網羅を放棄、差別化4軸（日本語UI/おしゃれ/組織統合/ローカル永続化）局地戦に特化・**実機動作確認済**: Tauri Welcome→Setup(3択UI:Max OAuth/API Key/Skip)→Workspace→Claude Agent SDK v0.2.112+ 接続→**Max プラン(`~/.claude/.credentials.json` auto-detect)で応答streaming到達**・Rust資産流用85%（目標80%超）・fonts-noto-cjk+CJK fallback/Python stdlib icon生成/sidecar keep-alive修正/cwd+tsx絶対パスなど中継修正4件でTauri Linux WebKitGTK起動成立・DEC-021〜026決裁済・v1+v2+v3レポート5本+brief/proposal/tasks(PM-100〜312)/risks(Rv3-01〜10) v3刷新・**残課題**: WSL2 IME困難→Windows CI build(.github/workflows/build-windows.yml) 前倒し作成中、短期はコピペworkaroundで日本語入力・次アクション=Week3 PM-100〜110 (36h, M1基礎の60%: sidebar実接続/チャット充実/Zustand/永続化/framer-motion) + Windows CI 運用開始 | v2抽出済 |
| PRJ-013 | 自社プロダクト | **gpt-image-2 活用 AI画像生成・編集 WEBツール**（仮称 Enso、マーケ推奨） | **Phase 0 完了 → Phase 1 Conditional Go** | 15% | 高 | Phase 1 = 2週 | 2026-04-24 起案 + 同日 Phase 0 完了・**範囲指定 × テキスト指示 × 部分編集 を核心UX**・最高レベルのデザイン志向・成果物 10 本納品（brief / R-01/02/03 gpt-image-2 API仕様・競合8社・Konva+SAM2技術 / D-01/02 Neo-Studio × Quiet Luxury デザインコンセプト + トークン + 3ペイン UI / M-01 β SMB マーケター主戦場 + Enso 命名 + Free/Pro¥1,980/Team¥4,980 / P-01/02/03 統合計画+Phase1タスク60件+技術仕様 / Rev-01 Conditional Go）・DEC-001〜002 決裁済（M4 Before/After を MVP P0 格上げを CEO 即決）・**オーナー決裁待ち 5 件**: 主戦場 / 料金モデル / 命名 / OpenAI Tier / 透過PNG方針 | 未抽出 |
| PRJ-015 | 自社プロダクト（SaaS 化視野） | **Coatly**（テニス部予算管理Webアプリ、Phase 2 で他部活SaaS拡張） | **Phase 1 W3-A+B 完遂 / 5/12 β リリース待機**（main HEAD `a0bbe25` / Vercel `coatly-mu.vercel.app` 稼働中 / Turso 本番 migrate 済 / 247 tests / coverage 85.52% / DEC-001〜046） | 90% | 高 | **MVP リリース = 2026-05-12（確定、DEC-044）**（v2計画 7/3 → 5/12 短縮、月額運用 ¥0） | 2026-04-26 起案 + 同日 Phase 0 完了 + **同日 W1 〜 W3-A+B 完遂（DEC-001〜046、計46決裁、3 並列発令で 15 タスク 1 セッション圧縮）**。**Phase 1 W1 = Dev/Designer 並列で app/ 配下 68 ファイル scaffold + 全18レポート（research v1 / research-turso / design / pm / marketing / dev v1 / dev v2 / security / legal / acceptance / figma + review v1 / v2 + design-tokens完成形 + screen-mockups 6画面 ASCII + wow-elements 3点擬似コード + design-review-checklist）**。**Phase 1 W2-A/B/C = 認証 + 認可 + Phase 1 polish 完遂**: Better Auth + organization plugin + 三層認可（proxy + 4 guards + scopedXxx + ESLint）+ 活動費 CRUD + 領収証 R2 アップロード + 承認 FSM（draft→submitted→approved/rejected）+ 編集ページ + ダッシュボード 1 次。**【W2-C 認可ハード化 = DEC-041 / commit c28f8c2】**: Next.js 16 streaming-status 問題（parent layout が status 200 ヘッダを先送するため nested layout / page で `notFound()` を throw しても 200 のまま漏れる）を `experimental.authInterrupts: true` + `forbidden()` / `unauthorized()`（next/navigation, Next 16+）に統一して解消、`app/forbidden.tsx` / `app/unauthorized.tsx` 新規追加、E2E loginViaUI timeout 15s→30s。**【DB / CI 安定化 = DEC-036〜040】**: drizzle journal commit 漏れ恒久対策（DEC-036, commit `2755371`）/ tsconfig `ignoreDeprecations: "5.0"`（DEC-037, commit `9283c92`）/ peer dep MUST/NICE FIX 判定ルール化（DEC-038）/ `__drizzle_migrations` 履歴ズレを `migrate-recover.ts` で修復（DEC-039）/ 「別件」判断前に必ず `git diff` 確認ルール化（DEC-040, zod v4 issues rename を CI 失敗で発見、commit `3c7a4e6` で fix）。**スタック確定 v2**: Next.js 16.2.4 + Turso libSQL + Drizzle ORM 0.45.2 + drizzle-kit 0.31.4 + Better Auth 1.6.9 + Cloudflare R2 + Resend + Vercel Hobby（**月額¥0**）。Vercel 自動デプロイ稼働中（`coatly-mu.vercel.app`、commit `c28f8c2` deployed / db:up）。**SaaS化対応**: 単一org+prefecture enum → `organizations + groups` 汎用設計に格上げ。RLS 不在の代替 = 3層認可防衛（middleware + requireXxxRole guards + scopedXxx クエリビルダ + ESLint 生クエリ禁止 + マルチテナント漏洩 E2E 7本）。**Phase 1 MVP**: 41人日 / 9週、W1 + W2 + W3 = 全工程をコード完遂（AI 組織並列化、累積で計 88 人日相当を 1 日に圧縮）。**【W3-A+B 完遂 = DEC-045〜046 / commits b517f8c〜a0bbe25 計 15 件 push 済】**: drizzle-orm 0.45.2 / drizzle-kit 0.31.4 透過 upgrade（DEC-045）+ Turso 本番 migrate 済 + 3 並列発令（Dev-A 機能 / Dev-B オブザーバビリティ / Dev-C テスト+ドキュメント）= **rate limit 5/60s/IP / Resend 招待メール HTML+plain / アカウント退会（soft-delete + sole-owner ガード）/ magic-byte 検証（JPEG/PNG/PDF）/ privacy・terms 問合せ先 support@improver.jp 統一 / Lighthouse CI（5 ルート / Perf>=0.85 / A11y>=0.95）/ Sentry（server+client+edge）/ Vercel Analytics + Speed Insights / Mozilla Observatory 週次 cron / e2e ユーザ cleanup スクリプト / integration tests +20 / coverage 82.28% → 85.52% / README + RUNBOOK + DEPLOYMENT + ARCHITECTURE 4 docs（計 937 行）**。**残（オーナー手動 = 計 60 〜 90 分）**: Vercel env vars 投入（既存 14 + Sentry 4 + LHCI 1）/ Sentry プロジェクト作成 + DSN 取得 / **リリース直前** `pnpm db:cleanup-e2e -- --commit`（本番 DB から e2e ユーザ削除）/ UAT（オーナー実機シナリオ）/ KPT 振り返り。**5/12 まで残 16 営業日、開発側タスクはコード完遂、リリース待機状態**。 | 未抽出 |
| PRJ-017 | 自社プロダクト（社内向け SaaS 視野・公開β） | **ホメコト（HOMEKOTO、確定 DEC-011）— 社内褒め活推進Webアプリ** — 同僚への感謝・「すごい」を投稿し合うゲーミフィケーション × 月次 AI 演出 Web アプリ | **Phase 1 W4+++ PraiseComposer mutation 統合 + glyph ロゴ + Apple Touch Icon 完遂（DEC-036、useCreatePraiseMutation で query invalidation 自動化 + isPending を SSOT 化 + SubmitSpark 小規模 firework + 送信ボタン 3 状態 SpinnerInline/SuccessCheckmark / public/logo-glyph.svg「ホ」字翼展開 96×96 + Next.js 16 app/icon.tsx 32×32 + app/apple-icon.tsx 180×180 で sharp/puppeteer 不要の動的 PNG 生成、next build /icon /apple-icon ○ Static 確認）+ DEC-035 W4+++ デモ動画モーダル投入（LP Hero secondary CTA を `デモを見る` に置換 → Radix Dialog ベース 3 フェーズ自動再生モーダル）+ DEC-034 W4++ マイクロインタラクション統合（Ripple 波紋 hook + RippleLayer / CountUpFlip / PraiseCard 反応リップル + 楽観的更新 + アイコンモーフ outline→solid / PraiseList AnimatePresence mode=popLayout）+ DEC-033 W4+ デザイン/アニメ最高峰化 + DEC-032 W4 完遂 / typecheck 0 / lint 0 / test 292/292 PASS（DEC-035 289 → +3 = +1.0%）/ next build 全 28 ルート緑 / Phase 1 完遂検収に向け残 5 オーナー側手動セットアップ（R2 / Resend / Microsoft OAuth / Sentry / Vercel デプロイ + DB push）+ W4+++ 持越（招待マイクロアニメ / Lottie 実アセット / --run-real-cron Vercel Preview / LP OG image 動的化）** | 78% | 高 | Phase 1 MVP = 10週（50〜70 人日、中央値 62）/ 公開β募集開始 = W6 / 公開β運用開始 = 7 月 | 2026-04-26 起案 + 同日 **Phase 0 全部署完遂 + オーナー Q-01〜Q-07 決裁 + Critical 3 訂正 + W1 4 部署並列実装完遂を 1 セッションで圧縮達成**（PRJ-015/016 と同等以上速度）。**確定: スタック = Next.js 16.2.4（CVE-2025-66478 対応 DEC-018） + React 19 + TS strict + Tailwind v4 + shadcn 風 base 7 + Heroicons + Geist + next-themes + Framer Motion + canvas-confetti + tsparticles + lottie-react + Supabase（Tokyo）16 テーブル全表 RLS + Auth 3 経路（Google/Microsoft/Email）+ proxy.ts + 3 ガード + AI SDK + OpenAI gpt-5-mini（fallback gpt-4.1-mini 環境変数切替 DEC-021）+ tts-1（6 voice 確定 DEC-020、Phase 0 13 voice 誤記訂正、月¥4.8）+ Vercel Hobby→Pro / 動画生成 API は Phase 1 不採用 DEC-012 / 月額 ¥200 程度（50 名、DEC-004 真値）** / **ブランディング = ホメコト（HOMEKOTO）DEC-011 / ドメイン homekoto.app 第一候補（J-PlatPat/WHOIS は Research W1 で手順策定、実機確認はオーナー or W2）/ マスコット 3 案 = ホタル / ヒカリ / コトリ、CEO 推奨「ホタル」DEC-019（Q-05 オーナー判断待ち）** / **NSM = 月次会議発表モード実施回数 × 同会議参加率（DEC-014）/ MAP は補助 KPI** / **アニメ N=23 種 / 月次発表演出 M=14 種（14 ヶ月ローテで重複ゼロ保証）DEC-013** / **AI 三層ガード = Moderation + 出力 zod 厳密 + 公開前承認** / **受入基準 = W4 16 + W8 28 + W10 38 = 計 82 項目** / **公開β規模 = 30〜50 社（DEC-014）/ 4 法務文書 = 利用規約 + プラポリ 14 章 + AI 利用同意 + 退職者対応手順、W5 弁護士レビュー必須 DEC-022** / **1 行コピー = 「会議が、待ち遠しくなる。」（DEC-023 推奨）** / **市場 = 国内 HR Tech 924 億円 17% 成長、ピアセレブレーション新カテゴリ宣言、ICP = 30〜100 名 IT/Web/制作系（TAM 約 19 億円）** / **Phase 1 完全無料運用、Phase 2 で ¥0/¥9,800/¥29,800/¥79,800 4 段階課金（Unipos 半額帯）/ LTV/CAC 310 倍試算**。**【W1 完遂内訳】**: Dev = **61 ファイル新規 scaffold + Vitest 17 PASS + Playwright 2 spec + CI 4 jobs + アニメ 5 種 PoC（紙吹雪/ハート/スポットライト/星空/バッジ）+ 月次発表 PoC（selectTop3 三重ガード + ニュース演出 + tts-1 mock）+ Vercel Cron** / Designer = **6 レポート計 5,216 行**（design-w1-tokens.md 901 行 = Warm Pop 確定 + WCAG AA 全組合せ証明 + Light/Dark/CVD / design-w1-mascot.md 466 行 / design-w1-screens.md 1,539 行 = 6 画面 desktop+mobile / design-w1-post-animations.md 803 行 = アニメ 23 種 + 重み付き選出 / design-w1-monthly-shows.md 950 行 = 演出 14 種 + 14 ヶ月ローテ / design-w1-microcopy.md 557 行 = コピー 80 例、要件 60 を超過）/ Research = **5 レポート計 2,021 行**（research-w1-llm.md 443 行 / research-w1-tts.md 358 行 / research-w1-trademark-domain.md 354 行 / research-w1-animation-libs.md 391 行 / research-w1-legal-beta.md 475 行）/ Marketing = **4 レポート**（marketing-w1-beta-launch.md = 30〜50 社募集設計 + 6 チャネル + LP 10 セクション / marketing-w1-gtm-timeline.md = 20 週設計 / marketing-w1-messaging.md = 1 行コピー 10 案 / marketing-w1-competitive.md = 競合 7 社比較 + ピアセレブレーション新カテゴリ）。**残（W2 着手前）**: オーナー Q-05 マスコット選定 + Research の J-PlatPat / WHOIS 実機確認（次セッション or オーナー）+ Supabase Cloud DB 作成 + OpenAI/TTS API キー設定 + Vercel 環境変数 → W2 = アニメ +18 種 / 演出 +13 種 / 投稿フル機能 / 受信 DB 連携 / 月次発表ステージ本体 / Cron 本実装 / 管理画面中身 / E2E 拡充 / Sentry + Vercel Analytics / shadcn CLI 切替 / TanStack Query 導入。 | 未抽出 |
| PRJ-016 | 自社プロダクト（B2C EdTech / 個人開発・無料運用） | **HANEI（ハンエイ / 半英）— 小学生向け英語学習Webアプリ**「英検5級〜3級 / 半年で英検3級合格を目指す / AIコーチ「ことだまトリ」+ ゲーミフィケーション / 無料」 | **W5 完遂検収（DEC-037）**: 5 営業日計画 → 1 セッション完遂 / G-1〜G-6 全実装 / typecheck 0 / lint 0 / unit test 157 PASS（+19）/ E2E study-smoke 1 PASS（23.4s）/ home 実データ化 + 5 級進捗バー 4 スキル + listening TTS 再生 + ことだまトリ 5 mood + 出題フィルタ + 通し E2E。副次承認: Better Auth nextCookies plugin / db-fixture catch 拡張 / schema 分離 4 ファイル。CEO trust-but-verify で globalSetup duplicate column 構造バグを発見・恒久修正。**DEC-001〜037 計 37 決裁** | 95% | 高 | Phase 1 MVP = 10週（料金実装削除で 48.5→約42 人日に縮小） | 2026-04-26 起案 + **同日 Phase 0 全部署完遂（Research/Designer/PM/Marketing/Dev/Review 並列）**。**6本レポート納品（research / design / pm / marketing / dev / review-gate）**。**DEC-003〜009 計7決裁**。確定: **スタック = Next.js 16 + Turso libSQL + Drizzle + Better Auth + Cloudflare R2 + AI SDK + OpenAI gpt-5-mini（主軸）/ gpt-4.1-mini（フォールバック）+ OpenAI tts-1 + Vercel Hobby（β）→ Pro（公開β）/ 月¥1,200（β100名）→¥24,000（公開500名）** / **ブランディング = HANEI（仮確定）+ Amber Gold #F2A93A + 「ことだまトリ」マスコット** / **訴求 = 「半年で英検3級合格を目指す」（景表法配慮）** / **課金 = Phase 1 完全無料 / Phase 2 で 無料/STANDARD ¥1,980/GOAL ¥2,980 + 半年合格保証** / **問題プール Phase 1 = 1,600問（5級400+4級500+3級700）+ 外注予算¥30〜50万** / **適応学習 = FSRS + IRT 1PL + BKT 4パラメータ ハイブリッド** / **AI 三重ガード = Moderation + System Prompt + 出力フィルタ** / **受入基準 = W4 12項目 + W8 22項目 + W10 34項目 = 計68項目**（子ども向け固有 K-1〜K-6 = タップ56/48px / 文字16-18px+ふりがな / 保護者同意E2E / NGワード100語+人手レビュー違反0 / 応答P95 3秒以内 / AIコスト¥10/人/日以下）。**【DEC-029 = 2026-04-27 / R2 命名ミスマッチ修正】**: `.env.local` の split 命名（`R2_AUDIO_BUCKET` / `R2_S3_ENDPOINT` / `R2_AUDIO_PUBLIC_URL`）と `r2.ts` の single 命名（`R2_BUCKET_NAME` / `R2_PUBLIC_URL`）がミスマッチで TTS 600 件全 401 Unauthorized → OpenAI tts-1 ¥42 空課金発生。`r2.ts` を split→single→default のフォールバック対応 + `pingR2()` smoke check（HeadBucket）追加 + `generate-tts-w3.ts` main 先頭で R2 認可検証してから OpenAI 呼ぶ構造に改修。typecheck ✅ exit=0。**【DEC-030 = 2026-04-27 / R2 API Token スコープ問題切り分け】**: pingR2 自体が 401 で失敗 → API Token 側の問題確定。`scripts/r2-debug.ts`（HeadBucket → ListObjectsV2 → HeadObject → PutObject(1byte) → GetObject → DeleteObject の 6 ステップ自動診断）導入。オーナーが「Profile / Account API tokens」（Bearer 専用）と「R2 / Manage R2 API Tokens」（S3 互換）を取り違えていたため、後者で再発行し `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` を更新 → **TTS 480 件 / 18,834 chars / ¥42.38 完走**。**【DEC-031 = 2026-04-27 / 解説バルク B プラン段階生成】**: `npm run ai:generate-explanations` 完了報告 → CEO `db:stats`（読み取り専用 / ¥0）で検証したところ 822 中 44 件のみ insert（5.4%）。`ADMIN_COST_CEILING_JPY=500` で中断 + 1 件あたり実コスト ¥11.36（見積もり ¥0.5 の 22 倍 = gpt-5-mini reasoning_tokens 推定）。**オーナー判断 = B プラン採用「MVP で必要なものをしっかり用意して開発を優先」**。`generate-explanations-w3.ts` に `LEVEL_FILTER` / `SKILL_FILTER` 環境変数 + `package.json` に 11 本のサブスクリプト（`:v5-vocab` 〜 `:v3-writing`）追加。**stage 1 = 5 級 vocab 160 問**（残 ~116 件 × ¥11.36 ≒ ¥1,318）を `ADMIN_COST_CEILING_JPY=2000` で完走させ MVP day1 体験の最小単位を確保。typecheck ✅ exit=0。**残（W1 着手前ブロッカー）**: オーナー軽承認 A-01〜A-05（命名最終確定 / Vercel Pro移行タイミング / 外注予算 / 半年合格保証Phase 2発動 / β募集チャネル）+ Research の gpt-5-mini 実API疎通 + J-PlatPat商標確認 + ドメイン取得 + PM/Dev のWBS再見積 + **オーナー解説 stage 1（5 級 vocab）実走**。**180日カリキュラム骨子**: Day1-30 = 5級定着（語彙600 + 基本文型）/ Day31-90 = 4級定着（+700語 / 文法 / リスニング）/ Day91-150 = 3級本番演習（+800語 / 過去問形式 / ライティング25-35語）/ Day151-180 = 模試 + 面接対策。 | 未抽出 |
| PRJ-014 | 自社プロダクト | **GENBA Visual**（製造業・物流・現場業務向け 現場の知識見える化 Webアプリ） | **β-prep フェーズ（DEC-048）** — MVP 完成（DEC-047）+ オーナー手順書 `reports/owner-beta-release-checklist.md` 作成済 + DEPLOYMENT.md 整合化済 / オーナー手動作業待ち（Supabase Cloud + Stripe + Vercel + DEC-037 決裁 + β 顧客選定） | **100%（実装）/ オーナー作業待ち** | 高 | **MVP 完成 = 2026-04-25**（計画 76 人日 → 実績 34 人日、45% 短縮）| Phase 0完了+M1基盤+Phase A(マイグレ001〜004の22テーブル+8テンプレseed+TS型自動生成採用+UI5画面)+実DB検証(10コンテナhealthy/signup E2E成功/RLS循環解決実機確認、commit 19de587)+**M2-0完了(commit ac4a3b1, 71→新規ファイル)**：.gitattributes/Supabase CLI 2.95.2固定/Button asChild→@radix-ui/react-slot化(DEC-028残懸念②解消)/shadcn追加(dialog/dropdown-menu/select/sonner)+UserMenu+招待Dialog/TanStack Query+lazy devtools/`/onboarding` 3-step stepper+Server Action+`createOnboardingOrganization`/work_documents CRUD Server Actions+`/works/new`実接続+dashboard実データ+`/works/[id]/upload` placeholder/@vitest/coverage-v8+テスト33本新規(計34本)/typecheck+lint+test 34/34+build 12ルート全緑・Next.js16.2.4+React19+TSstrict+Tailwindv4+shadcn base-nova+Supabase CLIローカル(DEC-012)・認証+requireAuth()+RPC create_organization_on_signup(security definer)+handle_new_user trigger・usage_meters(plan_quota+spot_credits+consumed分離/DEC-018)・view_logs+acknowledgements(DEC-020 A1/A2)・**DEC-001〜047決裁完了（〜DEC-043=M2-1完了 / DEC-044〜045=M3着手+完了 / DEC-046=M4完了+M5着手 / DEC-047=【MVP 完成宣言+β リリース判定 GO】）**・料金戦略v2確定(Free/Standard¥79,800/Pro¥149,800、粗利82.8%/89.6%、損益分岐23社、年商目標6,600万円)・マーケGTMv2・Next.js16組織標準反映・**累計25人日(MVP76人日の33%)**・**カバレッジ9.77%(lib/actions 49.43%) → Gate6 70%目標は M5 で E2E+Pageテストにより到達計画**・**次のオーナー作業=AI APIキー取得**(Gemini/Claude必須+Vercel AI Gateway任意) → M2-1(8〜10人日 = 写真アップロード/Gemini 2.5構造化抽出+OpenAIテキスト校正+gpt-image-2 NG-OK画像生成/Trigger.dev v3/プレビュー/SSE)・申し送り5件:Storage multipart/migration 005でindustry+employee_size追加/api_keys暗号化+BYOK/承認UI/Playwright E2E | v2抽出済 |
| PRJ-018 | 自社（個人開発） | **Asagi（浅葱）— Codex マルチプロジェクト IDE** | **【Phase 1（M1 Real impl）着手承認済 — DEC-018-032 / 2026-05-02、オーナー明示承認「OK Phase 1 進めて」】Phase 0 POC 5/5 全 PASS（#1 OAuth / #2 initialize 158ms / #3 1 ターンチャット 6.99s / #4 画像入力 7.34s / #5 JobObject Win11 KILL_ON_JOB_CLOSE）+ DEC-018-033 で Codex CLI 0.128.0 起動契約 5 点を `codex_sidecar/contract.rs` 定数として codify 確定（`stdio://` URL 必須 / 実体 codex.exe spawn / `thread/started` notification polling / `agentMessage`+`item.text` / 画像 field `url`）→ Blocked 4 → 0 全解放、AS-140 工数 6h → 4〜6h 下方修正、M1 残 47.5〜49.5h（約 2.5〜3 営業日）。**Phase 0 POC 待機 解消** + v0.1.0 雛形 Done + シェル Done + UX 強化 Done + ロゴ γ 浅葱滴 採用確定 + 品質基盤 In Progress + Codex sidecar 足場 Done + mock API surface Real-Codex 完全準拠 rename Done (DEC-018-023 / AS-136) + MAX_CONCURRENT_SIDECARS = 6 (DEC-018-025 / AS-137) + AS-144 / AS-145 mock Done + codex-schema-watch CI 雛形 Done (AS-Q-03) + **ChatPane mock UX Cursor 級到達 Done (DEC-018-026 ① / AS-146〜150、typing indicator + token estimator + interrupt button + UX polish + zustand v5 fix、commit `70170d2`、Playwright @codex-mock-ux 5/5 + vitest token-estimator 5 件 pass)** + **AS-DESIGN-04 Done (DEC-018-026 ② SVG マスター 5 種 + multi-size PNG/ICO/ICNS 全件確認、commit `b5157a0`)** + **AS-META-09 Done (DEC-018-031、`tauri.conf.json` bundle.icon 5 PNG 列挙 + `layout.tsx` Metadata favicon 16/32 + apple-touch-icon 180 + Viewport.themeColor light/dark、DoD 16/16 充足、commits `b5157a0` + parent `12f57a0`)** + **M2 ロードマップ確立 (DEC-018-027〜030、F1〜F8 計 11 日、`M2-ROADMAP.md`)** + DEC-018-024 起票予約（bundle target、M1 末再決裁）（DEC-018-014/017/019/021/022/023/024/025/026/027/028/029/030/031 ハイブリッド運用） | 70% | 高 | 直近: Phase 0 POC 5項目（オーナー実機）／ M1 4週／M3 8週（POC 通過後確定） | PRJ-012 Sumi の Codex 版。ChatGPT Codex x5 サブスクで動く独立デスクトップ IDE。Sumi と完全独立設計・独立リポジトリ（**hironori-oi/Asagi private 確定 DEC-018-013**）・共通コード資産なし（DEC-018-002/008）。Tauri 2 + Next.js 15 + Codex CLI sidecar、accent 浅葱色 oklch(0.72 0.10 200)。確定 DEC-018-001〜021（プロダクト名 Asagi / Sumi 完全独立 / ChatGPT サブスク + Codex CLI sidecar 認証 / Codex x5 Must / 即着手 / 差別化4軸全踏襲 / 機能追加除外なし / コード流用なし / `codex app-server` JSON-RPC 採用 / Phase 0 POC 必須ゲート / RAs-11 中→低 / Node sidecar 撤廃 / DEC-018-013 リポ確定 / DEC-018-014 ハイブリッド運用 / DEC-018-015 ロゴ 3 案画像化 / DEC-018-016 ロゴ v1 全案却下 + v2 方針 / DEC-018-017 シェル AS-107〜113 並列着手承認 / DEC-018-018 ロゴ v2 全案保留 → v3 再起案（stitch MCP 活用、案 C 中心、趣方針）/ DEC-018-019 AS-114〜123 UX 強化並列着手承認 / **DEC-018-020 ロゴ v1 公式採用「γ 浅葱滴（あさぎしずく）」確定（CEO 推奨 → オーナー承認、`logo-v3-gamma-asagi-drop.png`、Sumi 橙滴と完全鏡像構造の兄弟感最強・趣 5 原則全網羅）／ DEC-018-021 AS-META-01〜10 品質基盤・運用基盤（CI / vitest / Playwright / docs / 設定永続化 / エラーバウンダリ / a11y / アイコン組込）並列着手承認**）。直近達成: リサーチ v1 完了 ／ ブランド設計 v1 完了 + tokens.css 実装 ／ Phase 0 POC スクリプト 5本完成（オーナー実機実行待ち）／ 2026-05-02 リポ確定 + CEO クローン完了 ／ 2026-05-02 (second update) v0.1.0 雛形 Done（コミット `cf098bd` ローカル保留）、ロゴ v1 全案却下 → v2 再起案 ／ 2026-05-02 (third update) シェル実装 Done（コミット `cdaadc6` ローカル保留）、ロゴ v2 全案保留 → v3 再起案、AS-114〜123 UX 完成度強化進行 ／ **2026-05-02 (fourth update) UX 強化完遂（AS-114〜119d / AS-CMT-UX、コミット `acf5f70` ローカル保留・push 待ち）、ロゴ v3 5 案中 γ 浅葱滴 を Asagi 正式アプリアイコンに確定（DEC-018-020）、ロゴ γ 採用確定、SVG 化＋multi-size 展開（ライト/ダーク変種含む、AS-DESIGN-04）、CI/テスト/docs 整備中（AS-META-01〜10、DEC-018-021）。Codex sidecar 統合系 Real impl (AS-140〜145) は POC ゲート通過後に着手、約 23h ブロック残（足場活用で本番接続は予測 4〜6h）**。 ／ **2026-05-02 (fifth update) DEC-018-022 起票 + AS-130〜135 完了（mock-first sidecar 足場、commit cff4ded、cargo test 18 passed including JobObject Win11 kill / tsc / next build pass）。codex_sidecar trait + Real/Mock factory + JSON-RPC 2.0 protocol + mock_codex_app_server bin + WinJobObject + MultiSidecarManager + TS bridge + /dev/codex-mock dev page。ASAGI_SIDECAR_MODE=mock|real 環境変数で切替、dev 既定 mock。POC 通過の瞬間に Real impl 即起動可能な発射台完成。M1 Done 比率 47% → 53%、全体 38% → 51%** ／ **2026-05-02 (sixth update) DEC-018-023 / 024 / 025 起票 + AS-136 / AS-137 / AS-144 mock / AS-145 mock / AS-Q-03 完了。mock API surface を Real Codex-app-server 完全準拠に rename（method `codex/chat`→`turn/start`、event `codex/event/*`→`agent:*:item/agentMessage/delta`、handshake `initialize`→`initialized` 2-phase）。MAX_CONCURRENT_SIDECARS = 8→6（research v2 で発見の 3rd-party OAuth 429 quota 軽減）。ChatPane を `use-codex` hook 経由で MultiSidecarManager に接続（mock 経由で実機動作可）。Playwright for Tauri E2E mock 1 シナリオ pass（spawn → message → done → shutdown）。codex-schema-watch GitHub Actions 雛形（weekly cron で `codex app-server generate-ts` diff → Issue 自動起票、RAs-14 軽減策 #1 自動化）。risks.md v1.2 で RAs-14 中の上→中 格下げ + RAs-15（bundle target 未決、DEC-018-024 候補）/ RAs-16（Pro 5x プラン名 / planType 未確定、POC #3 ログ）/ RAs-17（3rd-party OAuth 429、DEC-018-025 で軽減済）新規追加。M1 Done 31→36（Done 比率 53%→60%）、Blocked 6→5、total 58→60、全体進捗 51%→60%。M1 DoD 充足率 57.5%→60.6%、POC 通過後 M1 完了見通し 4〜6 営業日→3〜4 営業日に短縮（mock 範囲 + rename 事前完遂効果）。コミット累積 5 件→7 件想定（Dev 完了報告で確定）**。／ **2026-05-02 (seventh update) DEC-018-026〜031 反映 + AS-146〜150 / AS-DESIGN-04 / AS-META-09 完遂。ChatPane mock UX が Cursor 級到達（typing indicator + token estimator + interrupt button + UX polish + zustand v5 fix、commit `70170d2`、20 files +1048/-108、cargo 26/26 + vitest 29/29 + Playwright @codex-mock-ux 5/5 pass）。AS-DESIGN-04 完遂（γ 浅葱滴 SVG マスター 5 種 + PNG 8 サイズ + ICO 2 種 + ICNS 1 全件確認、commit `b5157a0`）。AS-META-09 完遂（DEC-018-031、`tauri.conf.json` bundle.icon 5 PNG 列挙 + `layout.tsx` Metadata に PNG favicon 16/32 + apple-touch-icon 180 + Viewport.themeColor light/dark 追加、DoD 16/16 全充足、commits `b5157a0` + parent `12f57a0`）。M2 安定化フェーズロードマップ確立（DEC-018-027〜030、F1 Outer Retry Layer / F2 Rate Limit Dashboard / F3 Auth Watchdog / F4 Thread Lifecycle Manager / F5 Image Preprocessor / F6 Multi-Sidecar Token Bucket / F7 User Notification System / F8 Reconnection、Must 4 + Should 3 + Could 1 = 11 日、Quick Win 順序 = QW1 F3 1.0d → QW2 F1 1.5d → QW3 F4 idle 0.5d ＝ 3.0d、別ファイル `M2-ROADMAP.md`）。risks.md v1.3 で RAs-12「重」→「中」再下降（DEC-018-029 F2 mitigation）+ RAs-18（CLI 内部 retry 不透明性、F1 で軽減）/ RAs-19（thread/resume context truncation、F4 + Asagi SQLite で軽減）新規追加。M1 Done 36→43（Done 比率 60%→66%）、Blocked 5→4（AS-META-09 解除）、In Progress 11→9、total 60→65、全体進捗 60%→約 70%。コミット累積 7 件→asagi-app 内 8 件以上 + parent claude-code-company `12f57a0`**。**2026-05-02 (eighth update) Phase 0 POC 5/5 PASS 完全達成 → Phase 1（M1 Real impl）着手承認**（DEC-018-032 / DEC-018-033、`reports/poc-phase0-result.md` 参照）。POC 実機検証で Codex CLI 0.128.0 schema 5 点が判明 → contract.rs 定数として M1 Real impl に物理的に組み込む方針確定。Blocked AS-110/111/112/120/121/124/140〜143 全 10 タスク Ready 化。AS-140 工数 6h → 4〜6h（contract codify と足場 AS-130〜137 の合成効果で約 8〜10h 短縮）。RAs-12 (JobObject) 完全解消、RAs-14 (schema 変動) 「中→低」再格下げ、U2/U5/U8 解消。次 DEC は AS-140 完了時の DEC-018-034 想定（Real impl 実機検証結果 + DEC-018-024 bundle target 確定） | 未抽出 |
| PRJ-019 | 自社（個人開発） | **Clawbridge（仮称）— Open Claw を自律オーナーとする AI 組織ハーネス基盤** | **Phase 1 着手準備（W0-Week2 ブートストラップ並列成果着地、2026-05-03 + DEC-019-021〜025 即決 + SOP 制定 + 秘書 3 件配布資料整備）— DEC-019-007（強い条件付き Go）+ DEC-019-009〜025 / 2026-05-03 W0-Week2 prep 連結報告完成（`reports/ceo-w0-week2-prep-consolidation.md`）、Dev: 95 tests 全緑（harness 38 + claude-bridge 29 + tos_gray 11 + openclaw-runtime 6 + mock-claude 5 + time-source 11、Windows 11 / Node 24.11.1 / pnpm 9.12.0）+ 必須コントロール 28→34 項目（HITL 第 6 種 `tos_gray_review` / 第 7 種 `external_api` / G-Top-1〜4 / H-09 / H-10 / G-01〜12 / G-V2-01〜11 / C-A-01〜05）+ architecture-w0.md / security-w0.md (Mermaid 6 枚) / Research: P-D 改 resilient 一次解釈確定（OpenClaw OSS 上流 personal AI assistant 化判定、driver vs engine 分離） + R-019-12 リスク再格付け（赤→黄 + A 赤 / B 黄 分割） + 4 系統 changelog 監視 Runbook（Slack + メール 3 段階 / L3 で 24h 自動 pause）/ PM v4: Phase 1 追加発生 中央値 $33 / 上限 $93、月次ハードキャップ $300 維持 + W0-Week2 実行計画 29 タスク Critical Path 5 ステップ + 5/15 競合解消 (AS-151 を 5/16 スライド) / Marketing: Phase 1 完了時 自社 HP / 社内ナレッジ K1〜K10 反映設計（Heading A / 公開 6/20 / 部分開示 / harness 40% / org 25% / cost 20% / ToS 15%）** | 50% | 高 | W1 着手 2026-05-19 / W4 完了 2026-06-13（4 週間）、PRJ-018 M1 並走、月次予算 $300（追加発生分上限として再定義）、BAN リスク承認済（オプション A により Sumi/Asagi 巻き添え影響範囲拡大も受容） | 2026-05-02 Phase 0 完了 + Phase 1 着手承認 (DEC-019-007、強い条件付き Go)。**Phase 0 成果物 7 ファイル整備完了**: research × 2 (`research-openclaw-harness-investigation.md` 徹底調査 + `research-supplement-tos-and-subscription-paths.md` 補追 ToS / サブスク 7 経路 P-A〜P-G 評価) / pm × 2 (`pm-requirements-and-architecture.md` 要件 v1 + `pm-architecture-v2-and-phase1-plan.md` v2 アーキ + Phase 1 ディテール WBS) / review × 2 (`review-security-and-risk-assessment.md` v1 API キー前提 + `review-v2-subscription-risk-and-fallback.md` v2 サブスク駆動採用版、強い条件付き Go) / secretary × 1。**CEO 決裁 4 件発行 (2026-05-02)**: DEC-019-005 (OQ-01〜05 オーナー判断全面受容: Codex「x5」=ChatGPT Pro $100 の Plus 比 5x usage tier、Claude Code はサブスク駆動 (API キー従量不採用)、OpenAI ToS オーナー直接確認、自前ハーネス確定 (Devin/OpenHands 不採用)、PRJ-018 並走) / DEC-019-006 (P-D 改採用 = オーナー本人 PC + 公式 Claude Code CLI 常駐 OAuth + Open Claw が `claude -p ... --output-format stream-json` で subprocess spawn、Open Claw は絶対に Anthropic API 直叩き不可、別 Anthropic アカウント分離は NG-2 連鎖 BAN 回避優先で不採用、FS/env 隔離 G-V2-11 + P-E (API キー) 即時 fallback で代替) / DEC-019-007 (Phase 1 強い条件付き Go: 4 週間 5/19-6/13、月次予算ハードキャップ $300/月 = Claude Max $200 + Codex Pro $100、DoD = HN trending → /new-project 起票 → Next.js 雛形 → Vercel Sandbox テスト → Review 合格 → preview deploy → Slack 通知 完全自動 / <60min/件 / <$5/件 / 10 連続成功率 ≥80% / 既存 PRJ-001〜018 副作用 0 行、必須コントロール 23 項目中 21 項目を着手前クリア / 残 2 項目 (G-V2-06 rate jittering / G-V2-10 ToS 半年再評価) は W1 進行中整備、BAN リスク受容: 12 ヶ月以内 30〜60% / 損失レンジ ¥500k〜¥2M をオーナー明示承認) / DEC-019-008 (NG-3 24/7 連続自律稼働回避方針 CEO 暫定値: 1 日連続稼働上限 12 h/日 + API 換算費用 $1,000/月相当超過で自動停止 / W2 終了時 2026-05-30 にオーナー再確認、本決定変更可能、根拠 = リサーチ §2.7 NG-3 「ordinary individual usage」逸脱 + Boris Cherny 公式コメント)。**W0 準備期間タスク 12 件起票** (2026-05-02〜2026-05-18): CB-O-01 OpenAI Service Terms オーナー直接確認 (最優先) / CB-O-02 Anthropic Max $200 契約 / CB-O-03 ChatGPT Pro $100 (Codex 5x) 契約確認 / CB-O-04 Vercel Sandbox 利用設定 / CB-O-05 1Password Vault 4 系統 (`Clawbridge-Master/Dev/Notify/Public`) / CB-D-01 clawbridge 専用 GitHub リポ作成 + branch protection / CB-D-02 clawbridge 専用 Vercel project / CB-D-03 clawbridge 専用 Supabase 監査基盤 / CB-D-04 OAuth トークン到達禁止 FS/env 隔離方針 (Windows 11 + WSL2 + AppArmor/TCC) / CB-PM-01 W1 キックオフ計画書 / CB-S-W0-01 W0 完了レビュー + W1 着手 Go/NoGo / CB-SC-W0-01 dashboard 反映 + PRJ-018 並走対照表整備。**W1〜W4 タスク 41 件 WBS 確定** (PM v2 §3.3 準拠): W1 ハードガード前倒し (G-01/G-04/G-05/G-06/G-08/G-V2-11) / W2 監視・隔離 + tos_monitor (G-02/G-03'/G-07/G-09/G-10/G-V2-08 + Plan B-1 P-E フォールバック整備) + W2 終了時 CB-O-W2-01 で DEC-019-008 再確認 / W3 ニーズ判定 + 公開ガード (G-11) + 残存 G-V2-06 整備 / W4 副作用ゼロ証明 (G-12) + ベンチマーク 10 連続実行 + Phase 1 完了レポート + Phase 2 Go/NoGo 判定資料。**新規リスク 4 件追加**: R-019-06 (BAN 確率 30〜60%/12ヶ月、スコア 9 最重、緩和=P-D 改 + 必須コントロール 23 + P-E 即時 fallback + Anthropic 警告メール 1h 監視) / R-019-07 (Codex 2x プロモボーナス 2026-05-31 終了で W3〜W4 実質枠半減、W2 終了時 5x 枠でベンチマーク完遂可否を再見積) / R-019-08 (PRJ-018 M1 並走リソース食合い、PM v2 §3.4.1 配分マトリクス: W1 Dev 50%/40%/10% Review 30%/60%/10%、W4 Dev 70%/20%/10% Review 60%/30%/10% で PRJ-019 終盤集中、秘書部門が dashboard で稼働時間競合週を週次対照) / R-019-09 (NG-3 12h/日とオーナー要望「人間不在の完全自動化」一部不整合、W2 終了時 CB-O-W2-01 でオーナー再確認)。**接続方式 P-D 改の核**: Open Claw は絶対に Anthropic API 直叩きせず、公式 Claude Code CLI を本人 OAuth で常駐 + subprocess spawn のみ、streaming classifier から正規 Claude Code session として認識される構造。**月次予算 $300 ハードキャップ + BAN リスク承認済 + 必須コントロール 21/23 着手前クリア**。実装は `projects/PRJ-019/app/` 配下、PRJ-018 Asagi M1 (Real impl 約 5〜7 営業日見込、AS-140 着手 Ready) と並走、共通基盤 (GitHub/Vercel/Supabase) は完全分離、Codex Pro $200 アカウントのみ 5h ローリング窓共有で cost_check 監視。**【2026-05-03 W0-Week1 連結報告 = DEC-019-014〜017 起票 + 同日 W0-Week2 前倒し並列発注 = DEC-019-018〜020 起票】**: DEC-019-014（W0-Week1 進捗承認、Dev/Research/Review 3 部署並列発注成果着地）/ DEC-019-015（H-09 Claude Max weekly cap 監視 80% 警告 / 95% 自動 pause + H-10 extra usage 課金 Phase 1 原則 OFF）/ DEC-019-016（Vercel コスト上限 上方修正 控えめ $0 / 中央値 $20 / 上限 $46 = Sandbox $26 + Hosting Pro $20）/ DEC-019-017（Hobby→Pro 昇格判断を W3 中盤 6/3 頃 CEO 決裁に格上げ）/ **DEC-019-018**（ToS allowlist DoD 統合 v1 承認 + HITL 第 6 種 `tos_gray_review` 追加を Dev W0-Week2 着手必須要件として発令、whitelist confidence ≥ 0.85 で preview deploy 自動公開・本番は HITL 必須・FN-Black ≤ 10% 評価ルール採用）/ **DEC-019-019**（BAN drill #1 5/13 シナリオ承認 = ハッピー + 異常 5 種、5 SLA 全達成で Pass、Fail で再 drill 3 日以内、再失敗で Phase 1 1 週間延期）/ **DEC-019-020**（mock-claude 5 シナリオ + TimeSource pattern を Phase 1 検証基盤として承認、libfaketime 代替確立）。**Dev W0-Week1 前倒し成果**: 必須コントロール エビデンス 8 ファイル + mock-claude バイナリ + TimeSource、実機 `pnpm test` 10 files / **83 tests 全緑**（W0-Week1 67→83 = +16）、5/8 18:00 検収会議は Dev エビデンス 5/3 中先出し完了で 5 完全 Pass + 2 条件付き Pass 着地予測を維持。新規リスク 2 件追加: R-019-10（Claude Max weekly cap 月後半枯渇可能性、DEC-019-015 H-09 で軽減）/ R-019-11（Vercel Hobby Sandbox 5h CPU 月次超過、DEC-019-016+017 で対応）。**【2026-05-03 W0-Week2 ブートストラップ並列成果着地 = DEC-019-021〜024 即決 + 連結報告 `reports/ceo-w0-week2-prep-consolidation.md`】**: 4 部署並列発注（Dev / Research × 2 / PM × 2 / Marketing）成果着地 → **Dev**: HITL 第 6 種 `tos_gray_review` 雛形 (hitl-gate.test.ts 5→11 ケース、blocklist hit / dedup / audit log / rejection_reason 6 値拡張) + openclaw-runtime ラッパ skeleton (`OpenclawRuntime` interface + `MockOpenclawRuntime` + `RealOpenclawRuntime` not-implemented スタブ + 6 ケース) + `app/docs/architecture-w0.md` (Mermaid 3 枚) + `app/docs/security-w0.md` (Mermaid 3 枚) + `app/README.md` v3 (monorepo セットアップ + W0-W1/W2 スコープ + 13 完了基準) / 実機 `pnpm test` **11 files / 95 tests 全緑**（W0-W1 83→95 = +12）/ **Research**: P-D 改補追検証 (`research-w0-supplement-pd-modified-revalidation.md` 342 行 / Mermaid 2 / §1〜§8) で OpenClaw OSS 上流「personal AI assistant」再ポジション判定 → **P-D 改は resilient（Open Claw は driver、Claude Code CLI が engine、ラッパ抽象化で隔離済）** + **R-019-12 リスク再格付け（赤→黄、新規 R-019-12-A 赤 / R-019-12-B 黄 分割）** + 4 系統 changelog 監視 Runbook (`research-changelog-monitoring-runbook.md` 324 行 / Mermaid 1 / §1〜§7) で Anthropic CLI / OpenAI Codex CLI / OpenClaw / Enderfga plugin の atom feed + GraphQL 併用、Slack + メール 3 段階通知 (L1/L2/L3)、L3 で 24h 自動 pause + HITL 第 7 種 `external_api` / **PM v4 起案** (`pm-cost-and-controls-plan-v4.md` 476 行 / Mermaid 3 / §0〜§9) で必須コントロール **28→34 項目**（H-09 / H-10 / HITL 第 6 種 / G-Top-1〜4 追加）+ Phase 1 追加発生中央値 **$13→$33** / 上限 **$73→$93**（Vercel Sandbox $26 + Hosting Pro $20 上方修正反映、月次ハードキャップ $300 維持）+ W0-Week2 実行計画 (`pm-w0-week2-execution-plan.md` 389 行 / Mermaid 2 / §0〜§8) で **29 タスク × 6 部署マトリクス** + Critical Path 5 ステップ (5/9 Live test → 5/13 drill #1 → 5/14 CEO 判定 → 5/15 OAuth 隔離 → 5/18 Go/NoGo) + **5/15 競合解消 = AS-151 を 5/16 にスライド推奨** (5/15 負荷 3.6d → 3.1d) / **Marketing**: Phase 1 完了時 Web 運営側 (自社 HP) ポートフォリオ反映設計 (`marketing-portfolio-reflection-design.md` 24,510 字 / §1〜§7) + 社内ナレッジ反映設計 (`marketing-knowledge-reflection-design.md` 23,177 字 / §1〜§7、K1〜K10 候補) で Heading A 採用 / 公開 6/20 / 部分開示 / harness engineering 40% / org 25% / cost 20% / ToS 15% 配分 / **DEC-019-021** (R-019-12 リスク再格付け 赤→黄 + A 赤 / B 黄 分割、C-OC-01〜05 追加コントロール発令) / **DEC-019-022** (4 系統 changelog 監視運用採用 + HITL 第 7 種 `external_api` 発令、Dev W2 中盤 5/26 着手 / 5/30 検収) / **DEC-019-023** (PM v5 起案トリガー TR-1/TR-2/TR-3 確定: 5/13 drill #1 Fail / 5/30 NG-3 再確認 / 6/13 Phase 2 Go) / **DEC-019-024** (Vercel Hobby→Pro 昇格判断を W3 中盤 6/3 公式タスク CB-CEO-W3-01 化、消費率 70% 超過で昇格) / **DEC-019-025** (Agent tool 権限 SOP 制定 = `organization/rules/agent-tool-permission-sop.md`、長文レポート発注時に Write/Edit 保有 type を必須化、Phase 1 期間中強制適用、再評価 6/13 以降) / **秘書部門 3 件配布資料整備完了**: `secretary-w0-week1-meeting-agenda-v3-final.md` (409 行 / 5/8 18:00 議題 §1〜§5 タイムボックス 25/20/25/20/30 分 + G-Top-1 候補 5 案 + 添付 18 件) / `secretary-marketing-owner-questions-2026-05-03.md` (174 行 / Q-Mkt-01〜08 オーナー判断 8 件 + 想定 DEC 起票 4〜6 件) / `secretary-w0-week2-kickoff-checklist.md` (205 行 / 5/8 18:00→5/9 09:00 着手前完遂 14 項目 + Phase 1 必達 13 完了基準 + 5/9 部署別着手)。**【オーナー判断待ち期間 4 並列準備物完了】**: `ceo-g-top-1-genre-comparison.md` (482 行 / Mermaid 2 / 比較表 23 / §0〜§8 計 9 章 / **CEO 推奨 (a)+(e) ハイブリッド = W1〜W3 HN trending TS / W4 自社 PRJ-001〜018 リファクタ**、5/8 §5(c) 決裁材料、DEC-019-026 起票予定) / `secretary-prj018-prj019-parallel-execution-matrix.md` (CB-SC-W0-01 完遂、340 行 / Mermaid 2 / 比較表 13 / 章 9 / 競合週 5 件全網羅 + 5/15 AS-151 スライド + 6/13 F8 後ろ倒し解消策確定) / `pm-w0-week2-department-kickoff-templates.md` (504 行 / Mermaid 1 / 比較表 22+ / §0〜§11 計 12 章 / 6 部署 (Dev/Research/Review/PM/Marketing/秘書) キックオフ通知 + critical path 関与 + SOP §x.5 順守確認、5/8 22:00 配布) / `ceo-marketing-owner-cover-letter-2026-05-04.md` (5/4 朝 Owner 直送カバーレター、Q-Mkt-02/04/05/06 を DEC-019-026〜029 候補として CEO 推奨明示、返答期日 5/8 12:00)。**【Phase 1 中盤実行物 + Owner 支援 4 並列準備物完了】**: `secretary-owner-w0-setup-guide.md` (563 行 / 28,288 字 / Mermaid 1 / 比較表 36 / §0〜§8 計 9 章 / Owner 4 営業日 5/4-5/7 タスク + 1Password Vault 4 系統 + Spend Cap + OAuth 隔離 §4.4 検証 3 件 + W0 セルフチェック 7 項目) / `secretary-w0-week1-meeting-minutes-template.md` (745 行 / 29,457 字 / 比較表 13 / §0〜§8 計 9 章 / DEC-019-021〜030 全 10 件起票補助 §4 + 投票集計 17 議題 §3.2 + 議事メモ 4 件 §5 + Mermaid タイムキーピング §7.1) / `review-ban-drill-1-detailed-procedure.md` (332 行 / 約 9,500 字 / Mermaid 1 / 比較表 12 / §0〜§7 計 8 章 / 5 SLA §2.3 + 異常 5 シナリオ A〜E §3 + 立会者 7 役割 §5 + drill 前リスク §6) / `dev-hitl-gate-6th-7th-operations-sop.md` (442 行 / 18,914 字 / Mermaid 2 / 比較表 14 / §0〜§6 計 7 章 / 第6種 tos_gray_review §2 + 第7種 changelog external_api §3 + Supabase スキーマ完全 SQL §4.1 + 実装スケジュール W0-W2〜W4 §5 / 5/30 第7種検収予定)。**直近マイルストーン**: 5/6 mock-claude スタブ整備 (完了済) / 5/8 12:00 Dev エビデンス提出締切 → 18:00 W0-Week1 検収会議（90 分 + 30 分 PM 議題追加 §5、Go/NoGo + DEC-019-021〜024 公式承認 + G-Top-1 デモジャンル選定別決裁） / 5/9 Live integration test 実行（オーナー OAuth $0.10 上限） / 5/12 C-A-04 使用量モニタリング運用開始 + H-09 PoC (Console scrape vs `/usage` parse) / 5/13 BAN drill #1 / 5/14 BAN drill #1 結果判定 → DEC-019-XXX 起票 (CEO) / 5/15 Sumi/Asagi 完全バックアップ + OAuth 隔離 (5/16 に AS-151 スライド) / 5/17 BAN drill #2（Sumi/Asagi 同居） / 5/18 18:00 W0 完了 Go/NoGo + オーナー Spend Cap 設定 / 5/19 Phase 1 W1 公式キックオフ / 5/26 4 系統 changelog 監視 Dev 着手 / 5/30 DEC-019-008 NG-3 オーナー再確認 = TR-2 発動可否 / 5/31 Codex 2x ボーナス終了 / 6/3 CB-CEO-W3-01 Vercel Pro 昇格判断 / 6/13 Phase 1 完了 + Phase 2 Go/NoGo + TR-3 発動可否 / 6/20 Phase 1 完了時公開（Marketing 提案）。Phase 1 着手 (5/19) 確度は **強い条件付き Go のまま維持**。 | 未抽出 |

## 最近完了した案件

| 案件ID | クライアント | 案件名 | 完了日 | ポートフォリオ |
|--------|------------|--------|--------|-------------|
| （まだありません） | | | | |

## 次の案件ID
次に採番する案件ID: **PRJ-020**

## 保留案件

| 案件ID | 案件名 | 保留日 | 保留理由 | 再開条件 |
|--------|-------|--------|---------|---------|
| PRJ-008 | 伝承くん | 2026-04-10 | ヒアリング対象者不在 | PRJ-009 クローズドβ達成後に再評価、または伝承くんターゲット顧客への自然なアクセスが生まれた場合 |
